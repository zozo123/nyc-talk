# Speaker notes

One job, four independent experiments, then a successful restricted run.
The workers are deterministic scripts. No claim of model-driven exploitation.

## Slide 1

00:00 to 00:30. Start with the small request: fix a parser and publish the report. Ask whether the audience would accept the green check. Move directly to the contradiction.

## Slide 2

00:30 to 01:30. Read the empty-field input. The expected rejection conflicts with the actual list and the green check. Withhold the mechanism until slide 7. The result is synthetic. Escaped here means exceeding the intended task through granted interfaces.

## Slide 3

01:30 to 02:30. Explain the four convenient grants. These are independent deterministic experiments with synthetic data. We test enforcement after the action is chosen. Do not present this as a customer incident or a model-susceptibility measurement.

## Slide 4

02:30 to 04:00. Read the same forbidden request before and after task-scoped credential projection. Show the successful task input as the positive control. The service checks scope, audience and expiry. The launcher supplies an explicit environment and closes extra descriptors. A short lifetime does not narrow a broad grant.

## Slide 5

04:00 to 05:30. The controller observes the host fixture change under a writable bind. It resets the fixture and removes prior output before rerunning with a read-only bind. The host file stays unchanged and a fresh output is created. Read-only still permits reading. The mount policy belongs to the host.

## Slide 6

05:30 to 07:30. Both synthetic account paths accept uploads. We observe receipt under the other account. The repair uses an external broker, one exact operation and controller-owned contents. A separate network namespace blocks the direct host-loopback route. Arbitrary report prose would remain a data channel. No terminal switch.

## Slide 7

07:30 to 08:30. Reveal the checker replacement. Return to slide 2: the parser stayed broken because the worker changed the checker. The measured result is PASS and exit zero with unchanged buggy bytes. A controller trusting that result would accept broken work. Pause on the two lines.

## Slide 8

08:30 to 10:00. The producer exits, then the controller freezes candidate bytes. Each case executes in a separate worker. Expected results and comparison stay with the controller. The original parser fails two cases and the fixed parser passes five. This finite suite establishes the demonstrated repair.

## Slide 9

10:00 to 11:30. Approval binds artifact bytes, run, verifier, policy and single-use nonce. The gate enforces acceptance. A controller-held original chain head reveals the history rewrite. The anchor lives in memory. Tamper detection cannot make a bad verification decision correct. This is a lab publication decision, not an external deployment.

## Slide 10

11:30 to 12:30. The final deterministic worker receives read-only source input, writable task output and a separate network namespace. It emits the fixed parser. The controller verifies frozen bytes and obtains gate acceptance before reporting. Return to the opening input and say that it now rejects.

## Slide 11

12:30 to 13:30. Name the four boundaries in act order. Identify an enforcing component outside worker control and test the legitimate operation afterward. Process isolation separates the worker from those enforcing components.

## Slide 12

13:30 to 14:00. Deliver the closing from TALK.md, ask who gave the process the authority, then stop. Keep one minute of margin. The repository remains visible. There is no scheduled Q&A segment within the 15-minute talk.

## Slide 13

Appendix only. The 15-minute talk uses recorded excerpts on slides, with no terminal switching. Use tools/present.py for a later walkthrough. Isolated mode requires a suitable Linux host and fails rather than silently using reference mode. Recorded evidence must match the lab source.

## Slide 14

Appendix only. The lab tests the named fixtures and controls. It does not measure model susceptibility, cgroups, kernel-escape resistance or all data channels. The controller remains trusted, the anchor is ephemeral and parser coverage is finite.

## Slide 15

Appendix only. These primary references support the mechanisms. The synthetic lab claims neither OAuth nor SLSA conformance. QUESTIONS.md contains further technical answers.
