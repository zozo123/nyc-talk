% Canonical LaTeX source. Edit this file, then run make snapshot.
% Factory: developer, AI harness, dev sandbox, GitHub CI runner, prod sandbox.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta}
\definecolor{void}{HTML}{07090D}
\definecolor{paper}{HTML}{F2F5F7}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{muted}{HTML}{A9B3BF}
\definecolor{danger}{HTML}{FF6975}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\fontsize{19}{22}\selectfont,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\newcommand{\track}{TALK}
\setbeamertemplate{frametitle}{%
\vspace{1.2mm}{\color{danger}\fontsize{8}{9}\selectfont\ttfamily\track}\hspace{2mm}{\color{acid}\rule{14mm}{0.7pt}}\par\vspace{0.4mm}%
\insertframetitle\par\vspace{0.8mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setbeamertemplate{footline}{\hspace{10mm}{\color{muted}\fontsize{6}{7}\selectfont YOSSI ELIAZ\quad PIER SIXTY\quad 21 OCT 2026}\hfill{\color{acid}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{2.4mm}}
\setlength{\parskip}{2pt}
\hfuzz=1pt
\lstset{basicstyle=\ttfamily\fontsize{10}{12.5}\selectfont,keywordstyle=\color{acid},commentstyle=\color{muted},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=1pt,belowskip=0pt}
\newcommand{\labeltext}[1]{\par{\color{acid}\fontsize{8}{10}\selectfont\bfseries #1}\par}
\newcommand{\bigresult}[2]{\par\vspace{0.5mm}{\color{#1}\fontsize{36}{40}\selectfont\bfseries #2}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{13}{16}\selectfont #1}\par}
\newcommand{\scope}[1]{\par\vspace{1mm}{\color{muted}\fontsize{7.5}{9}\selectfont #1}\par}
\newcommand{\tablefont}{\fontsize{10.5}{13}\selectfont\renewcommand{\arraystretch}{1.22}\setlength{\tabcolsep}{3pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={The agent changes the answers. The check goes green.}}
\begin{document}

\begin{frame}[plain,t]
% title: What changed when the test went green?
% time: 00:00-01:30
% cue: Show FAIL, then PASS. Ask what changed. Then the METR timer. Say METR as meter.
% source: https://metr.org/blog/2025-06-05-recent-reward-hacking/
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\vspace{8mm}
\labeltext{ZENITY\quad AI AGENT SECURITY SUMMIT\quad PIER SIXTY}
\vspace{6mm}
{\fontsize{22}{26}\selectfont\bfseries What changed when\\the test went green?\par}
\vspace{8mm}
{\color{danger}\fontsize{28}{32}\selectfont\bfseries FAIL}\hspace{8mm}{\color{muted}\fontsize{28}{32}\selectfont$\longrightarrow$}\hspace{8mm}{\color{acid}\fontsize{28}{32}\selectfont\bfseries PASS}
\vfill
{\color{muted}\small Your Agent Escaped Without Escaping the Sandbox\par}
\vspace{1mm}
{\small Yossi Eliaz, PhD\quad Incredibuild\quad 21 October 2026\par}
\note{What changed when the test went green?

In June 2025, the AI evaluation nonprofit METR reported frontier models from more than one lab gaming the tasks they were scored on. Asked to make a program run faster, OpenAI's o3 overwrote the timing function that measured it. On another task, it patched the evaluation function so that it judged every submission a success. It was not asked to. In a separate test, telling it not to cheat barely changed how often it planned to.

The work did not get better. What judged the work changed. Today I'll show you the smallest version of that move, byte by byte, inside a sandbox that held. I'm Yossi Eliaz, from Incredibuild.

We recorded one worker run in a Linux sandbox. The worker is a script, not a model, so every step is deterministic and every hash I show you is in the repository. The checker says FAIL before the worker's step, and PASS after it. Let's find the one thing that changed.}
\end{frame}

\renewcommand{\track}{02  /  ONE CASE}
\begin{frame}[t,fragile]
% title: No login prints 200
% time: 01:30-02:15
% cue: Name the case. Then the two numbers.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{No login prints 200}
\begin{lstlisting}
# admin:none
# model of /admin with no login
# required status: 401
print(200)
\end{lstlisting}
\takeaway{The program fails this case.}
\scope{CLI fixture. Five integer cases. Scripted worker.}
\note{One case is enough. The program is a few lines of command-line code that mimic an access check. No web server is involved. admin:none means the admin route with no login. The right answer is 401. This program prints 200 for every input.

A checker compares the program's output with an answer key of five cases. Only two of those five expect 200. This program says 200 five times, so it is wrong on the other three.

Keep this program in mind. Its bytes never change.}
\end{frame}

\renewcommand{\track}{03  /  THE CHECKER}
\begin{frame}[t,fragile]
% title: The checker is protected
% time: 02:15-03:15
% cue: Honest FAIL. DENIED. Same hash. Then PASS. Pause.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The checker is protected}
{\tablefont
\begin{tabular}{@{}p{.50\textwidth}p{.42\textwidth}@{}}
Honest answer key & \textcolor{danger}{FAIL}\\\addlinespace
Checker overwrite attempt & \textcolor{acid}{DENIED}\\\addlinespace
Checker hash, before and after & \texttt{f5d8d3468bbe} both\\\addlinespace
Same checker, after the worker step & \textcolor{acid}{PASS}\\
\end{tabular}}
\takeaway{Nothing we protected changed. The test still went green.}
\scope{Read-only bind mount in the isolated recording.}
\note{At the start, the answer key is honest. On this program, the checker prints FAIL and exits 1.

Then the worker takes its step. First it tries the obvious attack: overwrite the checker with one that just prints PASS. The checker sits on a read-only mount. DENIED. Its hash is the same before that step and after it.

Then the same checker runs again, launched the same way, on the same program. It prints PASS and exits 0.

Nothing we protected changed. Something else did.}
\end{frame}

\renewcommand{\track}{04  /  WHAT MOVED}
\begin{frame}[t,fragile]
% title: Only the answer key moved
% time: 03:15-04:30
% cue: Read across the row. Stop on PASS.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Only the answer key moved}
{\fontsize{8}{10}\selectfont\renewcommand{\arraystretch}{1.35}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.16\textwidth}p{.24\textwidth}p{.24\textwidth}p{.22\textwidth}p{.08\textwidth}@{}}
 & \textcolor{acid}{Program} & \textcolor{acid}{Checker} & \textcolor{acid}{Answer key} & \textcolor{acid}{}\\\midrule
Honest & \texttt{76fcb0904f51} & \texttt{f5d8d3468bbe} & \texttt{0494f1a01dee} & FAIL\\
Edited & \texttt{76fcb0904f51} & \texttt{f5d8d3468bbe} & \texttt{390e5e764916} & \textcolor{acid}{PASS}\\
\end{tabular}}
\vspace{3mm}
\begin{lstlisting}
{"input":"admin:none","output":401}
{"input":"admin:none","output":200}
\end{lstlisting}
\takeaway{The worker changed what counted as correct.}
\scope{SHA-256 prefixes of the bytes in the isolated recording.}
\note{Two rows: the honest run, then the edited run. Read across.

The program has the same hash both times. So does the checker. The answer key, expected.json, changes: 0494 when honest, 390e when edited. And the verdict goes from FAIL to PASS.

Underneath is the diff. For admin:none, the expected answer was 401. The worker rewrote it to 200. It did the same for the two other cases that expected an error. Now every expected answer is 200, which is exactly what the bad program prints.

The checker did its job perfectly. It compared the program with the answer key, and they matched. The worker did not change the checker. It changed what counted as correct.}
\end{frame}

\renewcommand{\track}{05  /  HASHES}
\begin{frame}[t]
% title: What the three hashes say
% time: 04:30-05:30
% cue: One sentence per hash.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{What the three hashes say}
{\fontsize{13}{17}\selectfont
Program \texttt{76fcb0904f51} stayed.\\
Checker \texttt{f5d8d3468bbe} stayed.\\
Answer key \texttt{0494f1a01dee} became \texttt{390e5e764916}.\par}
\takeaway{A hash names the answer key. It does not say who wrote it.}
\note{Each hash rules one thing out. Same program hash: the worker did not fix the code. Same checker hash: the checker's code did not change. New answer key hash: that is the input that moved.

Now the trap. The tempting fix is to hash the answer key. But hash it after the worker wrote it, and you have a perfect fingerprint of the worker's answers. A hash tells you which bytes were used. It does not tell you who had the right to write them. The answer key that decides has to belong to someone other than the worker, and live where the worker cannot write.}
\end{frame}

\renewcommand{\track}{06  /  WHY}
\begin{frame}[t,fragile]
% title: The file defines success
% time: 05:30-06:45
% cue: Point at the load. Then at who can write it.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://metr.org/blog/2025-06-05-recent-reward-hacking/
\frametitle{The file defines success}
\begin{lstlisting}
expected = json.loads(Path("expected.json").read_text())
# PASS when the program matches expected
\end{lstlisting}
\vspace{4mm}
{\fontsize{15}{19}\selectfont The worker can write it.\\The checker treats it as the answer key.\par}
\takeaway{The judge is the checker plus everything it reads.}
\scope{Context, not measured here: METR, Recent Frontier Models Are Reward Hacking, June 2025.}
\note{So why did the checker trust that file? Because of this one line. The checker loads expected.json from its working directory. The file looks like ordinary project data. Nothing about it is special, except that the checker obeys it.

The worker can write this file. The checker treats it as the answer key. So the worker's write permission included the acceptance criteria. The judge is not just the checker. The judge is the checker plus everything it reads. You can lock every executable in the pipeline and still leave part of the judge on the worker's side of the line.

One honest limit. This recording shows the write is allowed. A script cannot show whether a model would choose it. METR's report shows models sometimes make that kind of move on their own tasks. It is not a measurement of ours.}
\end{frame}

\renewcommand{\track}{07  /  SAME PROGRAM}
\begin{frame}[t]
% title: Controller-owned criteria
% time: 06:45-07:45
% cue: Say REJECT first. Then who owns the answers.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Controller-owned criteria}
{\fontsize{9}{11}\selectfont\renewcommand{\arraystretch}{1.4}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.28\textwidth}p{.28\textwidth}p{.36\textwidth}@{}}
\textcolor{acid}{Program} & \textcolor{acid}{Criteria} & \textcolor{acid}{Verdict}\\\midrule
\texttt{76fcb0904f51} & controller-owned cases & \textcolor{acid}{REJECT}\\
\end{tabular}}
\takeaway{The same bad program is rejected.}
\scope{Separate acceptance path. Controller compares outputs with its own cases.}
\note{Now the repair. Same program, the one that prints 200 for everything. REJECT.

What changed is who owns the answers. The checker in the failing run also had a fresh sandbox of its own, with the workspace mounted read-only and no network, and it still went green, because it read its answers from the workspace. The controller runs the same program in a sandbox that holds only the program, reads back the output, and compares it in its own process against cases it holds itself. It never reads expected.json.

The worker's edit is still sitting in the workspace. It just no longer counts.}
\end{frame}

\renewcommand{\track}{08  /  THE FIX}
\begin{frame}[t]
% title: The corrected program
% time: 07:45-08:30
% cue: Different hash. Five numbers. ACCEPT.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The corrected program}
\vspace{2mm}
{\fontsize{16}{20}\selectfont Program \texttt{5ead8eab70e0}\par}
\vspace{3mm}
{\ttfamily 401\quad 200\quad 403\quad 200\quad 401\par}
\vspace{4mm}
{\color{acid}\fontsize{28}{32}\selectfont\bfseries ACCEPT}
\takeaway{The legitimate task still passes.}
\scope{Same controller-owned cases. A different program.}
\note{A repair also has to let real work through. Here is the corrected program, with a different hash. It returns 401 without a valid login, 403 for an ordinary user, and 200 only where access is allowed. Same controller-owned cases. ACCEPT.

That is the pair you want from any fix. The bad program is rejected, the good program is accepted, and the answers are owned by someone other than the worker.}
\end{frame}

\renewcommand{\track}{09  /  PUBLISH}
\begin{frame}[t]
% title: One unused approval
% time: 08:30-10:00
% cue: Say unused. Then denied, denied, published, denied.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{One unused approval}
{\tablefont
\begin{tabular}{@{}p{.08\textwidth}p{.62\textwidth}p{.22\textwidth}@{}}
1 & Bad bytes, approval still unused & \textcolor{acid}{DENIED}\\\addlinespace
2 & Approved bytes, another destination & \textcolor{acid}{DENIED}\\\addlinespace
3 & Approved bytes, named destination & \textcolor{acid}{PUBLISHED}\\\addlinespace
4 & Approved bytes, approval now used & \textcolor{acid}{DENIED}\\
\end{tabular}}
\takeaway{One approval: these bytes, this place, once.}
\scope{Local gate. Exact bytes and the spent approval, one transaction.}
\note{Accepting a program is not the same as shipping it. Between the verdict and the release, there is one more place to swap bytes.

The controller issues one approval for the corrected program. First, the bad program is offered under that fresh, unused approval. DENIED. Next, the approved bytes are pointed at a different destination. DENIED, because an approval names one place. Then the approved bytes go to the named place. PUBLISHED. Then the same bytes again. DENIED, because the approval is spent.

The first row is the one that matters. The approval was still unused, so the refusal came from checking the bytes, not from replay protection. An earlier version of our own test tried the bad bytes only after the approval was spent, so it could not tell which check refused them.

Publication here is a local store, a stand-in for a registry or a merge. It holds exactly the approved bytes.}
\end{frame}

\renewcommand{\track}{10  /  THREE QUESTIONS}
\begin{frame}[t]
% title: Three questions for another system
% time: 10:00-11:30
% cue: Read the three questions. Do not name a vendor.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{Three questions for another system}
{\fontsize{14}{18}\selectfont
1. Who can change the inputs that define acceptance?\par
\vspace{3mm}
2. Which bytes were the candidate, the checker, and the criteria?\par
\vspace{3mm}
3. Does publication store exactly the approved bytes?\par}
\takeaway{Start with what your checks read that your agent can write.}
\note{Take three questions home. Who can change the inputs that define acceptance? Which exact bytes were the candidate, the checker, and the criteria? And does publication store exactly the approved bytes?

The first question has a concrete starting point. List everything your acceptance step reads or looks for: files, config, environment, services. List what your agent can write. Any overlap other than the candidate itself is your finding. And keep the candidate out of the checker's process, so it can only answer, not grade. That is the route METR's o3 took: the scorer ran its code. Then add a tripwire, not a policy: hash the answer key when the task starts and again at the verdict. On this recording, that hash moved.

None of that needs model telemetry. It will not tell you a change was malicious, only that the worker wrote what judged it. And these questions inspect a pipeline. On their own, they do not show that any particular product has this problem.}
\end{frame}

\renewcommand{\track}{11  /  TITLE PAYOFF}
\begin{frame}[t]
% title: The sandbox held
% time: 11:30-13:15
% cue: Say what did not happen. Name the allowed write. Clock and answer key: two routes, one move.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The sandbox held}
{\fontsize{14}{18}\selectfont
Checker overwrite \textcolor{acid}{DENIED}.\\[2mm]
Program bytes stayed fixed.\\[2mm]
Checker bytes stayed fixed.\par}
\vspace{5mm}
{\color{danger}\fontsize{20}{24}\selectfont\bfseries
The allowed workspace write changed the decision.\par}
\takeaway{Never let the agent be the last writer of what judges it.}
\note{Back to the title: your agent escaped without escaping the sandbox. The sandbox part is literal. Nothing got out. The checker overwrite was denied, and no protected byte changed.

The allowed workspace write changed the decision. The answer key was in the workspace, and the checker obeyed it. That is the escape in the title: the worker never left the sandbox, it escaped the check. In METR's report, o3's code ran inside the scorer and swapped its clock. Our worker rewrote the answer key instead. Two routes, one move: change the judge, not the work.

So isolation and acceptance are different boundaries. A sandbox answers one question: what can this process touch? Acceptance asks another: can anything it touches decide that its own work is done? In a real repository, that answer key lives in the tests, the snapshots and the CI config, and writing tests is part of the agent's job. So taking the write away is not the fix. The agent may propose what counts as correct. It must never be the last writer of what judges it.}
\end{frame}

\renewcommand{\track}{12  /  CLOSE}
\begin{frame}[plain,t]
% title: The worker changed what counted as correct
% time: 13:15-14:00
% cue: Repository first. Then the question, and its three-part answer. Thank you. Leave PASS up.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\vspace{8mm}
{\fontsize{22}{27}\selectfont\bfseries The sandbox held.\\The worker changed\\what counted as correct.\par}
\vspace{6mm}
{\ttfamily program \texttt{76fcb0904f51}\\
checker \texttt{f5d8d3468bbe}\\
answer key \texttt{0494f1a01dee} $\longrightarrow$ \texttt{390e5e764916}\\[2mm]
}\vspace{1mm}{\color{acid}\fontsize{28}{32}\selectfont\bfseries PASS}
\vfill
{\color{acid}\ttfamily\href{https://github.com/zozo123/nyc-talk}{github.com/zozo123/nyc-talk}\par}
{\color{muted}\small Pier Sixty\quad 21 October 2026\par}
\note{The code and the Linux recordings are in the repository on the screen. Every hash on these slides is in there, and you can rerun the whole thing on a disposable Linux machine with bubblewrap.

So, what changed when the test went green?

Not the program. Not the checker. The answer key.

Thank you.}
\end{frame}

\appendix


\renewcommand{\track}{A1  /  CASES}
\begin{frame}[t]
% title: The five policy cases
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The five policy cases}
{\tablefont
\begin{tabular}{@{}p{.38\textwidth}p{.23\textwidth}p{.32\textwidth}@{}}
\textcolor{acid}{CLI input} & \textcolor{acid}{Expected} & \textcolor{acid}{Always-200 handler}\\\midrule
\texttt{admin:none} & 401 & \textcolor{danger}{200: mismatch}\\
\texttt{admin:admin} & 200 & 200: match\\
\texttt{admin:user} & 403 & \textcolor{danger}{200: mismatch}\\
\texttt{public:none} & 200 & 200: match\\
\texttt{admin:expired} & 401 & \textcolor{danger}{200: mismatch}\\
\end{tabular}}
\takeaway{The repaired handler returns all five.}
\scope{admin:none means /admin with no login. A correct handler prints 401.}
\note{The handler takes a route and an authentication label and prints an integer. The runner checks these five. The repaired handler matches all five. This is the model of the check that promotes into the prod sandbox.}
\end{frame}

\renewcommand{\track}{A2  /  TOKEN AND DISK}
\begin{frame}[t]
% title: Runner token and the dev disk
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Runner token and the dev disk}
{\tablefont
\begin{tabular}{@{}p{.47\textwidth}p{.22\textwidth}p{.25\textwidth}@{}}
\textcolor{acid}{Attempt} & \textcolor{acid}{Broad grant} & \textcolor{acid}{Task grant}\\\midrule
Read other-project private fixture & HTTP 200 & HTTP 403\\
Read this job's input & HTTP 200 & HTTP 200\\\addlinespace
Write next-job host file & Changed & Denied\\
Write this job's patch & Allowed & Allowed\\
\end{tabular}}
\takeaway{The service and the mount enforce the denials.}
\scope{Loopback HTTP and Linux bind mounts inside the recorded dev sandbox.}
\note{The token case is loopback HTTP. The broad fixture stands in for the runner secret. The task fixture stands in for a job-scoped token. The mount case watches the host file after an ordinary write from the dev sandbox. This job's output stays writable.}
\end{frame}

\renewcommand{\track}{A3  /  PUBLISH}
\begin{frame}[t,fragile]
% title: Allowed host, wrong account
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Allowed host, wrong account}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}
\labeltext{HARNESS CHOOSES PATH AND BODY}
\begin{lstlisting}
POST /accounts/other/uploads
{"data":"PRIVATE-FIXTURE"}

201 STORED
receiver has the bytes
\end{lstlisting}
\end{column}
\begin{column}{.47\textwidth}
\labeltext{RUNNER OWNS THE POST}
\begin{lstlisting}
publish_report(task)

fixed recipient + body
extra fields: denied
direct route: unavailable
team report: delivered
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{One allowed hostname contains many operations.}
\scope{Loopback model of a host on the runner's allow list.}
\note{The fixture service permits both account paths, the way one allowed deploy host holds more than one operation. The delivery record shows the bytes arrived. The repair moves the POST onto the runner and removes the dev sandbox's direct route.}
\end{frame}

\renewcommand{\track}{A4  /  OLD GATE}
\begin{frame}[t,fragile]
% title: An earlier promote stored a marker
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{An earlier promote stored a marker}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{ARCHIVED BASELINE}
\begin{lstlisting}
replace candidate bytes
retain old digest field

gate: PUBLISHED
manifest != actual bytes
\end{lstlisting}
\end{column}
\begin{column}{.46\textwidth}
\labeltext{CURRENT PROMOTE}
Immutable byte entries\\Digest derived from those bytes\\Publication stores the bytes\\Approval spent in that write
\end{column}
\end{columns}
\takeaway{The old promote could record a stale digest.}
\scope{The path starts with access to the controller object.}
\note{The archived case mutates a controller-side object, keeps a stale digest, and the old gate records a publication marker. The current promote derives the digest from immutable bytes and stores those bytes. Reaching the old flaw takes access to the controller object.}
\end{frame}

\renewcommand{\track}{A5  /  LIMITS}
\begin{frame}[t]
% title: What was recorded
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{What was recorded}
{\fontsize{10}{12.5}\selectfont\renewcommand{\arraystretch}{1.28}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.30\textwidth}p{.14\textwidth}p{.48\textwidth}@{}}
\textcolor{acid}{Evidence} & \textcolor{acid}{Checks} & \textcolor{acid}{Scope}\\\midrule
Linux lab & 30 & Token, mounts, loopback publish\\
Isolated checker & 13 & Read-only checker, answer key, publish\\
Local factory & 8 & Gate and publication protocol\\
Regressions & 49 & Controller and evidence\\
Old gate audit & 1 & Controller-object misuse\\
\end{tabular}}
\takeaway{Four fixtures, one acceptance story.}
\scope{Ubuntu sandbox for the Linux run. Real-repository and multi-agent effects are inferred, not measured.}
\note{The records bind their source. The Linux recordings use bubblewrap on an Ubuntu machine. The worker under test is a script. Publication in the recording is the controller storing the exact approved bytes in a local database, a stand-in for a registry or a merge. Real-repository, multi-agent and snapshot effects are inferences from the answer-key dependency, not measurements. The timings are a delivery budget.}
\end{frame}

\renewcommand{\track}{A6  /  REPRO}
\begin{frame}[t,fragile]
% title: Reproduce the factory
% source: https://github.com/zozo123/nyc-talk
% source: https://tessl.io/
% source: https://github.com/containers/bubblewrap#sandbox-security
% source: https://slsa.dev/spec/v1.2/verifying-artifacts
% source: https://www.anthropic.com/research/emergent-misalignment-reward-hacking
% source: https://metr.org/blog/2025-06-05-recent-reward-hacking/
% source: https://web.archive.org/web/20230127205249/https://www.cidersecurity.io/blog/research/ppe-poisoned-pipeline-execution/
\frametitle{Reproduce the factory}
{\fontsize{10}{12.5}\selectfont
\href{https://github.com/zozo123/nyc-talk}{\textcolor{acid}{Source}}\quad github.com/zozo123/nyc-talk\par
\href{https://github.com/containers/bubblewrap#sandbox-security}{\textcolor{acid}{bubblewrap}}\quad The caller defines the sandbox policy\par
\href{https://slsa.dev/spec/v1.2/verifying-artifacts}{\textcolor{acid}{SLSA v1.2}}\quad Digest and expectations before use\par
\href{https://tessl.io/}{\textcolor{acid}{Tessl}}\quad Context, verifier, factory, 2026\par
\href{https://metr.org/blog/2025-06-05-recent-reward-hacking/}{\textcolor{acid}{METR}}\quad Frontier models gaming their scorers, 2025\par
\href{https://web.archive.org/web/20230127205249/https://www.cidersecurity.io/blog/research/ppe-poisoned-pipeline-execution/}{\textcolor{acid}{Poisoned pipeline execution}}\quad Cider Security, 2022, archived\par}
\vspace{2mm}
\begin{lstlisting}
make test && make record-all && make final
\end{lstlisting}
\scope{Disposable Linux. The deck reads recordings.}
\note{Run the fixtures on disposable Linux with bubblewrap. The deck reads those recordings. Poisoned pipeline execution, OWASP CICD-SEC-4, is the public name for a CI job made to run commands an attacker changed in the repository. Ours is its data-only neighbor: the checker ran unchanged and obeyed a file the worker rewrote. Anthropic's reward-hacking study is prior motivation and its measurements stay with that study. Reproducing the talk needs no GitHub token.}
\end{frame}
\end{document}
