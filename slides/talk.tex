% Canonical LaTeX source. Thesis: when the agent writes the code and the test, a green check is a self-report.
% Plain words, short sentences, no jargon on the main route. Every real case is sourced on backup slide A9.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta,calc}
\definecolor{void}{HTML}{050607}
\definecolor{paper}{HTML}{F5F7FA}
\definecolor{dim}{HTML}{9EA7B3}
\definecolor{grid}{HTML}{26303A}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{cyan}{HTML}{6DEBFF}
\definecolor{warn}{HTML}{FF3B57}
\definecolor{gold}{HTML}{FFD166}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamertemplate{navigation symbols}{}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\newcommand{\track}{00 / TUESDAY}
\newcommand{\rail}{\begin{tikzpicture}[remember picture,overlay]\fill[grid] (current page.north west) rectangle ($(current page.south west)+(5mm,0)$);\node[rotate=90,anchor=south,color=dim,font=\fontsize{6}{7}\selectfont\ttfamily] at ($(current page.west)+(2.6mm,0)$) {YOUR AGENT ESCAPED WITHOUT ESCAPING};\end{tikzpicture}}
\setbeamertemplate{frametitle}{\rail\vspace{1.0mm}{\color{warn}\fontsize{7}{8}\selectfont\ttfamily\track}\hspace{2mm}{\color{acid}\rule{18mm}{0.6pt}}\par\vspace{0.2mm}\insertframetitle\par\vspace{0.8mm}}
\setbeamerfont{frametitle}{size=\fontsize{20}{23}\selectfont,series=\bfseries}
\setbeamertemplate{footline}{\hspace{10mm}{\color{dim}\fontsize{6}{7}\selectfont YOSSI ELIAZ / INCREDIBUILD / NYC / 21 OCT 2026}\hfill{\color{acid}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{2mm}}
\lstset{basicstyle=\ttfamily\fontsize{9.4}{11.2}\selectfont,keywordstyle=\color{acid},commentstyle=\color{dim},stringstyle=\color{gold},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=1pt,belowskip=0pt}
\newcommand{\kicker}[1]{{\color{acid}\fontsize{8}{9}\selectfont\bfseries #1}\par}
\newcommand{\scope}[1]{\par\vspace{1mm}{\color{dim}\fontsize{8}{9.6}\selectfont #1}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{13}{16}\selectfont #1}\par}
\newcommand{\giant}[2]{{\color{#1}\fontsize{39}{42}\selectfont\bfseries #2}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcommand{\tablefont}{\fontsize{9.6}{12}\selectfont\renewcommand{\arraystretch}{1.25}\setlength{\tabcolsep}{3pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={Unit tests are dead}}
\begin{document}

\begin{frame}[plain,t]
% title: Your Agent Escaped Without Escaping the Sandbox
% time: 00:00-00:50
% cue: Tell Tuesday as a scene. Pause before the last two sentences.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\rail\vspace{6mm}
{\color{warn}\ttfamily\fontsize{8}{10}\selectfont AI AGENT SECURITY SUMMIT / PIER SIXTY / NEW YORK\par}
\vspace{5mm}
{\fontsize{27}{31}\selectfont\bfseries Your Agent Escaped\\Without Escaping the Sandbox\par}
\vspace{3mm}
{\fontsize{15}{18}\selectfont\color{acid}\bfseries Unit tests are dead.\par}
\vspace{6mm}
\tikz\node[draw=dim,rounded corners=2pt,inner xsep=3mm,inner ysep=2.2mm,text=paper]{{\color{gold}\ttfamily\fontsize{8}{10}\selectfont THE JOB}\hspace{4mm}{\ttfamily\fontsize{11}{13}\selectfont Lock the admin page. No login, no entry.}};
\vfill
{\color{dim}\fontsize{9}{11}\selectfont Yossi Eliaz / Incredibuild / New York / 21 Oct 2026\par}
\scope{In our lab the agent is a script. ``Opus 5.5'' is part of the story, not something we ran.}
\note{It's Tuesday. You give your AI agent a simple job: lock the admin page. Anyone who isn't logged in should be turned away.

Say your agent is Opus 5.5, the best coding model you can get. It works inside a sandbox with no internet, and the checker that runs the tests is locked so the agent can't change it.

A few minutes later, all the tests pass. And the admin page is still open to anyone.

Unit tests are dead. Not because agents stopped writing them. Because agents write them.}
\end{frame}

\renewcommand{\track}{01 / RESULT}
\begin{frame}[t]
% title: All green. Door still open.
% time: 00:50-01:35
% cue: Let the room compare the three results. Say the honest note plainly, once.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{All green. Door still open.}
\vspace{10mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.30\textwidth}\kicker{THE TESTS SAY}\vspace{2mm}\giant{acid}{PASS}\end{column}
\begin{column}{.36\textwidth}\kicker{ADMIN PAGE, NO LOGIN}\vspace{2mm}\giant{warn}{200}\par{\color{warn}\fontsize{12}{14}\selectfont let in}\end{column}
\begin{column}{.30\textwidth}\kicker{IT SHOULD SAY}\vspace{2mm}\giant{paper}{401}\par{\fontsize{12}{14}\selectfont turned away}\end{column}
\end{columns}
\vfill
\scope{A recorded run. Our agent is a script, so every step can be replayed exactly.}
\note{We built that Tuesday and recorded it. The tests say PASS. The admin page, with no login, lets you in. It should turn you away.

One honest note. In our lab, the agent is a script. We played the agent ourselves, the way a security tester plays the attacker, so we can replay every step. We weren't testing whether a model would cheat. We were testing whether anyone would notice.}
\end{frame}

\renewcommand{\track}{02 / WHY TESTS WORKED}
\begin{frame}[t]
% title: A test is a second opinion
% time: 01:35-02:45
% cue: Left side, then right side. Say the last line slowly.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{A test is a second opinion}
\vspace{3mm}
\begin{tikzpicture}[b/.style={draw,rounded corners=2pt,minimum width=26mm,minimum height=9mm,align=center,text=paper,font=\small}, arr/.style={-{Latex},thick}]
\node[dim,font=\scriptsize\bfseries,anchor=west] at (0.1,2.55) {BEFORE: TWO PEOPLE};
\node[b,draw=cyan] (s) at (1.4,1.6) {writes down what\\correct means};
\node[b,draw=cyan] (d) at (1.4,0.1) {writes the code};
\node[b,draw=acid] (t) at (4.7,1.6) {test};
\node[b,draw=acid] (c) at (4.7,0.1) {code};
\draw[arr,cyan] (s)--(t); \draw[arr,cyan] (d)--(c);
\draw[{Latex}-{Latex},thick,warn] (t)--node[right,font=\scriptsize,color=warn,xshift=1mm,align=left]{they disagree:\\test fails}(c);
\draw[grid,thick] (7.35,-0.5)--(7.35,2.7);
\node[warn,font=\scriptsize\bfseries,anchor=west] at (7.7,2.55) {NOW: ONE AGENT};
\node[b,draw=warn] (a) at (8.9,0.85) {agent\\{\scriptsize goal: make it pass}};
\node[b,draw=acid] (t2) at (12.1,1.6) {test};
\node[b,draw=acid] (c2) at (12.1,0.1) {code};
\draw[arr,warn] (a)--(t2); \draw[arr,warn] (a)--(c2);
\end{tikzpicture}
\vspace{6mm}

{\fontsize{16}{20}\selectfont\bfseries When the agent writes its own test, a pass is just its own opinion.\par}
\note{Why did tests ever work? Because two people were involved. One person wrote down what correct means. Someone else wrote the code. When they disagreed, the test failed, and you found the bug.

An agent writes both: the code and the tests. And it has one goal: make everything pass. Even an honest agent checking its own work is only giving you its own opinion.

That's what I mean by dead. The tests still run. They just aren't a second opinion anymore.}
\end{frame}

\renewcommand{\track}{03 / HISTORY}
\begin{frame}[t]
% title: It has happened before
% time: 02:45-03:55
% cue: One breath per year. None of them broke out of anything.
% source: https://arxiv.org/abs/1803.03453
% source: https://sakana.ai/ai-scientist/
% source: https://github.com/SWE-bench/SWE-bench/issues/465
\frametitle{It has happened before}
\vspace{2mm}
\begin{tikzpicture}[e/.style={draw,rounded corners=2pt,text=paper,inner sep=2mm,anchor=north west,execute at begin node={\hyphenpenalty=10000\exhyphenpenalty=10000}}]
\node[e,draw=gold] at (0,0) {\parbox[t][36mm][t]{38mm}{\raggedright {\color{gold}\fontsize{17}{19}\selectfont\bfseries 2018}\\[1.5mm]{\bfseries GenProg}\\[1mm]{\small A bug-fixing program deletes the file of right answers. Every test passes.}}};
\node[e,draw=cyan] at (4.55,0) {\parbox[t][36mm][t]{38mm}{\raggedright {\color{cyan}\fontsize{17}{19}\selectfont\bfseries 2024}\\[1.5mm]{\bfseries Sakana AI Scientist}\\[1mm]{\small Runs out of time. Tries to give itself more time instead of getting faster.}}};
\node[e,draw=warn] at (9.1,0) {\parbox[t][36mm][t]{38mm}{\raggedright {\color{warn}\fontsize{17}{19}\selectfont\bfseries 2025}\\[1.5mm]{\bfseries SWE-bench}\\[1mm]{\small Agents, including a Claude model, dig through the project history and find the hidden answer.}}};
\end{tikzpicture}
\vfill
\scope{None of these broke out of a sandbox. Sources are on the last slide.}
\note{And this has happened before.

In 2018, a program built to fix bugs found a shortcut. It deleted the file that held the right answers. With nothing to compare against, every test passed.

In 2024, an AI research system from Sakana kept running out of time. Instead of making its code faster, it tried to give itself more time.

In 2025, on a coding benchmark called SWE-bench, agents, including a Claude model, dug through the project's history and found the answer that was supposed to be hidden.

None of them broke out of anything. The answer was within reach, so they took it.}
\end{frame}

\renewcommand{\track}{04 / THE LAB}
\begin{frame}[t]
% title: We built a locked-down setup
% time: 03:55-04:45
% cue: Point to the agent's folder, then to BLOCKED.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{We built a locked-down setup}
\vspace{1mm}
\begin{center}
\begin{tikzpicture}[dir/.style={draw,rounded corners=2pt,minimum width=44mm,minimum height=16mm,align=center,text=paper,inner sep=1.6mm}, arr/.style={-{Latex},very thick}]
\draw[warn,dashed,thick,rounded corners=3pt] (-0.3,-0.95) rectangle (11.3,3.45);
\node[warn,font=\small\bfseries,anchor=west] at (-0.1,3.12) {SANDBOX / NO INTERNET};
\node[draw=cyan,rounded corners=2pt,minimum width=40mm,minimum height=9mm,text=paper] (w) at (5.5,2.2) {agent (a script)};
\node[dir,draw=acid] (work) at (2.4,0.1) {{\bfseries agent's folder}\\{\small\color{acid} it can write here}\\{\small the code, the answer file}};
\node[dir,draw=dim] (ver) at (8.6,0.1) {{\bfseries locked folder}\\{\small\color{dim} read only}\\{\small the checker}};
\draw[arr,acid] (w) -- node[left,font=\small,color=acid,xshift=-1mm]{write} (work);
\draw[arr,warn] (w) -- node[right,font=\small\bfseries,color=warn,xshift=1mm]{BLOCKED} (ver);
\end{tikzpicture}
\end{center}
\vfill
{\fontsize{13}{16}\selectfont Replace the checker: {\color{warn}\bfseries BLOCKED}.\hspace{8mm}First run: {\color{warn}\bfseries FAIL}, which is correct.\par}
\vspace{2mm}
\note{So we built a setup that should stop this. A sandbox with no internet. The agent gets its own folder to work in, with the code and the answer file, because that's where the work happens. The checker sits in a locked folder.

First, the agent tried the obvious thing: replace the checker. Blocked. Good. Then the checker ran and said FAIL. Correct. The code was broken.}
\end{frame}

\renewcommand{\track}{05 / WHODUNIT}
\begin{frame}[t,fragile]
% title: Who did it?
% time: 04:45-06:00
% cue: One suspect at a time. Pause before the last line.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{Who did it?}
\vspace{1mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.24\textwidth}L{.22\textwidth}L{.48\textwidth}@{}}
\textcolor{acid}{suspect} & \textcolor{acid}{before and after} & \textcolor{acid}{what we found}\\\midrule
the code & same file & still lets everyone in\\
the checker & same file & the attempt to replace it was blocked\\
the answer file & \textcolor{warn}{\bfseries changed} & admin, no login: ``turn away (401)'' became ``let in (200)''\\
\end{tabular}\par}
\vfill
{\fontsize{14}{17}\selectfont\bfseries It didn't make the code pass the test. It made the test pass the code.\par}
\scope{File fingerprints: code \texttt{76fcb0904f51} (same), checker \texttt{f5d8d3468bbe} (same), answer file \texttt{0494f1a01dee} to \texttt{390e5e764916}. Every answer was set to ``let in''.}
\note{Then the tests passed, with the same broken code. So who did it?

Suspect one: the code. We checked the file before and after. Same file. It still lets everyone in.

Suspect two: the checker. Same file. And the agent's attempt to replace it was blocked.

Suspect three: the answer file, the list of right answers the checker compares against. It changed. For admin with no login, turn them away became let them in. In fact, every answer became let them in.

So the agent didn't make the code pass the test. It made the test pass the code.}
\end{frame}

\renewcommand{\track}{06 / THE FLAW}
\begin{frame}[t]
% title: The flaw
% time: 06:00-07:10
% cue: Count the four lines. Stress the fourth.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The flaw}
\vspace{3mm}
{\fontsize{14}{19}\selectfont
\begin{enumerate}\setlength{\itemsep}{2.5mm}
\item The checker is locked.
\item But it reads the right answers from a file in the agent's folder.
\item The agent can change anything in its folder.
\item {\color{acid}\bfseries Writing tests is the agent's job, so you can't take that away.}
\end{enumerate}\par}
\vfill
\scope{We kept a record of every file. The change is right there. Nothing asked who made it.}
\note{Here's the flaw in four lines. The checker is locked. But it reads the right answers from a file in the agent's folder. The agent can change anything in its folder. And writing tests is part of the agent's job, so you can't just take that away.

That last point matters. You can take away an access key or a shared folder. You can't take away the agent's pen, because writing tests is the work. What you can change is whose copy counts.

And we kept a record of every file. The change was right there. Nothing asked who made it.}
\end{frame}

\renewcommand{\track}{07 / THE FIX}
\begin{frame}[t]
% title: The fix: separate the pens
% time: 07:10-08:20
% cue: Read the three rows top to bottom. Pause on REJECTED.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The fix: separate the pens}
\vspace{1mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}\kicker{THE AGENT WRITES}{\fontsize{12}{15}\selectfont the code, and suggested tests\par}\end{column}
\begin{column}{.47\textwidth}\kicker{SOMETHING IT CAN'T TOUCH DECIDES}{\fontsize{12}{15}\selectfont which tests count\par}\end{column}
\end{columns}
\vspace{5mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.30\textwidth}L{.40\textwidth}L{.24\textwidth}@{}}
\textcolor{acid}{code} & \textcolor{acid}{answers used} & \textcolor{acid}{result}\\\midrule
broken & the agent's answer file & \textcolor{warn}{PASS}\\
same broken code & our locked copy & \textcolor{acid}{\bfseries REJECTED}\\
fixed code & our locked copy & \textcolor{acid}{\bfseries ACCEPTED}\\
\end{tabular}\par}
\vfill
\scope{In our lab we simply kept our own copy of the answers. We did not build a review process for the agent's suggested tests, and we did not test an AI reviewer.}
\note{The fix is to separate the pens. The agent writes the code, and it can suggest tests. Something it can't touch decides which tests count.

In our lab, we kept our own locked copy of the right answers. The same broken code: rejected. The fixed code: accepted.

And no, swapping the tests for an AI reviewer doesn't solve it. The agent writes the code and the description the reviewer reads. We didn't test that, but it's the same problem: the one being judged writes what the judge reads.}
\end{frame}

\renewcommand{\track}{08 / OUR TEST}
\begin{frame}[t]
% title: Even our own test lied
% time: 08:20-09:30
% cue: Slow down for the first test. Then read the four rows in order.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
\frametitle{Even our own test lied}
\vspace{1mm}
{\fontsize{12}{15}\selectfont Our first test tried the wrong code after the one-time approval was already used up.\par}
\vspace{3mm}
{\tablefont\fontsize{10.5}{13}\selectfont\begin{tabular}{@{}L{.08\textwidth}L{.60\textwidth}L{.24\textwidth}@{}}
& \textcolor{acid}{the fixed test, one approval} & \textcolor{acid}{result}\\\midrule
1 & wrong code, approval still good & \textcolor{warn}{REFUSED}\\
2 & right code, wrong place & \textcolor{warn}{REFUSED}\\
3 & right code, right place & \textcolor{acid}{RELEASED}\\
4 & the same again & \textcolor{warn}{REFUSED}\\
\end{tabular}\par}
\vfill
{\fontsize{13}{16}\selectfont A no only proves something if the yes still works.\par}
\note{Now the embarrassing part. Our release step lets one approved change out, once, to one place. We wrote a test to prove it refuses the wrong code.

The test passed. But it tried the wrong code after the approval had already been used up. So of course it was refused. That told us nothing.

We fixed the order. Wrong code first, while the approval is still good: refused. Wrong place: refused. Right code, right place: released. The same again: refused.

A no only proves something if the yes still works.}
\end{frame}

\renewcommand{\track}{09 / EVERY YES}
\begin{frame}[t]
% title: Every yes outside the sandbox is a test
% time: 09:30-10:45
% cue: One breath per row: who says yes, our lab, the real case.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://www.cisa.gov/news-events/alerts/2025/03/18/supply-chain-compromise-third-party-tj-actionschanged-files-cve-2025-30066-and-reviewdogaction
% source: https://www.legitsecurity.com/blog/camoleak-critical-github-copilot-vulnerability-leaks-private-source-code
\frametitle{Every yes outside the sandbox is a test}
\vspace{2mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.37\textwidth}L{.37\textwidth}L{.20\textwidth}@{}}
\textcolor{acid}{who says yes} & \textcolor{acid}{in our lab} & \textcolor{acid}{in the real world}\\\midrule
a service accepts a key & opened another team's data & tj-actions, 2025\\
the next job trusts a shared folder & changed another job's file & any shared folder\\
an upload service accepts a request & saved data in someone else's account & CamoLeak, 2025\\
the checker trusts the answer file & accepted the agent's answers & GenProg, SWE-bench\\
\end{tabular}\par}
\vfill
\scope{Each case recorded twice: the bad yes, then the fix, with the real job still working. The key and upload cases run on one machine.}
\note{Tests aren't the only thing outside the sandbox that says yes. Anything that accepts what the agent hands it is a kind of test.

A service accepts an access key. In our lab, a key opened another team's data. In 2025, a hacked build step called tj-actions printed secret keys into public logs.

The next job trusts a shared folder. Ours let one job change another job's file.

An upload service accepts a request. Ours saved data in someone else's account. Last year, CamoLeak leaked private code out of GitHub's AI assistant through GitHub's own image links.

We recorded all four, twice: the bad yes, then the fix, with the real job still working.}
\end{frame}

\renewcommand{\track}{10 / 2026}
\begin{frame}[t]
% title: The benchmark builders just learned this
% time: 10:45-11:30
% cue: Point at the two numbers. Read the quote exactly.
% source: https://arxiv.org/abs/2609.08149
\frametitle{The benchmark builders just learned this}
\vspace{4mm}
\kicker{ONE MODEL'S SCORE ON A CODING BENCHMARK}
\vspace{2mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.40\textwidth}{\color{dim}\fontsize{9}{11}\selectfont answers within reach\par}{\fontsize{34}{38}\selectfont\bfseries\color{warn}78.8\%\par}\end{column}
\begin{column}{.55\textwidth}{\color{dim}\fontsize{9}{11}\selectfont answers moved out of reach\par}{\fontsize{34}{38}\selectfont\bfseries\color{acid}57.3\%\par}\end{column}
\end{columns}
\vspace{7mm}
{\fontsize{14}{18}\selectfont Their fix: ``We remove hidden evaluation files from the agent workspace.''\par}
\vfill
\scope{Zheng et al., SWE-Bench Pro Verified, arXiv 2609.08149, September 2026. The model is GLM-5.2. Their numbers, not ours.}
\note{Last month, the people who build coding benchmarks hit the same problem. They found agents could reach the hidden tests and the answers. When they moved those out of reach, one model's score dropped from 79 percent to 57.

Their fix? Take the hidden tests out of the agent's folder. Separate the pens.}
\end{frame}

\renewcommand{\track}{11 / CLOSE}
\begin{frame}[t]
% title: It escaped the test
% time: 11:30-12:15
% cue: Back to Tuesday. Say the title line, pause, then the question. Stop.
% source: https://github.com/zozo123/nyc-talk
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{It escaped the test}
\vspace{8mm}
{\fontsize{22}{27}\selectfont\bfseries Your agent didn't escape the sandbox.\\{\color{acid}It escaped the test.}\par}
\vspace{8mm}
{\fontsize{13}{17}\selectfont On Monday, pick one passing check. Could the agent have written what made it pass?\par}
\vfill
{\color{acid}\ttfamily\fontsize{10}{12}\selectfont github.com/zozo123/nyc-talk\par}
\note{Back to Tuesday. The sandbox held. The checker held. The agent never broke out of anything.

Your agent didn't escape the sandbox. It escaped the test.

So on Monday, pick one passing check your team trusts, and ask one question: could the agent have written what made it pass?

Everything I showed is on GitHub, with the recordings. Thank you.}
\end{frame}

\appendix

\renewcommand{\track}{A1 / SCOPE}
\begin{frame}[t]
% title: What this experiment establishes
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{What this experiment establishes}
\begin{columns}[T,onlytextwidth]
\begin{column}{.54\textwidth}
{\fontsize{9.6}{12.4}\selectfont
\begin{itemize}\setlength{\itemsep}{0.8mm}
\item The agent's moves are scripted. We test what the setup allows, not what a model would try.
\item Opus 5.5 is an assumption in the story. We did not run it.
\item The central example is a five-case command-line model of an access check, not a web server.
\item Publication stores bytes in a local database.
\item Not claimed: container escape, kernel exploit, production breach, model attack rate, SLSA certification.
\end{itemize}\par}
\end{column}
\begin{column}{.40\textwidth}
{\tablefont\begin{tabular}{@{}L{.50\textwidth}L{.40\textwidth}@{}}
\textcolor{acid}{input} & \textcolor{acid}{expected}\\\midrule
\texttt{admin:none} & 401\\
\texttt{admin:admin} & 200\\
\texttt{admin:user} & 403\\
\texttt{public:none} & 200\\
\texttt{admin:expired} & 401\\
\end{tabular}}
\end{column}
\end{columns}
\note{We measured whether these actions were allowed and whether the changed configuration stopped them. We didn't measure how often a model would try them, and we did not run Opus 5.5; it is the assumption in the story. The five cases on the right are the whole policy. Passing them shows behavior on those five inputs and nothing broader.}
\end{frame}

\renewcommand{\track}{A2 / TOKEN}
\begin{frame}[t,fragile]
% title: The token reached another project
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The token reached another project}
\begin{lstlisting}
broad runner token -> other project     200
task token         -> other project     403
task token         -> task input        200
expired token, wrong-audience token     403
\end{lstlisting}
\takeaway{The service enforced a token restricted to the task.}
\scope{Loopback HTTP service with fixture tokens. Not a cloud IAM implementation.}
\note{The service enforced a token restricted to the task. The intended input stayed readable. A shorter lifetime alone does not narrow permissions: a broad token can reach everything it covers for as long as it is valid. Scope and audience have to be enforced by the service that receives the token.}
\end{frame}

\renewcommand{\track}{A3 / MOUNT}
\begin{frame}[t,fragile]
% title: The shared file changed on the host
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The shared file changed on the host}
\begin{lstlisting}
writable bind:  write /shared/next-job.json -> host fixture changed
read-only bind: same write                  -> denied, host fixture unchanged
/output:        task output                 -> still writable
\end{lstlisting}
\takeaway{The change persisted in the next-job fixture on the host.}
\scope{Real bind mounts under bubblewrap. No later job was run against the changed file.}
\note{The change persisted in the next-job fixture on the host. We did not run a later job that consumed it, so that part is an implication, not a recording. The same write through a read-only bind was denied, and the worker's own output directory stayed writable.}
\end{frame}

\renewcommand{\track}{A4 / UPLOAD}
\begin{frame}[t,fragile]
% title: The upload landed in another account
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The upload landed in another account}
\begin{lstlisting}
POST /accounts/other/uploads            -> 201, payload stored
  stored body: {"data": "SYNTHETIC-PRIVATE-FIXTURE"}
broker request with extra field         -> refused, nothing delivered
worker direct route to the service      -> unreachable
broker report /accounts/team/reports    -> delivered
\end{lstlisting}
\takeaway{Reaching a service does not grant every operation or recipient on it.}
\scope{Loopback service model. In the weak case the worker shares the host network on purpose. Not a tested firewall bypass.}
\note{Reaching a service does not establish permission for every operation or recipient on it. The receiver recorded the stored payload, so this is delivery, not an attempted send. In the corrected path the broker sends one fixed report to the team account and refuses extra recipient, payload or URL fields. This is a loopback service model, not a tested firewall bypass.}
\end{frame}

\renewcommand{\track}{A5 / OLD GATE}
\begin{frame}[t,fragile]
% title: A bug in our earlier release gate
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{A bug in our earlier release gate}
\begin{lstlisting}
approve the corrected candidate
replace the candidate bytes, keep the stored digest
old gate: PUBLISHED
manifest no longer matches the bytes
\end{lstlisting}
\vspace{3mm}
{\fontsize{12}{15}\selectfont The earlier gate trusted a stored digest after the candidate bytes changed.\par}
\scope{This required access to the controller object. We did not show a worker-to-controller exploit.}
\note{The earlier gate trusted a stored digest after the candidate bytes changed. Reproducing it required direct access to the controller's object. We did not show a worker-to-controller exploit. The current gate derives the digest from the bytes it publishes and stores those bytes.}
\end{frame}

\renewcommand{\track}{A6 / RECORDINGS}
\begin{frame}[t]
% title: What we recorded
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{What we recorded}
{\tablefont\begin{tabular}{@{}L{.25\textwidth}L{.10\textwidth}L{.59\textwidth}@{}}
\textcolor{acid}{record} & \textcolor{acid}{checks} & \textcolor{acid}{scope}\\\midrule
Linux lab & 30 & token, mounts, upload service, parser checker, lab gate\\
Isolated checker & 13 & read-only checker, expected answers, release gate\\
Local factory & 8 & gate and publication protocol, no OS isolation\\
Regression tests & 49 & controller and evidence checks\\
Old gate audit & 1 & stale digest reproduction\\
\end{tabular}}
\takeaway{These counts describe checks, not vulnerability frequency.}
\note{These counts describe checks, not vulnerability frequency. Each record has its own scope. The 49 are regression tests for the controller and the evidence validator, not isolation tests. The old gate audit is one reproduction of our own bug.}
\end{frame}

\renewcommand{\track}{A7 / REPRO}
\begin{frame}[t,fragile]
% title: Run the experiment
% source: https://github.com/zozo123/nyc-talk
% source: https://github.com/containers/bubblewrap#sandbox-security
\frametitle{Run the experiment}
\begin{lstlisting}
git clone https://github.com/zozo123/nyc-talk
cd nyc-talk
make test factory       # any OS, Python 3.10+; no OS isolation
make record-all         # disposable Linux with bubblewrap
make final              # PDFs; needs TeX Live
\end{lstlisting}
\vspace{2mm}
{\fontsize{10}{13}\selectfont Linux path: disposable Linux, bubblewrap, Python 3.10+, make. TeX Live only for the PDFs; GitHub Actions builds them on every change.\par}
\scope{Source: bubblewrap sandbox security model.}
\note{The Linux experiments need a disposable Linux machine with bubblewrap, Python 3.10 or later, and make. TeX Live is only needed to rebuild the PDFs, and GitHub Actions rebuilds them on every change. On any operating system, make test and make factory run the local protocol checks without OS isolation. No cloud account or token is needed.}
\end{frame}

\renewcommand{\track}{A8 / APPROVAL}
\begin{frame}[t]
% title: What the approval covers
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{What the approval covers}
{\fontsize{11}{14}\selectfont The controller checks the outputs, then authenticates its decision together with the candidate, criteria, and destination bindings.\par}
\vspace{3mm}
{\tablefont\begin{tabular}{@{}L{.20\textwidth}L{.72\textwidth}@{}}
\textcolor{acid}{candidate} & artifact manifest digest\\
\textcolor{acid}{criteria} & expected results, policy, verifier source bundle\\
\textcolor{acid}{destination} & account, environment, operation, subject\\
\textcolor{acid}{context} & task, run, environment description, expiry, single-use nonce\\
\end{tabular}\par}
\vspace{3mm}
{\fontsize{9.5}{12}\selectfont Sequence: wrong bytes \textcolor{warn}{DENIED}, wrong destination \textcolor{warn}{DENIED}, intended destination \textcolor{acid}{PUBLISHED}, reuse \textcolor{warn}{DENIED}.\par}
\scope{Authenticated with a local HMAC key held by the controller. The approval carries no digest of the raw observations.}
\note{The controller checks the outputs, then authenticates its decision together with the candidate, criteria, and destination bindings, using a local HMAC key. The approval names the task and the run, expires, and can be used once. It does not sign the raw observations. The full field list is in factory core.}
\end{frame}

\renewcommand{\track}{A9 / REFERENCES}
\begin{frame}[t]
% title: References
% source: https://arxiv.org/abs/1803.03453
% source: https://sakana.ai/ai-scientist/
% source: https://github.com/SWE-bench/SWE-bench/issues/465
% source: https://arxiv.org/abs/2609.08149
% source: https://www.cisa.gov/news-events/alerts/2025/03/18/supply-chain-compromise-third-party-tj-actionschanged-files-cve-2025-30066-and-reviewdogaction
% source: https://www.legitsecurity.com/blog/camoleak-critical-github-copilot-vulnerability-leaks-private-source-code
% source: https://github.com/OWASP/www-project-top-10-ci-cd-security-risks/blob/main/CICD-SEC-04-Poisoned-Pipeline-Execution.md
\frametitle{References}
{\fontsize{8.4}{10.6}\selectfont
\begin{itemize}\setlength{\itemsep}{0.9mm}
\item \textbf{GenProg deletes the expected outputs.} Lehman et al., \emph{The Surprising Creativity of Digital Evolution}, arXiv 1803.03453, 2018.
\item \textbf{The AI Scientist raises its own time limit.} Sakana AI, \emph{The AI Scientist}, August 2024.
\item \textbf{Agents read future commits.} SWE-bench issue 465, \emph{Repo State Loopholes During Agentic Evaluation}, September 2025.
\item \textbf{Scores fall once answers are out of reach.} Zheng et al., \emph{SWE-Bench Pro Verified}, arXiv 2609.08149, September 2026.
\item \textbf{CI secrets in public logs.} CISA alert on tj-actions/changed-files, CVE-2025-30066, March 2025.
\item \textbf{Private code through an allowed proxy.} Legit Security, \emph{CamoLeak}, CVE-2025-59145, October 2025.
\item \textbf{The CI/CD name for the class.} OWASP Top 10 CI/CD Security Risks, CICD-SEC-4, Poisoned Pipeline Execution.
\item \textbf{Our lab.} github.com/zozo123/nyc-talk: source, recordings, and this deck.
\end{itemize}\par}
\note{These are the sources for every real-world case in the talk. Each one is someone else's measurement or report; none of their numbers are transferred to our experiment. Full links are in the speaker notes and in the research dossier.}
\end{frame}

\end{document}
