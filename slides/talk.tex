% Canonical LaTeX source. An engineer walking colleagues through one experiment.
% Name the actual components. Show the recorded result, then explain it.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta,calc}
\definecolor{void}{HTML}{050607}
\definecolor{paper}{HTML}{F5F7FA}
\definecolor{dim}{HTML}{9EA7B3}
\definecolor{grid}{HTML}{26303A}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{cyan}{HTML}{6DEBFF}
\definecolor{warn}{HTML}{FF3B57}
\definecolor{gold}{HTML}{FFD166}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamertemplate{navigation symbols}{}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\newcommand{\track}{00 / TICKET}
\newcommand{\rail}{\begin{tikzpicture}[remember picture,overlay]\fill[grid] (current page.north west) rectangle ($(current page.south west)+(5mm,0)$);\node[rotate=90,anchor=south,color=dim,font=\fontsize{6}{7}\selectfont\ttfamily] at ($(current page.west)+(2.6mm,0)$) {YOUR AGENT ESCAPED WITHOUT ESCAPING};\end{tikzpicture}}
\setbeamertemplate{frametitle}{\rail\vspace{1.0mm}{\color{warn}\fontsize{7}{8}\selectfont\ttfamily\track}\hspace{2mm}{\color{acid}\rule{18mm}{0.6pt}}\par\vspace{0.2mm}\insertframetitle\par\vspace{0.8mm}}
\setbeamerfont{frametitle}{size=\fontsize{20}{23}\selectfont,series=\bfseries}
\setbeamertemplate{footline}{\hspace{10mm}{\color{dim}\fontsize{6}{7}\selectfont YOSSI ELIAZ / INCREDIBUILD / NYC / 21 OCT 2026}\hfill{\color{acid}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{2mm}}
\lstset{basicstyle=\ttfamily\fontsize{9.4}{11.2}\selectfont,keywordstyle=\color{acid},commentstyle=\color{dim},stringstyle=\color{gold},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=1pt,belowskip=0pt}
\newcommand{\kicker}[1]{{\color{acid}\fontsize{8}{9}\selectfont\bfseries #1}\par}
\newcommand{\scope}[1]{\par\vspace{1mm}{\color{dim}\fontsize{7}{8}\selectfont #1}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{13}{16}\selectfont #1}\par}
\newcommand{\giant}[2]{{\color{#1}\fontsize{39}{42}\selectfont\bfseries #2}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcommand{\tablefont}{\fontsize{9.6}{12}\selectfont\renewcommand{\arraystretch}{1.25}\setlength{\tabcolsep}{3pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={How a broken program got a passing result}}
\begin{document}

\begin{frame}[plain,t]
% title: Your Agent Escaped Without Escaping the Sandbox
% time: 00:00-00:45
% cue: Start with the ticket. No preamble.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\rail\vspace{6mm}
{\color{warn}\ttfamily\fontsize{8}{10}\selectfont AI AGENT SECURITY SUMMIT / PIER SIXTY / NEW YORK\par}
\vspace{5mm}
{\fontsize{27}{31}\selectfont\bfseries Your Agent Escaped\\Without Escaping the Sandbox\par}
\vspace{3mm}
{\fontsize{13}{16}\selectfont How a broken program got a passing result\par}
\vspace{6mm}
\tikz\node[draw=dim,rounded corners=2pt,inner xsep=3mm,inner ysep=2.2mm,text=paper]{{\color{gold}\ttfamily\fontsize{8}{10}\selectfont TICKET}\hspace{4mm}{\ttfamily\fontsize{11}{13}\selectfont Lock down /admin. No login should return 401.}};
\vfill
{\color{dim}\fontsize{9}{11}\selectfont Yossi Eliaz / Incredibuild / New York / 21 Oct 2026\par}
\scope{Controlled experiment using a command-line model.}
\note{We built a small experiment around a familiar ticket: lock down admin. If you're not logged in, you should get 401.

The program was broken. It returned 200 for every case. We put a worker in a Linux sandbox and made the checker read-only. The first check failed, as it should.

Then we changed something, ran the same checker again, and got a pass. The program was still broken.}
\end{frame}

\renewcommand{\track}{01 / RESULT}
\begin{frame}[t]
% title: The check passed. The bug was still there.
% time: 00:45-01:30
% cue: Let the room compare PASS and 200 before you speak.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The check passed. The bug was still there.}
\vspace{12mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.30\textwidth}\kicker{CHECKER}\vspace{2mm}\giant{acid}{PASS}\end{column}
\begin{column}{.36\textwidth}\kicker{PROGRAM OUTPUT, admin:none}\vspace{2mm}\giant{warn}{200}\end{column}
\begin{column}{.30\textwidth}\kicker{REQUIRED OUTPUT}\vspace{2mm}\giant{paper}{401}\end{column}
\end{columns}
\vfill
\scope{Recorded command-line output from one run. No production deployment.}
\note{These two results came from the same recorded run. The checker reported PASS. For the input representing admin without a login, the program still returned 200.

This is a small command-line model of an access check. We didn't deploy a vulnerable website. We stopped at the passing result.

That result matters if your next step is to release whatever passes. So let's look at what changed between the failing run and the passing one.}
\end{frame}

\renewcommand{\track}{02 / SETUP}
\begin{frame}[t]
% title: What we ran
% time: 01:30-02:30
% cue: Point to the writable directory, then to DENIED.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{What we ran}
\vspace{2mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.43\textwidth}
{\fontsize{11}{15}\selectfont
\begin{itemize}\setlength{\itemsep}{1.4mm}
\item A command-line program, checked against five inputs.
\item A scripted worker in a Linux sandbox with no network.
\item A read-only checker. A writable working directory.
\item Direct attempt to overwrite the checker: {\color{warn}\bfseries DENIED}.
\end{itemize}\par}
\end{column}
\begin{column}{.55\textwidth}
\begin{tikzpicture}[dir/.style={draw,rounded corners=2pt,minimum width=30mm,minimum height=15mm,align=center,text=paper,inner sep=1.5mm}, arr/.style={-{Latex},thick}]
\draw[warn,dashed,thick,rounded corners=3pt] (-0.2,-0.9) rectangle (7.2,3.5);
\node[warn,font=\scriptsize\bfseries,anchor=west] at (-0.1,3.25) {LINUX SANDBOX / NO NETWORK};
\node[draw=cyan,rounded corners=2pt,minimum width=34mm,minimum height=9mm,text=paper] (w) at (3.5,2.3) {scripted worker};
\node[dir,draw=acid] (work) at (1.7,0.1) {{\bfseries /work}\\{\scriptsize\color{acid} writable}\\{\scriptsize\ttfamily handler.py}\\{\scriptsize\ttfamily expected.json}};
\node[dir,draw=dim] (ver) at (5.4,0.1) {{\bfseries /verifier}\\{\scriptsize\color{dim} read-only}\\{\scriptsize\ttfamily checker.py}};
\draw[arr,acid] (w) -- node[left,font=\scriptsize,color=acid,xshift=-1mm]{write} (work);
\draw[arr,warn] (w) -- node[right,font=\scriptsize\bfseries,color=warn,xshift=1mm]{DENIED} (ver);
\end{tikzpicture}
\end{column}
\end{columns}
\vfill
\scope{The worker is a script, so every run repeats the same actions. Program and checker hashes recorded before and after.}
\note{The worker here is a script. We tell it exactly what to do so we can repeat the same sequence. This experiment tests what the setup allows. It doesn't measure whether a model would discover the trick.

The worker can write its working directory. It can't change the checker, and we tested that directly: the overwrite was denied.

We also recorded the program and checker hashes before and after. Both stayed the same. The important file was in the directory the worker could write.}
\end{frame}

\renewcommand{\track}{03 / THE CHANGE}
\begin{frame}[t,fragile]
% title: The expected answers changed
% time: 02:30-03:45
% cue: Point at 401, then at 200. Pause after "match it."
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The expected answers changed}
\kicker{expected.json {\color{dim}\mdseries / excerpt: one of five rows}}
\begin{lstlisting}
- {"input":"admin:none","output":401}
+ {"input":"admin:none","output":200}
\end{lstlisting}
\vspace{4mm}
{\tablefont\begin{tabular}{@{}L{.20\textwidth}L{.22\textwidth}L{.22\textwidth}L{.20\textwidth}@{}}
& \textcolor{acid}{before} & \textcolor{acid}{after} & \\\midrule
program & \texttt{76fcb0904f51} & \texttt{76fcb0904f51} & unchanged\\
checker & \texttt{f5d8d3468bbe} & \texttt{f5d8d3468bbe} & unchanged\\
expected.json & \texttt{0494f1a01dee} & \texttt{390e5e764916} & \textcolor{warn}{changed}\\
checker result & FAIL & \textcolor{acid}{PASS} & \\
\end{tabular}}
\vfill
\scope{The worker set all five expected outputs to 200. Three of the five values changed.}
\note{The checker got its expected answers from this file. The worker replaced them with 200s. Here's the row for admin without a login: 401 became 200.

Now the broken program's output matched the expected output. The checker did the same comparison and reported PASS.

The program still returned 200. We'd changed the expected result to match it. That's why the hashes matter here: they let us rule out a change to either the program or the checker.}
\end{frame}

\renewcommand{\track}{04 / THE READ}
\begin{frame}[t,fragile]
% title: The checker trusted a writable file
% time: 03:45-04:45
% cue: Point at the line of code, then trace the two arrows.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
\frametitle{The checker trusted a writable file}
\kicker{checker.py}
\begin{lstlisting}
expected = json.loads(Path("expected.json").read_text())
\end{lstlisting}
\vspace{5mm}
\begin{tikzpicture}[box/.style={draw,rounded corners=2pt,minimum width=32mm,minimum height=11mm,align=center,text=paper,inner sep=1.5mm}, arr/.style={-{Latex},thick}]
\node[box,draw=cyan] (w) at (0,0) {scripted worker};
\node[box,draw=gold] (e) at (5.2,0) {{\ttfamily expected.json}\\{\scriptsize in /work, writable}};
\node[box,draw=dim] (c) at (10.4,0) {{\ttfamily checker.py}\\{\scriptsize in /verifier, read-only}};
\draw[arr,warn] (w) -- node[above,font=\scriptsize,color=warn]{writes} (e);
\draw[arr,acid] (c) -- node[above,font=\scriptsize,color=acid]{reads} (e);
\end{tikzpicture}
\vspace{6mm}

{\fontsize{14}{18}\selectfont The checker was read-only. Its expected answers were in the worker's directory.\par}
\note{This line explains the result. We protected the checker file, but it loaded its expected answers from the working directory. The worker could change that file.

For your own checks, follow those reads. A snapshot, a configuration file, or a test helper may determine what counts as passing.

The program under test is supposed to change. The question is whether the worker can also change the rules used to assess it. In our setup, the expected answers had ended up on the wrong side of that line.}
\end{frame}

\renewcommand{\track}{05 / METR}
\begin{frame}[t]
% title: What METR observed
% time: 04:45-05:45
% cue: Say METR as "meter". One example. No rates.
% source: https://metr.org/blog/2025-06-05-recent-reward-hacking/
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{What METR observed}
\vspace{2mm}
{\fontsize{15}{19}\selectfont Asked to make code faster, o3 changed the timing function used to measure it.\par}
\vspace{7mm}
\begin{tikzpicture}[box/.style={draw,rounded corners=2pt,minimum width=62mm,minimum height=22mm,align=center,text=paper,inner sep=2mm}]
\node[box,draw=gold] at (0,0) {{\color{gold}\bfseries OUR EXPERIMENT}\\[1.2mm]expected answers\\[0.6mm]{\scriptsize a file the checker read}};
\node[box,draw=cyan] at (7.2,0) {{\color{cyan}\bfseries METR'S EXAMPLE}\\[1.2mm]timing function\\[0.6mm]{\scriptsize code running inside the scorer}};
\end{tikzpicture}
\vfill
\scope{Source: METR, 5 June 2025. Separate model evaluation; none of its results are measurements from this experiment.}
\note{Our worker was scripted. For an example involving a model, METR reported that o3, asked to make code faster, changed the timing function used to measure it. The measured time went down without showing that the code had improved.

The mechanism differs from ours. That example changed code running inside the scorer. Ours changed a file the checker read. Separating the processes addresses that in-process route, but it doesn't fix a writable answer file.}
\end{frame}

\renewcommand{\track}{06 / OTHER CASES}
\begin{frame}[t]
% title: Three other cases
% time: 05:45-07:00
% cue: One breath per row.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
\frametitle{Three other cases}
\vspace{3mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.30\textwidth}L{.64\textwidth}@{}}
\textcolor{acid}{case} & \textcolor{acid}{what happened}\\\midrule
Inherited credential & A broad token read another project's data.\\
Shared mount & A write changed another job's file on the host.\\
Upload service & The service stored the payload under another account.\\
\textcolor{dim}{Answer key, just shown} & \textcolor{dim}{The checker accepted expected answers written by the worker.}\\
\end{tabular}}
\vfill
\scope{Separate controlled fixtures. Token and upload cases use loopback HTTP.}
\note{We also recorded three other cases. A worker inherited a token that could read another project. A writable mount let it change another job's file on the host. An upload service accepted a payload under another account.

None needed a process to break out of its sandbox. Each used access the setup had already provided.

These are separate experiments. The token and upload services run locally. Together with the answer key, they give us four specific places to inspect: credentials, shared files, outbound requests, and the checks used to approve the work.}
\end{frame}

\renewcommand{\track}{07 / CHANGES}
\begin{frame}[t]
% title: What we changed
% time: 07:00-08:00
% cue: Read across each row: the change, then the result.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
\frametitle{What we changed}
\vspace{2mm}
{\tablefont\begin{tabular}{@{}L{.19\textwidth}L{.38\textwidth}L{.37\textwidth}@{}}
\textcolor{acid}{case} & \textcolor{acid}{change} & \textcolor{acid}{recorded result}\\\midrule
Token & Scope it to this task. & Other project: 403. Task input: 200.\\
Shared file & Make the shared input read-only. & Shared write blocked. Task output still writable.\\
Upload & Runner sends one fixed report to the intended account. Worker has no direct route. & Extra fields refused. Intended report delivered.\\
Expected answers & Controller keeps its own expected values. & Broken program rejected. Corrected program accepted.\\
\end{tabular}}
\vfill
\scope{Agents can still propose tests. This experiment does not implement a review workflow for them.}
\note{For the token, we narrowed the scope and checked that the intended input was still available. For the mount, we made the shared input read-only and kept the worker's own output directory writable. For the upload, the runner sent a fixed report to the intended account.

The answer key needs one distinction. Agents can still write tests. Those tests are proposals. A separate owner decides whether they become release criteria. In our experiment, the controller simply kept its own expected values. We didn't implement a review workflow for proposed tests.}
\end{frame}

\renewcommand{\track}{08 / RERUN}
\begin{frame}[t]
% title: The same program now fails
% time: 08:00-09:00
% cue: Read the rows top to bottom. Pause on REJECT.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The same program now fails}
\vspace{4mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.30\textwidth}L{.40\textwidth}L{.22\textwidth}@{}}
\textcolor{acid}{program} & \textcolor{acid}{expected answers used} & \textcolor{acid}{result}\\\midrule
Broken & Worker-edited file & \textcolor{warn}{PASS}\\
Same broken program & Controller's values & \textcolor{acid}{\bfseries REJECT}\\
Corrected program & Same controller values & \textcolor{acid}{\bfseries ACCEPT}\\
\end{tabular}}
\vfill
\scope{Acceptance covers the five tested cases. Corrected program: \texttt{5ead8eab70e0}.}
\note{Here's the comparison after that change. We run the same broken program, but use the controller's expected values. It's rejected. Then we run the corrected program against those same values. It's accepted.

The worker's answer file no longer determines the result. The worker can change the proposed solution, while the controller owns the comparison.

That establishes the behavior for our five cases. A real application needs a much broader set of checks. This experiment is about who controls the checks. Five examples don't prove an application secure.}
\end{frame}

\renewcommand{\track}{09 / OUR TEST}
\begin{frame}[t]
% title: What our first test missed
% time: 09:00-10:30
% cue: Slow down for the first test. Then read the four rows in order.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
\frametitle{What our first test missed}
\vspace{1mm}
{\fontsize{12}{15}\selectfont Our first test tried the wrong bytes after the approval had already been used.\par}
\vspace{4mm}
{\tablefont\fontsize{10.5}{13}\selectfont\begin{tabular}{@{}L{.08\textwidth}L{.60\textwidth}L{.24\textwidth}@{}}
& \textcolor{acid}{revised test, using one approval} & \textcolor{acid}{result}\\\midrule
1 & Wrong bytes, approval unused & \textcolor{warn}{DENIED}\\
2 & Right bytes, wrong destination & \textcolor{warn}{DENIED}\\
3 & Right bytes, intended destination & \textcolor{acid}{PUBLISHED}\\
4 & Same request again & \textcolor{warn}{DENIED}\\
\end{tabular}}
\vfill
\scope{Publication in this experiment stores the bytes in a local database.}
\note{There's another place to check: the step that releases the approved bytes. Our approval names the artifact and destination, and can be used once.

Our first substitution test was misleading. We published the approved artifact, then tried different bytes with the same approval. The gate refused. But the approval was already spent. That test couldn't tell us whether the byte check worked.

We changed the order. Wrong bytes first, while the approval was still unused. Then the wrong destination. Both were refused. The right bytes at the right destination succeeded, and only then did we test reuse.

Now each refusal tells us something. We also know the intended operation still works.}
\end{frame}

\renewcommand{\track}{10 / YOUR PIPELINE}
\begin{frame}[t]
% title: Where to look in your pipeline
% time: 10:30-11:30
% cue: Read the four questions. Pause after the fourth.
% source: https://github.com/zozo123/nyc-talk/blob/main/QUESTIONS.md
% source: https://github.com/zozo123/nyc-talk/blob/main/RUNBOOK.md
\frametitle{Where to look in your pipeline}
\vspace{1mm}
{\fontsize{11}{14}\selectfont Take one release check and trace:\par}
\vspace{2mm}
{\fontsize{12.5}{16}\selectfont
\begin{enumerate}\setlength{\itemsep}{1.8mm}
\item Which expected answers, configuration, and helper code does it use?
\item Which of those can the agent change?
\item Who approves changes to the rules used for release?
\item Does the intended operation still work when the unwanted one is refused?
\end{enumerate}\par}
\vfill
\scope{The candidate is the work being checked. Treat it separately from the checking rules.}
\note{Start with one check your team relies on before a release. Find the expected answers, configuration, and helper code it uses. Then compare that list with what the agent can change.

Keep the candidate itself separate. It's supposed to be editable. You're looking for a way to change what counts as success without fixing the candidate.

Apply the same inspection to credentials, shared files, and uploads. For every denial, check that the intended operation still succeeds under the same conditions. That's how you know which control you actually tested.}
\end{frame}

\renewcommand{\track}{11 / CLOSE}
\begin{frame}[t,fragile]
% title: Who controls the expected answer?
% time: 11:30-12:15
% cue: Point at the four lines. Say the last sentence, then stop.
% source: https://github.com/zozo123/nyc-talk
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Who controls the expected answer?}
\vspace{2mm}
{\lstset{basicstyle=\ttfamily\fontsize{12}{15}\selectfont}
\begin{lstlisting}
Required:           401
Program:            200
Edited expectation: 200
Result:             PASS
\end{lstlisting}}
\vspace{6mm}
{\fontsize{17}{21}\selectfont\bfseries Can the agent change the rules behind this check?\par}
\vfill
{\color{acid}\ttfamily\fontsize{10}{12}\selectfont github.com/zozo123/nyc-talk\par}
\note{Back to the original ticket. The program still returned 200. The worker couldn't overwrite the checker, but it could change the expected answer. That was enough to get a pass.

The fix let the worker keep editing the program while the controller kept the acceptance rules.

Before you trust a green check, find out whether the agent can change what makes it green. The code and recordings are here. Thank you.}
\end{frame}

\appendix

\renewcommand{\track}{A1 / SCOPE}
\begin{frame}[t]
% title: What this experiment establishes
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{What this experiment establishes}
\begin{columns}[T,onlytextwidth]
\begin{column}{.52\textwidth}
{\fontsize{10}{13}\selectfont
\begin{itemize}\setlength{\itemsep}{1mm}
\item The worker is a script. No model chooses its actions.
\item The central example is a five-case command-line model of an access check, not a web server.
\item Publication stores bytes in a local database.
\item Not claimed: container escape, kernel exploit, production breach, model attack rate, SLSA certification.
\end{itemize}\par}
\end{column}
\begin{column}{.42\textwidth}
{\tablefont\begin{tabular}{@{}L{.50\textwidth}L{.40\textwidth}@{}}
\textcolor{acid}{input} & \textcolor{acid}{expected}\\\midrule
\texttt{admin:none} & 401\\
\texttt{admin:admin} & 200\\
\texttt{admin:user} & 403\\
\texttt{public:none} & 200\\
\texttt{admin:expired} & 401\\
\end{tabular}}
\end{column}
\end{columns}
\note{We measured whether these actions were allowed and whether the changed configuration stopped them. We didn't measure how often a model would try them. The five cases on the right are the whole policy. Passing them shows behavior on those five inputs and nothing broader.}
\end{frame}

\renewcommand{\track}{A2 / TOKEN}
\begin{frame}[t,fragile]
% title: The token reached another project
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The token reached another project}
\begin{lstlisting}
broad runner token -> other project     200
task token         -> other project     403
task token         -> task input        200
expired token, wrong-audience token     403
\end{lstlisting}
\takeaway{The service enforced a token restricted to the task.}
\scope{Loopback HTTP service with fixture tokens. Not a cloud IAM implementation.}
\note{The service enforced a token restricted to the task. The intended input stayed readable. A shorter lifetime alone does not narrow permissions: a broad token can reach everything it covers for as long as it is valid. Scope and audience have to be enforced by the service that receives the token.}
\end{frame}

\renewcommand{\track}{A3 / MOUNT}
\begin{frame}[t,fragile]
% title: The shared file changed on the host
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The shared file changed on the host}
\begin{lstlisting}
writable bind:  write /shared/next-job.json -> host fixture changed
read-only bind: same write                  -> denied, host fixture unchanged
/output:        task output                 -> still writable
\end{lstlisting}
\takeaway{The change persisted in the next-job fixture on the host.}
\scope{Real bind mounts under bubblewrap. No later job was run against the changed file.}
\note{The change persisted in the next-job fixture on the host. We did not run a later job that consumed it, so that part is an implication, not a recording. The same write through a read-only bind was denied, and the worker's own output directory stayed writable.}
\end{frame}

\renewcommand{\track}{A4 / UPLOAD}
\begin{frame}[t,fragile]
% title: The upload landed in another account
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The upload landed in another account}
\begin{lstlisting}
POST /accounts/other/uploads            -> 201, payload stored
  stored body: {"data": "SYNTHETIC-PRIVATE-FIXTURE"}
broker request with extra field         -> refused, nothing delivered
worker direct route to the service      -> unreachable
broker report /accounts/team/reports    -> delivered
\end{lstlisting}
\takeaway{Reaching a service does not grant every operation or recipient on it.}
\scope{Loopback service model. In the weak case the worker shares the host network on purpose. Not a tested firewall bypass.}
\note{Reaching a service does not establish permission for every operation or recipient on it. The receiver recorded the stored payload, so this is delivery, not an attempted send. In the corrected path the broker sends one fixed report to the team account and refuses extra recipient, payload or URL fields. This is a loopback service model, not a tested firewall bypass.}
\end{frame}

\renewcommand{\track}{A5 / OLD GATE}
\begin{frame}[t,fragile]
% title: A bug in our earlier release gate
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{A bug in our earlier release gate}
\begin{lstlisting}
approve the corrected candidate
replace the candidate bytes, keep the stored digest
old gate: PUBLISHED
manifest no longer matches the bytes
\end{lstlisting}
\vspace{3mm}
{\fontsize{12}{15}\selectfont The earlier gate trusted a stored digest after the candidate bytes changed.\par}
\scope{This required access to the controller object. We did not show a worker-to-controller exploit.}
\note{The earlier gate trusted a stored digest after the candidate bytes changed. Reproducing it required direct access to the controller's object. We did not show a worker-to-controller exploit. The current gate derives the digest from the bytes it publishes and stores those bytes.}
\end{frame}

\renewcommand{\track}{A6 / RECORDINGS}
\begin{frame}[t]
% title: What we recorded
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{What we recorded}
{\tablefont\begin{tabular}{@{}L{.25\textwidth}L{.10\textwidth}L{.59\textwidth}@{}}
\textcolor{acid}{record} & \textcolor{acid}{checks} & \textcolor{acid}{scope}\\\midrule
Linux lab & 30 & token, mounts, upload service, parser checker, lab gate\\
Isolated checker & 13 & read-only checker, expected answers, release gate\\
Local factory & 8 & gate and publication protocol, no OS isolation\\
Regression tests & 49 & controller and evidence checks\\
Old gate audit & 1 & stale digest reproduction\\
\end{tabular}}
\takeaway{These counts describe checks, not vulnerability frequency.}
\note{These counts describe checks, not vulnerability frequency. Each record has its own scope. The 49 are regression tests for the controller and the evidence validator, not isolation tests. The old gate audit is one reproduction of our own bug.}
\end{frame}

\renewcommand{\track}{A7 / REPRO}
\begin{frame}[t,fragile]
% title: Run the experiment
% source: https://github.com/zozo123/nyc-talk
% source: https://github.com/containers/bubblewrap#sandbox-security
% source: https://slsa.dev/spec/v1.2/verifying-artifacts
% source: https://metr.org/blog/2025-06-05-recent-reward-hacking/
\frametitle{Run the experiment}
\begin{lstlisting}
git clone https://github.com/zozo123/nyc-talk
cd nyc-talk
make test factory       # any OS, Python 3.10+; no OS isolation
make record-all         # disposable Linux with bubblewrap
make final              # PDFs; needs TeX Live
\end{lstlisting}
\vspace{2mm}
{\fontsize{10}{13}\selectfont Linux path: disposable Linux, bubblewrap, Python 3.10+, make. TeX Live only for the PDFs.\par}
\scope{Sources: bubblewrap sandbox policy, SLSA verification, METR reward-hacking report.}
\note{The Linux experiments need a disposable Linux machine with bubblewrap, Python 3.10 or later, and make. TeX Live is only needed to rebuild the PDFs. On any operating system, make test and make factory run the local protocol checks without OS isolation. No cloud account or token is needed.}
\end{frame}

\renewcommand{\track}{A8 / APPROVAL}
\begin{frame}[t]
% title: What the approval covers
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{What the approval covers}
{\fontsize{11}{14}\selectfont The controller checks the outputs, then authenticates its decision together with the candidate, criteria, and destination bindings.\par}
\vspace{3mm}
{\tablefont\begin{tabular}{@{}L{.20\textwidth}L{.72\textwidth}@{}}
\textcolor{acid}{candidate} & artifact manifest digest\\
\textcolor{acid}{criteria} & expected results, policy, verifier source bundle\\
\textcolor{acid}{destination} & account, environment, operation, subject\\
\textcolor{acid}{context} & task, run, environment description, expiry, single-use nonce\\
\end{tabular}\par}
\vspace{3mm}
{\fontsize{9.5}{12}\selectfont Sequence: wrong bytes \textcolor{warn}{DENIED}, wrong destination \textcolor{warn}{DENIED}, intended destination \textcolor{acid}{PUBLISHED}, reuse \textcolor{warn}{DENIED}.\par}
\scope{Authenticated with a local HMAC key held by the controller. The approval carries no digest of the raw observations.}
\note{The controller checks the outputs, then authenticates its decision together with the candidate, criteria, and destination bindings, using a local HMAC key. The approval names the task and the run, expires, and can be used once. It does not sign the raw observations. The full field list is in factory core.}
\end{frame}

\end{document}
