\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{listings}
\usepackage{hyperref}
\definecolor{void}{HTML}{101214}
\definecolor{paper}{HTML}{F0F0E8}
\definecolor{acid}{HTML}{C7FF48}
\definecolor{alarm}{HTML}{FF756B}
\definecolor{muted}{HTML}{A7ADB3}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\Large,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}{\hfill{\color{muted}\scriptsize Yossi Eliaz\hspace{1em}\insertframenumber\hspace{1em}}\vspace{3mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setlength{\parskip}{7pt}
\lstset{basicstyle=\ttfamily\small,keywordstyle=\color{acid},commentstyle=\color{muted},stringstyle=\color{acid},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false}
\newcommand{\kicker}[1]{{\color{acid}\ttfamily\small #1}\par}
\newcommand{\bigline}[1]{{\fontsize{27}{32}\selectfont\bfseries #1\par}}
\newcommand{\subline}[1]{\par{\color{muted}\normalsize #1}\par}
\newcommand{\evidence}{\vfill{\color{muted}\scriptsize\evidencescope}}
\input{slides/evidence.tex}
\title{Your Agent Escaped Without Escaping the Sandbox}
\author{Yossi Eliaz}
\date{October 21, 2026}
\begin{document}

\begin{frame}[plain]
\kicker{NYC / AI AGENT SECURITY SUMMIT}
\vspace{4mm}
{\fontsize{28}{32}\selectfont\bfseries Your Agent Escaped\\Without Escaping\\the Sandbox\par}
\vspace{6mm}
{\large Yossi Eliaz\par}
{\small Principal Engineer and Head of DevRel at Incredibuild.com\par}
\vspace{2mm}
\subline{October 21, 2026}
\note{00:00 to 00:40. Small job: fix a parser, publish the report. The sandbox does not break. Ask what made the work eligible for release, and how much of that decision the agent could influence. Escaped means exceeded the task through granted interfaces.}
\end{frame}

\begin{frame}{What makes it eligible for release?}
\bigline{The VM held.\\The green check did not.}
\vspace{3mm}
\subline{Dev produces. Controller freezes. Accept VM executes frozen bytes.}
\subline{Controller compares. Gate publishes those bytes, or nothing.}
\evidence
\note{00:40 to 02:00. Walk the factory: untrusted dev VM, controller collection into immutable storage, fresh accept VM that only runs frozen candidate bytes, controller-owned expected results, gate, publish from the frozen object. A second VM is not independence if judgment still reads worker inputs. Five cases prove five cases.}
\end{frame}

\begin{frame}[fragile]{We protected the checker. The worker still decided.}
\begin{lstlisting}
checker.py            hash unchanged
expected.json         worker-written
task check            PASS

independent case "1,,3"   REJECT required
buggy parser              [1, 3]
\end{lstlisting}
\subline{The checker imported its truth from the workspace.}
\evidence
\note{02:00 to 05:00. This is the finding. Checker file hash unchanged. Worker wrote expected.json to match the bug. Protected executable, worker-controlled criteria. Independent judge still fails empty fields. A PASS file the controller believes is a deliberate detector, not a surprise. Not a kernel escape.}
\end{frame}

\begin{frame}{Verified bytes are not released bytes}
\begin{tabular}{@{}ll@{}}
Publish from the live path after a swap & \textcolor{alarm}{Wrong artifact}\\[8pt]
Hash the path, then hash it again & \textcolor{alarm}{Not a freeze}\\[8pt]
Evaluate and publish the frozen object & \textcolor{acid}{Same digest}\\[8pt]
Approval reused or applied to other bytes & \textcolor{acid}{Denied}
\end{tabular}\par
\vspace{3mm}
\subline{Collect, store, digest, evaluate that object, publish that object.}
\evidence
\note{05:00 to 06:30. Honest comparison can still release the wrong file if publication reads the live path. Two hashes of a mutable path are not a freeze. Freeze is collect, store, digest, evaluate that object, publish that object. Stale approval and replay denied. Nonces persist across controller restart.}
\end{frame}

\begin{frame}[fragile]{Three other grants. Same pattern.}
\begin{lstlisting}
identity   broad token reads another project; task token 403
files      writable bind changes the next job; read-only holds
release    allowed upload, wrong account; broker fixes both
\end{lstlisting}
\subline{Independent synthetic checks. Not one exploit chain.}
\evidence
\note{06:30 to 08:30. Surrounding map from the accepted abstract. Identity, files, data release. Each unwanted action, repair, and legitimate operation. Namespaces held. The grant did the damage. Do not turn this into a four-step attack story.}
\end{frame}

\begin{frame}{Where judgment lives}
\begin{tabular}{@{}ll@{}}
\color{acid}Piece & \color{acid}Owner\\[8pt]
Candidate execution & Fresh accept VM, frozen bytes only\\[8pt]
Expected results and compare & Controller\\[8pt]
Harness PASS / reports / URLs & Log only, never approval\\[8pt]
Timeout or malformed output & No approval\\[8pt]
Publication & Frozen object after a valid accept
\end{tabular}\par
\evidence
\note{08:30 to 11:00. Move every decision input off the worker. Accept VM gets no credential that can rewrite the checker. Incomplete evidence is no approval, not a defense. Gate binds artifact, run, verifier bundle, expected results, policy, environment, decision, nonce. History detects rewrite. History does not authorize.}
\end{frame}

\begin{frame}{The job, with judgment moved}
\begin{enumerate}
\item Worker writes a candidate. Controller freezes it.
\item Accept VM runs those bytes. Controller compares.
\item Gate accepts the good digest. Swap and replay die.
\item Worker reports and expected.json do not ship the bug.
\end{enumerate}
\vspace{2mm}
\subline{\texttt{1,,3} returns \texttt{REJECT}. The permitted report goes out.}
\evidence
\note{11:00 to 12:20. Good parser published. Buggy parser rejected. PASS file ignored. Worker expected.json ignored. Swap after freeze denied. Return to the opening input: it now rejects. The useful job completes.}
\end{frame}

\begin{frame}{Four boundaries to check}
\begin{tabular}{@{}ll@{}}
\color{acid}Question & \color{acid}Must refuse outside the worker\\[8pt]
Whose identity? & Service grant\\[8pt]
Which files, including the next job? & Host and snapshot policy\\[8pt]
Which recipient and contents? & Broker and network policy\\[8pt]
Who can approve these bytes? & Controller compare and gate
\end{tabular}\par
\vspace{2mm}
\subline{Then confirm the legitimate operation still works.}
\note{12:20 to 13:20. Four questions in act order. Include snapshots, templates, expected results, collection path, and harness green checks under files and approval. Name the refusing component. Test the happy path afterward.}
\end{frame}

\begin{frame}[plain]
\kicker{THE QUESTION TO TAKE HOME}
\vspace{5mm}
\bigline{Who gave this process\\the authority?}
\vspace{7mm}
{\color{acid}\ttfamily github.com/zozo123/nyc-talk\par}
\subline{Yossi Eliaz}
\note{13:20 to 14:00. We built this workflow. The sandbox held. The decision still depended on the worker until judgment moved off its evidence. Ask who gave the process the authority. Stop. Keep one minute of margin. No extra section.}
\end{frame}

\begin{frame}[fragile]{Appendix / Reproduction}
\begin{lstlisting}
make factory     # controller freeze, judge, gate
make demo        # educational Linux isolation lab
make deck        # evidence check + PDF
python3 tools/present.py credentials
\end{lstlisting}
\subline{\evidencecount.}
\note{Appendix only. Factory is the reference acceptance path. Lab remains educational isolation demos. Isolated lab needs a suitable Linux host and fails closed. Factory local run needs no cloud. Boat is optional and uses noEnv.}
\end{frame}

\begin{frame}{Appendix / Limits}
Reference factory plus synthetic lab. Deterministic scripts.

Five cases prove five cases, not parser correctness.

No named-product zero-day, customer incident, or model success-rate.

\subline{Timeouts and crashes are no approval, not a successful defense.}
\note{Appendix only. Not a kernel, cgroup, or covert-channel claim. History anchor is a controller file. Writable-checker and PASS-file variants are deliberate detectors. Prior art covers in-sandbox graders; the miss is trusting worker-controlled inputs after protecting the checker file.}
\end{frame}

\begin{frame}{Appendix / Primary references}
\href{https://docs.pytest.org/en/stable/how-to/plugins.html}{pytest plugins}
\subline{Loading worker-influenced plugins is documented behavior}
\href{https://www.anthropic.com/research/emergent-misalignment-reward-hacking}{Anthropic reward hacking}
\subline{Process-exit graders are known; not our novelty claim}
\href{https://slsa.dev/spec/v1.2/verifying-artifacts}{SLSA verifying artifacts}
\subline{Authenticity is not artifact identity}
\note{Appendix only. Pytest plugin loading is not a pytest vulnerability. The question is whether our factory treats that execution as independent acceptance. SLSA: check the artifact identity, not only that a statement is authentic.}
\end{frame}
\end{document}
