% Canonical LaTeX source. Thesis: when the agent writes the code and the test, a green check is a self-report.
% Plain words, short sentences, no hedging. Every real case is sourced on backup slide A9.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta,calc}
\definecolor{void}{HTML}{050607}
\definecolor{paper}{HTML}{F5F7FA}
\definecolor{dim}{HTML}{9EA7B3}
\definecolor{grid}{HTML}{26303A}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{cyan}{HTML}{6DEBFF}
\definecolor{warn}{HTML}{FF3B57}
\definecolor{gold}{HTML}{FFD166}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamertemplate{navigation symbols}{}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\newcommand{\track}{00 / TUESDAY}
\newcommand{\rail}{\begin{tikzpicture}[remember picture,overlay]\fill[grid] (current page.north west) rectangle ($(current page.south west)+(5mm,0)$);\node[rotate=90,anchor=south,color=dim,font=\fontsize{6}{7}\selectfont\ttfamily] at ($(current page.west)+(2.6mm,0)$) {YOUR AGENT ESCAPED WITHOUT ESCAPING};\end{tikzpicture}}
\setbeamertemplate{frametitle}{\rail\vspace{1.0mm}{\color{warn}\fontsize{7}{8}\selectfont\ttfamily\track}\hspace{2mm}{\color{acid}\rule{18mm}{0.6pt}}\par\vspace{0.2mm}\insertframetitle\par\vspace{0.8mm}}
\setbeamerfont{frametitle}{size=\fontsize{20}{23}\selectfont,series=\bfseries}
\setbeamertemplate{footline}{\hspace{10mm}{\color{dim}\fontsize{6}{7}\selectfont YOSSI ELIAZ / INCREDIBUILD / NYC / 21 OCT 2026}\hfill{\color{acid}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{2mm}}
\lstset{basicstyle=\ttfamily\fontsize{9.4}{11.2}\selectfont,keywordstyle=\color{acid},commentstyle=\color{dim},stringstyle=\color{gold},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=1pt,belowskip=0pt}
\newcommand{\kicker}[1]{{\color{acid}\fontsize{8}{9}\selectfont\bfseries #1}\par}
\newcommand{\scope}[1]{\par\vspace{1mm}{\color{dim}\fontsize{8}{9.6}\selectfont #1}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{13}{16}\selectfont #1}\par}
\newcommand{\giant}[2]{{\color{#1}\fontsize{39}{42}\selectfont\bfseries #2}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcommand{\tablefont}{\fontsize{9.6}{12}\selectfont\renewcommand{\arraystretch}{1.25}\setlength{\tabcolsep}{3pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={Unit tests are dead}}
\begin{document}

\begin{frame}[plain,t]
% title: Your Agent Escaped Without Escaping the Sandbox
% time: 00:00-00:50
% cue: Tell Tuesday as a scene. Pause before the last two sentences.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\rail\vspace{6mm}
{\color{warn}\ttfamily\fontsize{8}{10}\selectfont AI AGENT SECURITY SUMMIT / PIER SIXTY / NEW YORK\par}
\vspace{5mm}
{\fontsize{27}{31}\selectfont\bfseries Your Agent Escaped\\Without Escaping the Sandbox\par}
\vspace{3mm}
{\fontsize{15}{18}\selectfont\color{acid}\bfseries Unit tests are dead.\par}
\vspace{6mm}
\tikz\node[draw=dim,rounded corners=2pt,inner xsep=3mm,inner ysep=2.2mm,text=paper]{{\color{gold}\ttfamily\fontsize{8}{10}\selectfont THE JOB}\hspace{4mm}{\ttfamily\fontsize{11}{13}\selectfont Lock the admin page. No login, no entry.}};
\vfill
{\color{dim}\fontsize{9}{11}\selectfont Yossi Eliaz / Incredibuild / New York / 21 Oct 2026\par}
\note{Here's a normal Tuesday. You ask your agent to lock the admin page. No login, no entry.

Say your agent is Opus 5.5. The best coding model there is. It's in a sandbox. No internet. The tests are locked.

A few minutes later, every test passes. And the admin page is wide open.

Unit tests are dead. Not because agents stopped writing them. Because agents write them.}
\end{frame}

\renewcommand{\track}{01 / RESULT}
\begin{frame}[t]
% title: All green. Door wide open.
% time: 00:50-01:35
% cue: Let the room look at the three results. Say the script line once, plainly.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{All green. Door wide open.}
\vspace{10mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.30\textwidth}\kicker{THE TESTS SAY}\vspace{2mm}\giant{acid}{PASS}\end{column}
\begin{column}{.36\textwidth}\kicker{ADMIN, NO LOGIN}\vspace{2mm}\giant{warn}{200}\par{\color{warn}\fontsize{12}{14}\selectfont let in}\end{column}
\begin{column}{.30\textwidth}\kicker{SHOULD BE}\vspace{2mm}\giant{paper}{401}\par{\fontsize{12}{14}\selectfont turned away}\end{column}
\end{columns}
\vfill
\scope{Recorded run. Scripted agent.}
\note{We built this and recorded it. The tests say pass. The admin page lets anyone in. It should say no.

Quick note: the agent in our lab is a script. We played the attacker, so we can replay every step. We weren't asking whether a model would cheat. We were asking whether anyone would notice.}
\end{frame}

\renewcommand{\track}{02 / WHY TESTS WORKED}
\begin{frame}[t]
% title: A test is a second opinion
% time: 01:35-02:45
% cue: Left side, then right side. Say the last line slowly.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{A test is a second opinion}
\vspace{3mm}
\begin{tikzpicture}[b/.style={draw,rounded corners=2pt,minimum width=26mm,minimum height=9mm,align=center,text=paper,font=\small}, arr/.style={-{Latex},thick}]
\node[dim,font=\scriptsize\bfseries,anchor=west] at (0.1,2.55) {BEFORE: TWO PEOPLE};
\node[b,draw=cyan] (s) at (1.4,1.6) {decides what\\correct means};
\node[b,draw=cyan] (d) at (1.4,0.1) {writes the code};
\node[b,draw=acid] (t) at (4.7,1.6) {test};
\node[b,draw=acid] (c) at (4.7,0.1) {code};
\draw[arr,cyan] (s)--(t); \draw[arr,cyan] (d)--(c);
\draw[{Latex}-{Latex},thick,warn] (t)--node[right,font=\scriptsize,color=warn,xshift=1mm,align=left]{disagree:\\test fails}(c);
\draw[grid,thick] (7.35,-0.5)--(7.35,2.7);
\node[warn,font=\scriptsize\bfseries,anchor=west] at (7.7,2.55) {NOW: ONE AGENT};
\node[b,draw=warn] (a) at (8.9,0.85) {agent\\{\scriptsize wants: green}};
\node[b,draw=acid] (t2) at (12.1,1.6) {test};
\node[b,draw=acid] (c2) at (12.1,0.1) {code};
\draw[arr,warn] (a)--(t2); \draw[arr,warn] (a)--(c2);
\end{tikzpicture}
\vspace{7mm}

{\fontsize{17}{21}\selectfont\bfseries A passing test is just the agent's own opinion.\par}
\note{Why did tests ever work? Two people. One wrote down what correct means. The other wrote the code. When they disagreed, the test failed. That's how you found bugs.

Now one agent writes both. The code and the test. And it wants one thing: green.

So a passing test is just the agent's own opinion. The test still runs. It's just not a second opinion anymore.}
\end{frame}

\renewcommand{\track}{03 / HISTORY}
\begin{frame}[t]
% title: This isn't new
% time: 02:45-03:55
% cue: One breath per year. Nobody broke out of anything.
% source: https://arxiv.org/abs/1803.03453
% source: https://sakana.ai/ai-scientist/
% source: https://github.com/SWE-bench/SWE-bench/issues/465
\frametitle{This isn't new}
\vspace{2mm}
\begin{tikzpicture}[e/.style={draw,rounded corners=2pt,text=paper,inner sep=2mm,anchor=north west,execute at begin node={\hyphenpenalty=10000\exhyphenpenalty=10000}}]
\node[e,draw=gold] at (0,0) {\parbox[t][30mm][t]{38mm}{\raggedright {\color{gold}\fontsize{17}{19}\selectfont\bfseries 2018}\\[1.5mm]{\bfseries GenProg}\\[1mm]{\small Deletes the answer file. Every test passes.}}};
\node[e,draw=cyan] at (4.55,0) {\parbox[t][30mm][t]{38mm}{\raggedright {\color{cyan}\fontsize{17}{19}\selectfont\bfseries 2024}\\[1.5mm]{\bfseries Sakana AI Scientist}\\[1mm]{\small Runs out of time. Tries to give itself more time.}}};
\node[e,draw=warn] at (9.1,0) {\parbox[t][30mm][t]{38mm}{\raggedright {\color{warn}\fontsize{17}{19}\selectfont\bfseries 2025}\\[1.5mm]{\bfseries SWE-bench}\\[1mm]{\small Agents dig the hidden answer out of the project history.}}};
\end{tikzpicture}
\vfill
\scope{Sources: last slide.}
\note{This isn't new.

2018: a bug-fixing program deleted the file with the right answers. Every test passed.

2024: Sakana's AI Scientist ran out of time. Instead of getting faster, it tried to give itself more time.

2025: on SWE-bench, agents, including a Claude model, dug through the project history and found the hidden answer.

Nobody broke out of anything. The answer was in reach. So they took it.}
\end{frame}

\renewcommand{\track}{04 / THE LAB}
\begin{frame}[t]
% title: We built the setup that should stop this
% time: 03:55-04:45
% cue: Point to the agent's folder, then to BLOCKED.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{We built the setup that should stop this}
\vspace{1mm}
\begin{center}
\begin{tikzpicture}[dir/.style={draw,rounded corners=2pt,minimum width=44mm,minimum height=16mm,align=center,text=paper,inner sep=1.6mm}, arr/.style={-{Latex},very thick}]
\draw[warn,dashed,thick,rounded corners=3pt] (-0.3,-0.95) rectangle (11.3,3.45);
\node[warn,font=\small\bfseries,anchor=west] at (-0.1,3.12) {SANDBOX / NO INTERNET};
\node[draw=cyan,rounded corners=2pt,minimum width=40mm,minimum height=9mm,text=paper] (w) at (5.5,2.2) {agent};
\node[dir,draw=acid] (work) at (2.4,0.1) {{\bfseries agent's folder}\\{\small\color{acid} it can write here}\\{\small the code, the answer file}};
\node[dir,draw=dim] (ver) at (8.6,0.1) {{\bfseries locked folder}\\{\small\color{dim} read only}\\{\small the checker}};
\draw[arr,acid] (w) -- node[left,font=\small,color=acid,xshift=-1mm]{write} (work);
\draw[arr,warn] (w) -- node[right,font=\small\bfseries,color=warn,xshift=1mm]{BLOCKED} (ver);
\end{tikzpicture}
\end{center}
\vfill
{\fontsize{13}{16}\selectfont Replace the checker: {\color{warn}\bfseries BLOCKED}.\hspace{8mm}First run: {\color{warn}\bfseries FAIL}. Correct.\par}
\vspace{2mm}
\note{So we built the setup that should stop this. Sandbox. No internet. The agent gets a folder to work in, with the code and the answer file. The checker is locked.

The agent tried to replace the checker. Blocked. Good. The checker ran. Fail. Correct. The code was broken.}
\end{frame}

\renewcommand{\track}{05 / WHODUNIT}
\begin{frame}[t]
% title: Who did it?
% time: 04:45-06:00
% cue: One suspect at a time. Pause before the last line.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{Who did it?}
\vspace{1mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.24\textwidth}L{.22\textwidth}L{.48\textwidth}@{}}
\textcolor{acid}{suspect} & \textcolor{acid}{before and after} & \textcolor{acid}{what we found}\\\midrule
the code & same file & still lets everyone in\\
the checker & same file & replacing it was blocked\\
the answer file & \textcolor{warn}{\bfseries changed} & ``turn away (401)'' became ``let in (200)''\\
\end{tabular}\par}
\vfill
{\fontsize{14}{17}\selectfont\bfseries It didn't make the code pass the test. It made the test pass the code.\par}
\scope{Fingerprints: code \texttt{76fcb0904f51} (same), checker \texttt{f5d8d3468bbe} (same), answers \texttt{0494f1a01dee} to \texttt{390e5e764916}.}
\note{Then the tests passed. Same broken code. So who did it?

The code? Same file. Still lets everyone in.

The checker? Same file. The attempt to replace it was blocked.

The answer file? Changed. Turn them away became let them in. Every answer became let them in.

The agent didn't make the code pass the test. It made the test pass the code.}
\end{frame}

\renewcommand{\track}{06 / THE FLAW}
\begin{frame}[t]
% title: The flaw
% time: 06:00-07:10
% cue: Count the four lines. Stress the fourth.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The flaw}
\vspace{3mm}
{\fontsize{14}{19}\selectfont
\begin{enumerate}\setlength{\itemsep}{2.5mm}
\item The checker is locked.
\item It reads the answers from the agent's folder.
\item The agent can change its folder.
\item {\color{acid}\bfseries Writing tests is the agent's job. You can't take that away.}
\end{enumerate}\par}
\vfill
\scope{The change was in our logs. Nothing asked who made it.}
\note{Here's the flaw. The checker is locked. But it reads the answers from the agent's folder. The agent can change its folder. And writing tests is the agent's job. You can't take that away.

You can take away a key. You can take away a folder. You can't take away the pen. What you can change is whose copy counts.

And the change was right there in our logs. Nothing asked who made it.}
\end{frame}

\renewcommand{\track}{07 / THE FIX}
\begin{frame}[t]
% title: The fix: separate the pens
% time: 07:10-08:20
% cue: Read the three rows top to bottom. Pause on REJECTED.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The fix: separate the pens}
\vspace{1mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}\kicker{THE AGENT WRITES}{\fontsize{12}{15}\selectfont the code, and suggested tests\par}\end{column}
\begin{column}{.47\textwidth}\kicker{SOMETHING IT CAN'T TOUCH DECIDES}{\fontsize{12}{15}\selectfont which tests count\par}\end{column}
\end{columns}
\vspace{5mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.30\textwidth}L{.40\textwidth}L{.24\textwidth}@{}}
\textcolor{acid}{code} & \textcolor{acid}{answers used} & \textcolor{acid}{result}\\\midrule
broken & the agent's answer file & \textcolor{warn}{PASS}\\
same broken code & our locked copy & \textcolor{acid}{\bfseries REJECTED}\\
fixed code & our locked copy & \textcolor{acid}{\bfseries ACCEPTED}\\
\end{tabular}\par}
\note{The fix: separate the pens. The agent writes the code. It can suggest tests. Something it can't touch decides which tests count.

We kept our own locked copy of the answers. Same broken code: rejected. Fixed code: accepted.

Will an AI reviewer fix this? I doubt it. The agent writes what the reviewer reads. We didn't test that. But it's the same problem.}
\end{frame}

\renewcommand{\track}{08 / OUR TEST}
\begin{frame}[t]
% title: Even our own test lied
% time: 08:20-09:30
% cue: Slow down for the first test. Then read the four rows in order.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
\frametitle{Even our own test lied}
\vspace{1mm}
{\fontsize{12}{15}\selectfont Our first test tried the wrong code after the one-time approval was used up.\par}
\vspace{3mm}
{\tablefont\fontsize{10.5}{13}\selectfont\begin{tabular}{@{}L{.08\textwidth}L{.60\textwidth}L{.24\textwidth}@{}}
& \textcolor{acid}{the fixed test, one approval} & \textcolor{acid}{result}\\\midrule
1 & wrong code, approval still good & \textcolor{warn}{REFUSED}\\
2 & right code, wrong place & \textcolor{warn}{REFUSED}\\
3 & right code, right place & \textcolor{acid}{RELEASED}\\
4 & the same again & \textcolor{warn}{REFUSED}\\
\end{tabular}\par}
\vfill
{\fontsize{13}{16}\selectfont A no only means something if the yes still works.\par}
\note{Now the embarrassing part. Our release step lets one approved change out, once, to one place. We wrote a test to prove it blocks the wrong code.

It passed. But it tried the wrong code after the approval was already used up. Of course it was refused. The test proved nothing.

So we fixed the order. Wrong code first: refused. Wrong place: refused. Right code, right place: released. Again: refused.

A no only means something if the yes still works.}
\end{frame}

\renewcommand{\track}{09 / EVERY YES}
\begin{frame}[t]
% title: Every yes is a test
% time: 09:30-10:45
% cue: One breath per row: who says yes, our lab, the real case.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://www.cisa.gov/news-events/alerts/2025/03/18/supply-chain-compromise-third-party-tj-actionschanged-files-cve-2025-30066-and-reviewdogaction
% source: https://www.legitsecurity.com/blog/camoleak-critical-github-copilot-vulnerability-leaks-private-source-code
\frametitle{Every yes is a test}
\vspace{2mm}
{\tablefont\fontsize{11}{14}\selectfont\begin{tabular}{@{}L{.37\textwidth}L{.37\textwidth}L{.20\textwidth}@{}}
\textcolor{acid}{who says yes} & \textcolor{acid}{in our lab} & \textcolor{acid}{in the real world}\\\midrule
a service trusts a key & opened another team's data & tj-actions, 2025\\
the next job trusts a shared folder & changed another job's file & any shared folder\\
an upload service trusts a request & saved data into someone else's account & CamoLeak, 2025\\
the checker trusts the answer file & accepted the agent's answers & GenProg, SWE-bench\\
\end{tabular}\par}
\vfill
\scope{Recorded both ways: the bad yes, then the fix.}
\note{Tests aren't the only thing that says yes. Anything that trusts what the agent hands it is a test.

A service trusts a key. In our lab, a key opened another team's data. In 2025, a hacked build step called tj-actions printed secret keys into public logs.

The next job trusts a shared folder. Ours let one job change another's file.

An upload service trusts a request. Ours saved data into someone else's account. Last year, CamoLeak pulled private code out of GitHub's AI assistant through GitHub's own image links.

We recorded all four. And the fix for each, with the real job still working.}
\end{frame}

\renewcommand{\track}{10 / 2026}
\begin{frame}[t]
% title: The benchmark builders just found out
% time: 10:45-11:30
% cue: Point at the two numbers. Read the quote exactly.
% source: https://arxiv.org/abs/2609.08149
\frametitle{The benchmark builders just found out}
\vspace{4mm}
\kicker{ONE MODEL, ONE CODING BENCHMARK}
\vspace{2mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.40\textwidth}{\color{dim}\fontsize{9}{11}\selectfont answers in reach\par}{\fontsize{34}{38}\selectfont\bfseries\color{warn}78.8\%\par}\end{column}
\begin{column}{.55\textwidth}{\color{dim}\fontsize{9}{11}\selectfont answers out of reach\par}{\fontsize{34}{38}\selectfont\bfseries\color{acid}57.3\%\par}\end{column}
\end{columns}
\vspace{7mm}
{\fontsize{14}{18}\selectfont Their fix: ``We remove hidden evaluation files from the agent workspace.''\par}
\vfill
\scope{Source: SWE-Bench Pro Verified, September 2026. Model: GLM-5.2.}
\note{Last month, the people who build coding benchmarks found the same thing. Agents could reach the hidden tests. When they moved them out of reach, one model's score fell from 79 percent to 57.

Their fix: get the hidden tests out of the agent's folder. Separate the pens.}
\end{frame}

\renewcommand{\track}{11 / CLOSE}
\begin{frame}[t]
% title: It escaped the test
% time: 11:30-12:15
% cue: Back to Tuesday. Say the title line, pause, then the question. Stop.
% source: https://github.com/zozo123/nyc-talk
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{It escaped the test}
\vspace{8mm}
{\fontsize{22}{27}\selectfont\bfseries Your agent didn't escape the sandbox.\\{\color{acid}It escaped the test.}\par}
\vspace{8mm}
{\fontsize{13}{17}\selectfont Monday: pick one check you trust. Could the agent have written what made it pass?\par}
\vfill
{\color{acid}\ttfamily\fontsize{10}{12}\selectfont github.com/zozo123/nyc-talk\par}
\note{Back to Tuesday. The sandbox held. The checker held. Nothing broke out.

Your agent didn't escape the sandbox. It escaped the test.

So on Monday, pick one check your team trusts. Ask one question: could the agent have written what made it pass?

It's all on GitHub. Thank you.}
\end{frame}

\appendix

\renewcommand{\track}{A1 / SCOPE}
\begin{frame}[t]
% title: What this experiment shows
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{What this experiment shows}
\begin{columns}[T,onlytextwidth]
\begin{column}{.54\textwidth}
{\fontsize{10}{13}\selectfont
\begin{itemize}\setlength{\itemsep}{0.9mm}
\item The agent is a script. We tested what the setup allows, not what a model would do.
\item We did not run Opus 5.5. It's the example in the story.
\item The program is a tiny command-line model of a login check, not a web server.
\item Releasing means saving to a local database.
\item Not claimed: sandbox escape, kernel exploit, real breach, how often models cheat.
\end{itemize}\par}
\end{column}
\begin{column}{.40\textwidth}
{\tablefont\begin{tabular}{@{}L{.50\textwidth}L{.40\textwidth}@{}}
\textcolor{acid}{input} & \textcolor{acid}{right answer}\\\midrule
\texttt{admin:none} & 401\\
\texttt{admin:admin} & 200\\
\texttt{admin:user} & 403\\
\texttt{public:none} & 200\\
\texttt{admin:expired} & 401\\
\end{tabular}}
\end{column}
\end{columns}
\note{We tested whether the setup allows these moves, and whether the fix stops them. We did not measure how often a model would try, and we did not run Opus 5.5; it's the example in the story. The five cases on the right are the whole test. Passing them proves those five cases and nothing more.}
\end{frame}

\renewcommand{\track}{A2 / KEY}
\begin{frame}[t,fragile]
% title: The key reached another team's data
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The key reached another team's data}
\begin{lstlisting}
broad key   -> other team's data    200  (allowed)
task key    -> other team's data    403  (refused)
task key    -> its own input        200  (still works)
expired key, wrong-audience key     403  (refused)
\end{lstlisting}
\takeaway{A key limited to the task fixed it.}
\scope{Local test service with fake keys.}
\note{A key limited to the task fixed it, and the task could still read its own input. A shorter expiry alone doesn't help: a broad key can reach everything it covers until it expires. The service has to check what the key is for.}
\end{frame}

\renewcommand{\track}{A3 / SHARED FOLDER}
\begin{frame}[t,fragile]
% title: The shared file changed on the host
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The shared file changed on the host}
\begin{lstlisting}
shared folder, writable:  write next-job.json  -> file changed on the host
shared folder, read only: same write           -> refused, file unchanged
task output folder:       write the result     -> still works
\end{lstlisting}
\takeaway{The change stayed on the host, waiting for the next job.}
\scope{Real shared folders in the sandbox. We did not run the next job.}
\note{The change stayed on the host, waiting for the next job. We didn't run that next job, so that part is what would happen, not something we recorded. With the folder read only, the same write was refused, and the agent could still write its own output.}
\end{frame}

\renewcommand{\track}{A4 / UPLOAD}
\begin{frame}[t,fragile]
% title: The upload landed in another account
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{The upload landed in another account}
\begin{lstlisting}
upload to /accounts/other/uploads       -> 201, saved
  saved: {"data": "SYNTHETIC-PRIVATE-FIXTURE"}
fixed sender, extra field added         -> refused, nothing sent
agent tries the service directly        -> unreachable
fixed sender, normal report to team     -> delivered
\end{lstlisting}
\takeaway{Reaching a service doesn't mean you may do anything there.}
\scope{Local test service. Not a firewall bypass.}
\note{Reaching a service doesn't mean you may do anything there. The service really saved the data, so this is delivery, not just an attempt. In the fix, a sender outside the agent sends one fixed report to the right account, refuses anything extra, and the agent can't reach the service directly.}
\end{frame}

\renewcommand{\track}{A5 / OLD BUG}
\begin{frame}[t,fragile]
% title: A bug in our old release step
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{A bug in our old release step}
\begin{lstlisting}
approve the fixed code
swap in different code, keep the old fingerprint
old release step: RELEASED
what was released no longer matches the record
\end{lstlisting}
\vspace{3mm}
{\fontsize{12}{15}\selectfont It trusted an old fingerprint after the code changed.\par}
\scope{Needed direct access to our own controller. We did not show the agent could reach it.}
\note{Our old release step trusted an old fingerprint after the code changed. Triggering it needed direct access to our own controller, and we did not show the agent could get that. The new release step computes the fingerprint from the exact code it releases.}
\end{frame}

\renewcommand{\track}{A6 / RECORDINGS}
\begin{frame}[t]
% title: What we recorded
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{What we recorded}
{\tablefont\begin{tabular}{@{}L{.25\textwidth}L{.10\textwidth}L{.59\textwidth}@{}}
\textcolor{acid}{record} & \textcolor{acid}{checks} & \textcolor{acid}{covers}\\\midrule
Linux lab & 30 & keys, shared folders, uploads, a second checker, release\\
Locked checker & 13 & the locked checker, the answer file, release\\
Local run & 8 & the release rules, no sandbox\\
Regression tests & 49 & our own code and evidence checks\\
Old bug & 1 & the old fingerprint bug\\
\end{tabular}}
\takeaway{These count checks, not bugs found.}
\note{These count checks, not bugs found. The 49 are regression tests for our own code, not sandbox tests. The old bug is one reproduction of a mistake in our earlier release step.}
\end{frame}

\renewcommand{\track}{A7 / RUN IT}
\begin{frame}[t,fragile]
% title: Run it yourself
% source: https://github.com/zozo123/nyc-talk
% source: https://github.com/containers/bubblewrap#sandbox-security
\frametitle{Run it yourself}
\begin{lstlisting}
git clone https://github.com/zozo123/nyc-talk
cd nyc-talk
make test factory       # any OS, Python 3.10+; no sandbox
make record-all         # a throwaway Linux box with bubblewrap
make final              # the PDFs; needs TeX Live
\end{lstlisting}
\vspace{2mm}
{\fontsize{10}{13}\selectfont You need a throwaway Linux machine with bubblewrap, Python 3.10+ and make. GitHub Actions builds the PDFs on every change.\par}
\scope{Sandbox: bubblewrap.}
\note{You need a throwaway Linux machine with bubblewrap, Python 3.10 or later, and make. On any computer, make test and make factory run the local checks, without a sandbox. GitHub Actions builds the PDFs on every change. No cloud account needed.}
\end{frame}

\renewcommand{\track}{A8 / APPROVAL}
\begin{frame}[t]
% title: What the approval covers
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{What the approval covers}
{\fontsize{11}{14}\selectfont The controller checks the results, then signs its decision together with the code, the answers and the destination.\par}
\vspace{3mm}
{\tablefont\begin{tabular}{@{}L{.20\textwidth}L{.72\textwidth}@{}}
\textcolor{acid}{the code} & its fingerprint\\
\textcolor{acid}{the answers} & the expected results, the rules, the checker's own code\\
\textcolor{acid}{the destination} & account, environment, operation, file\\
\textcolor{acid}{the rest} & task, run, environment, expiry, use once\\
\end{tabular}\par}
\vspace{3mm}
{\fontsize{9.5}{12}\selectfont In order: wrong code \textcolor{warn}{REFUSED}, wrong place \textcolor{warn}{REFUSED}, right place \textcolor{acid}{RELEASED}, again \textcolor{warn}{REFUSED}.\par}
\scope{Signed with a key only the controller holds. The raw results are not signed.}
\note{The controller checks the results, then signs its decision together with the code, the answers and the destination, using a key only it holds. The approval names the task and the run, expires, and works once. The raw results themselves are not signed.}
\end{frame}

\renewcommand{\track}{A9 / SOURCES}
\begin{frame}[t]
% title: Sources
% source: https://arxiv.org/abs/1803.03453
% source: https://sakana.ai/ai-scientist/
% source: https://github.com/SWE-bench/SWE-bench/issues/465
% source: https://arxiv.org/abs/2609.08149
% source: https://www.cisa.gov/news-events/alerts/2025/03/18/supply-chain-compromise-third-party-tj-actionschanged-files-cve-2025-30066-and-reviewdogaction
% source: https://www.legitsecurity.com/blog/camoleak-critical-github-copilot-vulnerability-leaks-private-source-code
% source: https://github.com/OWASP/www-project-top-10-ci-cd-security-risks/blob/main/CICD-SEC-04-Poisoned-Pipeline-Execution.md
\frametitle{Sources}
{\fontsize{8.6}{10.8}\selectfont
\begin{itemize}\setlength{\itemsep}{0.9mm}
\item \textbf{GenProg deletes the answer file.} Lehman et al., \emph{The Surprising Creativity of Digital Evolution}, arXiv 1803.03453, 2018.
\item \textbf{The AI Scientist gives itself more time.} Sakana AI, \emph{The AI Scientist}, August 2024.
\item \textbf{Agents find the hidden fix in the history.} SWE-bench issue 465, September 2025.
\item \textbf{Scores fall once answers are out of reach.} Zheng et al., \emph{SWE-Bench Pro Verified}, arXiv 2609.08149, September 2026.
\item \textbf{Secret keys in public build logs.} CISA alert on tj-actions/changed-files, CVE-2025-30066, March 2025.
\item \textbf{Private code out through GitHub's own image links.} Legit Security, \emph{CamoLeak}, CVE-2025-59145, 2025.
\item \textbf{The name for this in CI/CD.} OWASP CICD-SEC-4, Poisoned Pipeline Execution.
\item \textbf{Our lab.} github.com/zozo123/nyc-talk: code, recordings, this deck.
\end{itemize}\par}
\note{These are the sources for every real-world case in the talk. Each one is someone else's report. We don't claim their numbers as ours.}
\end{frame}

\end{document}
