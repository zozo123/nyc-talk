% Canonical LaTeX source. Edit this file, then run make snapshot.
% Speaker text, timing and source links live beside each frame.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta}
\definecolor{void}{HTML}{07090D}
\definecolor{paper}{HTML}{F2F5F7}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{muted}{HTML}{A9B3BF}
\definecolor{danger}{HTML}{FF6975}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\fontsize{21}{24}\selectfont,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{frametitle}{\vspace{5mm}\insertframetitle\par\vspace{4mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setbeamertemplate{footline}{\hspace{10mm}{\color{muted}\fontsize{6}{7}\selectfont YOSSI ELIAZ / NYC / 21 OCT 2026}\hfill{\color{muted}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{3mm}}
\setlength{\parskip}{3pt}
\lstset{basicstyle=\ttfamily\fontsize{11}{14}\selectfont,keywordstyle=\color{acid},commentstyle=\color{muted},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=4pt,belowskip=4pt}
\newcommand{\labeltext}[1]{\par{\color{acid}\fontsize{9}{11}\selectfont\bfseries #1}\par}
\newcommand{\bigresult}[2]{\par\vspace{1.5mm}{\color{#1}\fontsize{42}{46}\selectfont\bfseries #2}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{14}{17}\selectfont #1}\par}
\newcommand{\scope}[1]{\par\vspace{2mm}{\color{muted}\fontsize{7.5}{9}\selectfont #1}\par}
\newcommand{\tablefont}{\fontsize{11}{14}\selectfont\renewcommand{\arraystretch}{1.4}\setlength{\tabcolsep}{4pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={15-minute security talk: independent acceptance and exact-byte release}}
\title{Your Agent Escaped Without Escaping the Sandbox}
\author{Yossi Eliaz}
\date{October 21, 2026}
\begin{document}

\begin{frame}[plain,t]
% title: Your Agent Escaped Without Escaping the Sandbox
% time: 00:00-00:30
% cue: Start immediately. The contradiction is on the next slide.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\vspace{7mm}
\labeltext{AI AGENT SECURITY SUMMIT / NEW YORK / OCTOBER 21, 2026}
\vspace{6mm}
{\fontsize{29}{32}\selectfont\bfseries Your Agent Escaped\\Without Escaping\\the Sandbox\par}
\vspace{7mm}
{\fontsize{12}{15}\selectfont Yossi Eliaz, PhD\par}
{\color{muted}\fontsize{10}{13}\selectfont Principal Engineer and Head of DevRel at Incredibuild.com\par}
\takeaway{Who decides when the agent's work is ready to release?}
\vspace{6mm}
\note{You ask an agent to fix authorization. It finishes. The checker says PASS. The checker itself is protected. Would you release the result?

I am Yossi Eliaz. Let me show you a small experiment where every one of those statements is true, and the authorization bug is still there.}
\end{frame}

\begin{frame}[t]
% title: The checker passes. The policy fails.
% time: 00:30-01:20
% cue: Pause between PASS and 200. These are recorded observations.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The checker passes. The policy fails.}
\vspace{3mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}
\labeltext{CHECKER RESULT}
\bigresult{acid}{PASS}
Same checker hash\\Same candidate bytes
\end{column}
\begin{column}{.47\textwidth}
\labeltext{UNAUTHENTICATED ADMIN CASE}
\bigresult{danger}{200}
Policy requires \textbf{401}\\\texttt{admin:none}
\end{column}
\end{columns}
\takeaway{What changed?}
\scope{Recorded, deterministic reproduction. Five-case CLI status-code model. Known mechanism.}
\note{On the left, the checker reports PASS. On the right, the unauthenticated admin case returns two hundred. Our policy requires four-oh-one.

This is a deliberately small command-line model of authorization. Those numbers are program outputs. We are not showing packets from a production server.

The surprising part is what stayed constant. The broken candidate has the same bytes. The checker has the same bytes. The operating system refuses the worker's attempt to overwrite the checker.

Something else changed. To see it, follow the inputs to the decision.}
\end{frame}

\begin{frame}[t]
% title: The task and the trust boundary
% time: 01:20-02:15
% cue: State the worker's actual control. Keep the setup brief.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
\frametitle{The task and the trust boundary}
{\fontsize{17}{21}\selectfont Task: reject unauthorized admin access.\par}
\vspace{3mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}
\labeltext{WORKER CAN CHANGE}
Task code\\Workspace files\\Proposed tests and reports
\end{column}
\begin{column}{.47\textwidth}
\labeltext{WORKER CANNOT CHANGE}
Read-only checker mount\\Controller process and key\\Controller release store
\end{column}
\end{columns}
\takeaway{Promise: the worker cannot approve a policy-violating artifact.}
\scope{Linux namespaces and bind mounts. Synthetic fixtures. Trusted kernel and controller.}
\note{The worker can edit task code and its workspace. It can propose tests and write reports. That is ordinary coding-agent authority.

We protect the checker with a read-only bind mount. The controller process, approval key and release store remain outside worker control. We trust the kernel and the controller.

The promise is narrow: the worker should not be able to approve an artifact that fails the controller's acceptance policy.

Our worker is a deterministic script. That lets us test whether an action is permitted. It does not measure whether a particular model would discover or choose that action.}
\end{frame}

\begin{frame}[t,fragile]
% title: The original check catches the bug
% time: 02:15-03:10
% cue: Establish the negative control before revealing the edit.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The original check catches the bug}
\begin{columns}[T,onlytextwidth]
\begin{column}{.52\textwidth}
\labeltext{BAD CANDIDATE: ALWAYS 200}
\begin{lstlisting}
$ python3 handler.py admin:none
200
\end{lstlisting}
\vspace{3mm}
\labeltext{HONEST EXPECTATION}
\begin{lstlisting}
admin:none   requires 401
\end{lstlisting}
\end{column}
\begin{column}{.39\textwidth}
\labeltext{CHECKER}
\bigresult{danger}{FAIL}
Exit status: 1
\vspace{5mm}
\labeltext{WORKER WRITES CHECKER}
{\color{acid}\large DENIED\par}
\end{column}
\end{columns}
\takeaway{The checker detects the bad behavior. Its protection works.}
\scope{The honest key rejects the candidate before the worker edits any expectation.}
\note{First, establish that the check can catch the bug. The candidate always returns two hundred. With the honest expectations, the checker reports FAIL and exits with status one.

Next, the worker tries to write the checker. The operating system denies that write. We also hash the checker before and after the worker step.

So this is not the simple demonstration where someone replaces a grader with a program that exits zero. The check works, and the protected file stays protected.

Now look at the file the checker opens to learn what the answers should be.}
\end{frame}

\begin{frame}[t,fragile]
% title: The worker changes the answer key
% time: 03:10-04:45
% cue: Point at expected.json, then PASS. Pause before the takeaway.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The worker changes the answer key}
\labeltext{THE PROTECTED CHECKER READS FROM ITS WORKING DIRECTORY}
\begin{lstlisting}
expected = json.loads(Path("expected.json").read_text())
\end{lstlisting}
\vspace{3mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.59\textwidth}
\labeltext{WORKER-OWNED expected.json}
{\fontsize{22}{27}\selectfont\texttt{admin:none}\par}
{\fontsize{24}{29}\selectfont expected: \textcolor{muted}{\texttt{401}} $\longrightarrow$ \textcolor{danger}{\texttt{200}}\par}
\scope{The worker changes all five expected outputs to 200.}
\end{column}
\begin{column}{.32\textwidth}
\labeltext{SAME CHECKER}
\bigresult{acid}{PASS}
Exit status: 0
\end{column}
\end{columns}
\takeaway{The worker changed what counted as correct.}
\scope{Checker SHA-256 before = after: \texttt{f5d8d3468bbe...} (abbreviated). Candidate unchanged.}
\note{The checker reads expected dot json from its working directory. That file lives in the workspace the worker can edit.

The worker changes the expected outputs to two hundred. For the anonymous admin case, the expected value changes from four-oh-one to two hundred. The candidate still returns two hundred. Now those values match, and the unchanged checker prints PASS.

Protecting the executable protected only one dependency of the decision. The answer key still belonged to the producer.

The consequence is precise. A controller that promotes this PASS to release eligibility accepts the worker's definition of success. This experiment demonstrates the false PASS. It does not claim a production deployment occurred.

You can replace this file with a workspace plugin, imported helper, test configuration or worker-written report. Those are audit leads, not additional attacks we measured here. The concrete dependency we reproduced is the answer key.}
\end{frame}

\begin{frame}[t]
% title: Independent checks reject the bad bytes
% time: 04:45-06:15
% cue: Read the first three rows slowly. The fourth row proves the job still works.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Independent checks reject the bad bytes}
{\tablefont
\begin{tabular}{@{}p{.24\textwidth}p{.47\textwidth}p{.23\textwidth}@{}}
\textcolor{acid}{Candidate} & \textcolor{acid}{Acceptance criteria} & \textcolor{acid}{Result}\\\midrule
Always 200 & Honest workspace key & FAIL\\
Same bytes & Worker-edited workspace key & \textcolor{danger}{PASS}\\
Same bytes & Controller-owned cases & \textcolor{acid}{REJECT}\\\addlinespace
Corrected handler & Controller-owned cases & \textcolor{acid}{ACCEPT}\\
\end{tabular}}
\vspace{5mm}
{\color{muted}Controller expects:\quad\texttt{401\quad200\quad403\quad200\quad401}\par}
\takeaway{Move the comparison and its answer key outside worker control.}
\scope{Recorded paired controls. The corrected handler passes all five cases. Finite policy coverage.}
\note{Here is the controlled comparison. The first three rows use the same bad candidate bytes.

The honest workspace key makes the checker fail. The worker-edited key makes it pass. Controller-owned expectations reject that same candidate.

Then we run a corrected handler. It passes all five controller-owned cases. Anonymous admin and expired credentials produce four-oh-one. An ordinary user produces four-oh-three. The authorized administrator and public route produce two hundred.

The repair preserves the legitimate job. We have not simply disconnected everything until the attack stops.

Five cases establish this policy's tested behavior. They do not prove all possible application behavior. The useful result is the paired change: move the criteria outside producer control, reject the bad artifact, and still accept the valid one.}
\end{frame}

\begin{frame}[t]
% title: Production and acceptance
% time: 06:15-07:40
% cue: Explain the two arrows across the boundary: bytes and observations.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{Production and acceptance}
\centering
\begin{tikzpicture}[>=Stealth,font=\small]
\node[anchor=west,text=muted,font=\bfseries] at (0,0) {UNTRUSTED EXECUTION};
\node[anchor=west,text=acid,font=\bfseries] at (7,0) {TRUSTED CONTROLLER};
\draw[muted,dashed] (6.2,.35)--(6.2,-4.05);
\node[anchor=west,align=left] (worker) at (0,-1) {Worker produces candidate};
\node[anchor=west,align=left] (freeze) at (7,-1) {Freeze exact bytes};
\node[anchor=west,align=left] (run) at (0,-2.4) {Isolated candidate run\\Read-only candidate mount};
\node[anchor=west,align=left] (judge) at (7,-2.4) {Compare against owned cases};
\node[anchor=west,align=left,text=acid] (release) at (7,-3.65) {Approve and publish frozen bytes};
\draw[->,acid] (worker.east)--(freeze.west);
\draw[->,muted] (freeze.south)--++(0,-.35)-|(run.north);
\draw[->,acid] (run.east)--node[above,font=\scriptsize,text=muted]{observations}(judge.west);
\draw[->,acid] (judge.south)--++(0,-.5);
\end{tikzpicture}
\scope{Recorded repair: Linux sandbox execution and controller-side comparison. No cloud dependency.}
\note{The controller first freezes the candidate bytes. A separate isolated run executes those bytes with a read-only candidate mount. The run returns observations. The controller compares them with criteria the worker cannot rewrite.

The controller also owns the approval key and publication store. A harness saying finished has no authority to issue that approval.

A fresh acceptance machine can help implement this separation. But copying the producer's answer key into it preserves the bad dependency. A fork of the producer's disk also carries whatever expectations, configuration and caches the producer left there.

Our recorded repair uses Linux isolation and a controller-side comparison. The design requirement is ownership of the decision's dependencies. Count those dependencies before you count machines.}
\end{frame}

\begin{frame}[t]
% title: The gate binds approval to bytes
% time: 07:40-09:00
% cue: Stress fresh approval. A consumed nonce would confound the substitution test.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The gate binds approval to bytes}
\labeltext{ONE APPROVAL FOR THE GOOD CANDIDATE. THREE ATTEMPTS, IN ORDER.}
\vspace{4mm}
{\tablefont
\begin{tabular}{@{}p{.08\textwidth}p{.65\textwidth}p{.21\textwidth}@{}}
1 & Fresh approval + substituted bad bytes & \textcolor{acid}{DENIED}\\\addlinespace
2 & Same approval + original good bytes & \textcolor{acid}{PUBLISHED}\\\addlinespace
3 & Same approval + original bytes again & \textcolor{acid}{DENIED}\\
\end{tabular}}
\takeaway{A failed substitution leaves the valid publication possible.}
\scope{Recorded local gate. SQLite stores the exact bytes and consumes the nonce in one transaction.}
\note{Independent checking still leaves a second question: do we release the bytes we checked?

Take a fresh approval for the good candidate. Offer bad bytes under that approval. The gate denies them. Offer the original bytes with the same approval. They publish. Offer them again. The gate rejects the replay.

The order matters. If you consume the approval first, a broken digest check can hide behind the already-used nonce. We corrected that misleading test order in this repository.

Here, publication means storing the approved bytes in a controller-owned SQLite database. It is a local release model. The same transaction stores the bytes and consumes the nonce. A real deployment needs to preserve that binding all the way to the consumer.}
\end{frame}

\begin{frame}[t]
% title: Three agents share one writable answer key
% time: 09:00-10:00
% cue: Explicitly label this as an architectural implication.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{Three agents share one writable answer key}
\vspace{4mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.35\textwidth}
{\fontsize{19}{27}\selectfont Builder\\Reviewer\\Judge\par}
\end{column}
\begin{column}{.6\textwidth}
\labeltext{ALL USE THE SAME ANSWER KEY}
{\fontsize{20}{25}\selectfont\texttt{expected.json}\par}
\vspace{3mm}
{\color{danger}\large The builder can write it.\par}
\end{column}
\end{columns}
\takeaway{A snapshot preserves that dependency too.}
\scope{Architectural inference from the recorded dependency. No multi-agent or snapshot attack trial.}
\note{This is why the experiment matters to an agent factory. Add a builder, a reviewer and a judge. Give them different prompts, or different models. If they all trust the builder's writable answer key, they still share the same compromised criterion.

Freezing that workspace in a snapshot preserves the criterion. It does not establish who was authorized to choose it.

This slide is an architectural implication of our dependency, not a measured multi-agent attack. Agents can propose tests and explore solutions. Promotion criteria need a separate owner, and proposals need an explicit route to become authorized policy.}
\end{frame}

\begin{frame}[t]
% title: Four deployment boundaries
% time: 10:00-12:00
% cue: The surrounding abstract categories. Spend most time on the positive controls.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Four deployment boundaries}
{\fontsize{10.5}{13}\selectfont\renewcommand{\arraystretch}{1.65}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.16\textwidth}p{.43\textwidth}p{.35\textwidth}@{}}
\textcolor{acid}{Boundary} & \textcolor{acid}{Unauthorized effect must fail} & \textcolor{acid}{Legitimate job must work}\\\midrule
Identity & Read another project's data & Read the task input\\
Files & Modify another job's input & Write the task output\\
Outbound & Send to another account & Send the authorized report\\
Acceptance & Change criteria or swap bytes & Release the verified fix\\
\end{tabular}}
\takeaway{Test the effect at the enforcing component.}
\scope{Supporting lab: synthetic tokens and loopback HTTP, real bind mounts, fixed-operation broker.}
\note{The answer-key failure sits inside a wider map of authority. Our repository includes three supporting boundary demonstrations.

First, identity. A broad fixture token reads another project's private data. A task-scoped token gets denied there, while still reading its own input. The service must enforce the resource grant. Short lifetime alone cannot narrow a broad token.

Second, files. A writable mount lets the worker change the next job's fixture. A read-only input mount blocks that write, while a separate output remains writable.

Third, outbound operations. One service accepts both the legitimate report and an upload to another account. The corrected broker fixes the destination and body, and the worker has no direct route around it. We inspect the receiver to confirm delivery or its absence.

Finally, acceptance. The worker must not change the governing criteria or substitute the released bytes.

For each boundary, require both a denied unauthorized effect and a successful legitimate job. Otherwise a system that cannot do any work looks secure. These are separate boundary experiments, not steps in one attack chain.}
\end{frame}

\begin{frame}[t]
% title: A release decision needs more than PASS
% time: 12:00-13:00
% cue: Explain the three responsibilities. Do not turn history into the gate.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://slsa.dev/spec/v1.2/verifying-artifacts
\frametitle{A release decision needs more than PASS}
{\fontsize{15}{20}\selectfont
\textcolor{acid}{Verification}\quad These bytes satisfied these criteria.\par
\vspace{4mm}
\textcolor{acid}{Gate}\quad This approval authorizes these bytes on this run.\par
\vspace{4mm}
\textcolor{acid}{History}\quad This record matches a protected anchor.\par}
\takeaway{Bind artifact, verifier, expected results, policy and run.}
\scope{The gate also binds environment description, task, decision, nonce, expiry and schema.}
\note{A useful approval names the artifact, verifier, expected results, policy and run. Our reference gate also binds the environment description, task, decision, nonce, expiry and schema.

Keep verification, authorization and history distinct. A consistent history can faithfully record a bad decision. Our hash-chain check detects a rewrite only relative to a separately protected anchor.

Likewise, a green worker test or a successful harness exit cannot substitute for independent acceptance. Missing or malformed observations produce no approval.

For a release review, ask for evidence that the published artifact is the artifact evaluated under the intended criteria. A PASS without that context leaves the important question unanswered.}
\end{frame}

\begin{frame}[plain,t]
% title: Who controls what makes the work releasable?
% time: 13:00-14:00
% cue: Leave this slide on screen. Stop by 14:00 and retain one minute of margin.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\vspace{7mm}
{\fontsize{27}{31}\selectfont\bfseries Who controls what\\makes the work\\releasable?\par}
\vspace{5mm}
{\fontsize{15}{20}\selectfont The worker proposes.\\Independent criteria decide.\\The gate releases the evaluated bytes.\par}
\vfill
{\color{acid}\ttfamily\fontsize{12}{15}\selectfont\href{https://github.com/zozo123/nyc-talk}{github.com/zozo123/nyc-talk}\par}
\scope{Yossi Eliaz / Source, recorded evidence and reproducible controls}
\vspace{5mm}
\note{The checker was protected. The candidate never improved. The worker changed the answer key, and PASS changed meaning.

That is the question I want you to take home: who controls what makes the work releasable?

Trace the authority through every input to that decision. Keep the criteria outside producer control. Freeze the candidate, evaluate it independently, and publish those exact bytes. Then rerun the unauthorized attempt and the legitimate task.

The repository has the source, recorded observations and limits. This is a controlled reproduction of a known failure mechanism, with a repair you can inspect. Thank you.}
\end{frame}

\appendix
\begin{frame}[t]
% title: Appendix: the five policy cases
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The five policy cases}
{\tablefont
\begin{tabular}{@{}p{.38\textwidth}p{.23\textwidth}p{.32\textwidth}@{}}
\textcolor{acid}{CLI input} & \textcolor{acid}{Expected} & \textcolor{acid}{Always-200 candidate}\\\midrule
\texttt{admin:none} & 401 & \textcolor{danger}{200: mismatch}\\
\texttt{admin:admin} & 200 & 200: match\\
\texttt{admin:user} & 403 & \textcolor{danger}{200: mismatch}\\
\texttt{public:none} & 200 & 200: match\\
\texttt{admin:expired} & 401 & \textcolor{danger}{200: mismatch}\\
\end{tabular}}
\takeaway{The repaired handler returns all five expected values.}
\scope{CLI fixture. The label admin:expired models an expired credential. No token is parsed here.}
\note{The handler accepts a route and an authentication label on the command line. It emits an integer status value. The independent controller checks these five observations. The repaired result is correct for these cases. This does not exercise an HTTP stack or validate a real credential.}
\end{frame}

\begin{frame}[t]
% title: Appendix: identity and file effects
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Identity and file effects}
{\tablefont
\begin{tabular}{@{}p{.47\textwidth}p{.22\textwidth}p{.25\textwidth}@{}}
\textcolor{acid}{Attempt} & \textcolor{acid}{Broad grant} & \textcolor{acid}{Scoped grant}\\\midrule
Read other-project private fixture & HTTP 200 & HTTP 403\\
Read intended task input & HTTP 200 & HTTP 200\\\addlinespace
Write next-job host fixture & Changed & Denied\\
Write own patch output & Allowed & Allowed\\
\end{tabular}}
\takeaway{The resource service and mount policy enforce the denials.}
\scope{Real loopback HTTP and Linux bind mounts. Public fixture tokens and synthetic data only.}
\note{The token case is actual loopback HTTP. Synthetic runner and task grants model different resource scopes. Expired and wrong-audience fixtures are also denied. The mount case observes the host fixture after an ordinary worker write. Read-only input protects integrity, not confidentiality. The legitimate output is separately writable.}
\end{frame}

\begin{frame}[t,fragile]
% title: Appendix: an allowed service accepts the wrong upload
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{An allowed service, an unauthorized upload}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}
\labeltext{WORKER CHOOSES PATH AND BODY}
\begin{lstlisting}
POST /accounts/other/uploads
{"data":"PRIVATE-FIXTURE"}

201 STORED
receiver has the bytes
\end{lstlisting}
\end{column}
\begin{column}{.47\textwidth}
\labeltext{BROKER OWNS THE OPERATION}
\begin{lstlisting}
publish_report(task)

fixed recipient + body
extra fields: denied
direct route: unavailable
team report: delivered
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{A permitted hostname still contains many possible operations.}
\scope{Loopback allowed-service model. No enterprise firewall or SaaS vulnerability claim.}
\note{The fixture service intentionally permits both account paths. We check its delivery record to establish the consequence. The repair constrains the broker request and removes direct worker reachability to the service. A free-form reporting API would need additional analysis. These observations do not establish that every possible output channel prevents exfiltration.}
\end{frame}

\begin{frame}[t,fragile]
% title: Appendix: the audit of our original gate
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{The audit of our original gate}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{ARCHIVED BASELINE: 4b06c51}
\begin{lstlisting}
replace candidate bytes
retain old digest field

gate: PUBLISHED
manifest != actual bytes
\end{lstlisting}
\end{column}
\begin{column}{.46\textwidth}
\labeltext{CURRENT REFERENCE GATE}
Immutable byte entries\\Derived manifest and digest\\Publication stores bytes\\Atomic nonce consumption
\end{column}
\end{columns}
\takeaway{Internal API integrity flaw in our reference implementation.}
\scope{Requires controller-object access. No demonstrated worker-to-controller exploit path. Old gate stored a marker.}
\note{The archived counterexample mutates a controller-side Python object. It can retain a stale digest while changing the candidate bytes. The original gate then records a publication marker. The current implementation derives the digest from immutable bytes and stores the approved bytes. This is a real defect in our earlier reference API with a stated prerequisite, not evidence of an agent reaching the controller or a compromised production deployment.}
\end{frame}

\begin{frame}[t]
% title: Appendix: evidence and limits
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{Evidence and limits}
{\fontsize{10.5}{13}\selectfont\renewcommand{\arraystretch}{1.35}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.29\textwidth}p{.19\textwidth}p{.47\textwidth}@{}}
\textcolor{acid}{Evidence} & \textcolor{acid}{Checks} & \textcolor{acid}{Scope}\\\midrule
Linux lab & 29 & Namespaces, mounts, loopback HTTP\\
Isolated checker & 11 & Read-only checker, mutable key, release controls\\
Local factory & 6 & Protocol model without OS isolation\\
Regression suite & 37 & Controller and evidence regressions\\
Archived gate audit & 1 & Controller API misuse\\
\end{tabular}}
\takeaway{Assertion counts describe coverage, not vulnerability counts.}
\scope{No vendor zero-day, customer incident, LLM attack-success rate or general security certification.}
\note{The raw records bind their experiment source files. The build rejects stale or incomplete evidence. The centerpiece uses real read-only mounts. The local protocol executor by itself is not an operating-system sandbox. Boat was not used for this stage evidence. Multiple-agent and snapshot examples are architectural inferences. No human speaking-time measurement is claimed by the timed manuscript.}
\end{frame}

\begin{frame}[t,fragile]
% title: Appendix: sources and reproduction
% source: https://github.com/zozo123/nyc-talk
% source: https://github.com/containers/bubblewrap#sandbox-security
% source: https://slsa.dev/spec/v1.2/verifying-artifacts
% source: https://www.anthropic.com/research/emergent-misalignment-reward-hacking
\frametitle{Sources and reproduction}
{\fontsize{10.5}{13}\selectfont
\href{https://github.com/zozo123/nyc-talk}{\textcolor{acid}{Our source and evidence}}\quad github.com/zozo123/nyc-talk\par
\href{https://github.com/containers/bubblewrap#sandbox-security}{\textcolor{acid}{bubblewrap security model}}\quad Caller-defined sandbox policy\par
\href{https://slsa.dev/spec/v1.2/verifying-artifacts}{\textcolor{acid}{SLSA v1.2: verifying artifacts}}\quad Identity, digest and expectations\par
\href{https://www.anthropic.com/research/emergent-misalignment-reward-hacking}{\textcolor{acid}{Anthropic: reward hacking, November 2025}}\quad Prior empirical work\par}
\vspace{4mm}
\begin{lstlisting}
make test               # controller and evidence regressions
make record-all         # Linux fixtures; requires bubblewrap
make snapshot           # LaTeX PDF + recorded offline replay
\end{lstlisting}
\scope{Controlled reproduction of a known mechanism. Source hashes identify bytes; they are not third-party attestations.}
\note{The external references supply prior work and architectural context. We do not transfer Anthropic's model measurements to this experiment and do not claim SLSA compliance. Use a disposable Linux environment for the isolated fixtures. The deck and replay use recorded evidence and require no cloud account.}
\end{frame}
\end{document}
