\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{listings}
\usepackage{hyperref}
\definecolor{void}{HTML}{101214}
\definecolor{paper}{HTML}{F0F0E8}
\definecolor{acid}{HTML}{C7FF48}
\definecolor{alarm}{HTML}{FF756B}
\definecolor{muted}{HTML}{A7ADB3}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\Large,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}{\hfill{\color{muted}\scriptsize Yossi Eliaz\hspace{1em}\insertframenumber\hspace{1em}}\vspace{3mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setlength{\parskip}{7pt}
\lstset{basicstyle=\ttfamily\small,keywordstyle=\color{acid},commentstyle=\color{muted},stringstyle=\color{acid},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false}
\newcommand{\kicker}[1]{{\color{acid}\ttfamily\small #1}\par}
\newcommand{\bigline}[1]{{\fontsize{26}{30}\selectfont\bfseries #1\par}}
\newcommand{\subline}[1]{\par{\color{muted}\normalsize #1}\par}
\newcommand{\evidence}{\vfill{\color{muted}\scriptsize\evidencescope}}
\input{slides/evidence.tex}
\title{Your Agent Escaped Without Escaping the Sandbox}
\author{Yossi Eliaz}
\date{October 21, 2026}
\begin{document}

\begin{frame}[plain]
\kicker{NYC / AI AGENT SECURITY SUMMIT}
\vspace{4mm}
{\fontsize{28}{32}\selectfont\bfseries Your Agent Escaped\\Without Escaping\\the Sandbox\par}
\vspace{6mm}
{\large Yossi Eliaz\par}
{\small Principal Engineer and Head of DevRel at Incredibuild.com\par}
\vspace{2mm}
\subline{October 21, 2026}
\note{00:00 to 00:35. Hi, I'm Yossi. Read the title. Escaped without escaping. Every namespace and cgroup can work as designed and you can still have a real breach. That abstract line is the demo. The agent never climbed the wall. We cut four doors so it could do the job. It used the doors.}
\end{frame}

\begin{frame}{The wall is not the interesting part}
\bigline{A sandbox is a wall.\\A working agent needs doors.}
\vspace{3mm}
\textcolor{acid}{credentials}\quad
\textcolor{acid}{mounts}\quad
\textcolor{acid}{allowed endpoint}\quad
\textcolor{acid}{done}
\vspace{3mm}
\subline{You cannot weld them shut. So which layer can refuse while it is still being helpful?}
\note{00:35 to 01:30. Keep the wall. What is new: classical sandbox assumes the workload might leave. An agent is hired to finish a ticket. It will use every job interface: token, files, the one host, the signal that means done. Those are the four abstract doors. Not four random bugs. You cannot weld them shut. Question is which layer refuses a door from outside the worker.}
\end{frame}

\begin{frame}[fragile]{Door 1 / inherited credentials}
\begin{lstlisting}
GET /projects/other/private

runner token     200
task token       403
its own input    200
\end{lstlisting}
\subline{Short-lived credential projection. Scope is the lock.}
\evidence
\note{01:30 to 03:20. Job needed input. We copied the CI runner token. Other project 200. Nobody escaped. Dad's badge on the intern. Lock from the abstract: short-lived credential projection. This task, this audience, this hour. 403. Own input still 200. Expiry is courtesy. Scope is the lock.}
\end{frame}

\begin{frame}[fragile]{Door 2 / dangerous mounts}
\begin{lstlisting}
write /shared/next-job.json

writable bind    host file changes
scoped bind      write denied
/output          still works
\end{lstlisting}
\subline{Capability-scoped filesystems. Tomorrow's snapshot is already in the room.}
\evidence
\note{03:20 to 05:00. Job needs files. Shared writable folder, being helpful. Legal write, next job's file changes. Mount did its job. Lock: capability-scoped filesystems. Read this, write that. Chair leaves the room. Output still works. If tomorrow forks this snapshot, tomorrow is already in the room.}
\end{frame}

\begin{frame}[fragile]{Door 3 / allowed endpoint}
\begin{lstlisting}
same host, wrong mailbox

team/reports      intended
other/uploads     gets the private fixture
\end{lstlisting}
\subline{Real process isolation. The worker does not address the envelope.}
\evidence
\note{05:00 to 07:00. Job must publish. Firewall loves the hostname. Worker posts the private fixture to another account on that host. The allowed endpoint was the exit. Lock: real process isolation, worker cannot dial the mailroom. Broker writes one boring postcard. Extra fields no. Real report still arrives.}
\end{frame}

\begin{frame}[fragile]{Door 4 / verifier tampering}
\begin{lstlisting}
checker.py               hash unchanged
expected.json            worker-written
CI / merge               PASS

GET /admin  (no cookie)  401 required
shipped                  200
\end{lstlisting}
\subline{You hired it to get to done. Done is a door.}
\evidence
\note{07:00 to 09:45. Door labeled done. Ticket: /admin no cookie is 401. Checker locked, hash unchanged. It reads expected.json. Worker makes 200 the spec. PASS. Curl 200. Not a breakout. Success interface. File integrity green, admin open, you would have merged. Second box on the same folder is a new hoodie. PASS file is the smoke detector.}
\end{frame}

\begin{frame}{Close each door where it can actually refuse}
\begin{tabular}{@{}ll@{}}
\color{acid}Door & \color{acid}Layer from the abstract\\[8pt]
Inherited credentials & Short-lived credential projection\\[8pt]
Dangerous mounts & Capability-scoped filesystems\\[8pt]
Allowed endpoint & Real process isolation\\[8pt]
Verifier tampering & Tamper-evident execution history
\end{tabular}\par
\vspace{2mm}
\subline{History catches a rewrite. Freeze and grade off the worker so /admin stays 401.}
\evidence
\note{09:45 to 11:45. Say the four abstract locks out loud. History detects rewrite. It does not make /admin 401. Freeze bytes, grade off the worker, merge those bytes. Timeouts are not a win. Locked admin still merges. Open admin does not. Fake rulebook ignored. Swap denied. The agent can still do the job.}
\end{frame}

\begin{frame}{Four boundaries. Your deployment.}
\begin{tabular}{@{}ll@{}}
\color{acid}Door & \color{acid}Ask Monday\\[8pt]
Identity & Whose badge, how small, how brief?\\[8pt]
Files & What can it change, including tomorrow?\\[8pt]
Endpoint & Who gets the envelope, what is inside?\\[8pt]
Done & Who is allowed to say these bytes are true?
\end{tabular}\par
\vspace{2mm}
\subline{Refuse outside the worker. Then prove the real ticket still completes.}
\note{11:45 to 13:20. You leave with four boundaries. They are the four doors. Check them on your agent, not my lab. If done is a file the worker can write, or a spec it can edit, or a harness on the same disk, that is homework graded by the intern. Name a refuse outside the worker. Prove the house still works.}
\end{frame}

\begin{frame}[plain]
\kicker{THE WALL HELD. THE DOORS DID NOT.}
\vspace{5mm}
\bigline{Who gave this process\\the authority?}
\vspace{7mm}
{\color{acid}\ttfamily github.com/zozo123/nyc-talk\par}
\subline{Four doors. Four locks. Try them on your box.}
\note{13:20 to 14:00. The sandbox did its job. The agent did too: it used the doors we installed so it could finish. That is an escape without escaping. Don't ask if the wall is pretty. Ask who gave this process the authority. Stop. Smile.}
\end{frame}

\begin{frame}[fragile]{Appendix / Steal the repo}
\begin{lstlisting}
make factory     # freeze, judge, gate
make demo        # Linux isolation lab
make deck        # this PDF
\end{lstlisting}
\subline{\evidencecount.}
\note{Appendix only. Factory is door four, /admin. Lab is doors one to three. Isolated lab fails closed. No live terminal in the talk.}
\end{frame}

\begin{frame}{Appendix / What this is not}
Not a vendor 0-day. Not a kernel talk. Not last decade's least privilege with extra slides.

Agents finish tickets. The four doors are the job. That is why the title works.

\subline{Twenty-nine checks are not twenty-nine CVEs.}
\note{Appendix only. Goal-obsessed is why the doors matter in 2026. Not a model success-rate. History is a controller copy of the log.}
\end{frame}

\begin{frame}{Appendix / You are here}
\href{https://github.com/zozo123/nyc-talk}{github.com/zozo123/nyc-talk}
\subline{Script, slides, recorded results}
\href{https://zenity.io/resources/events/ai-agent-security-summit-nyc}{AI Agent Security Summit, NYC}
\note{Appendix only. Point at the repo if people linger. Do not present in the 15 minutes.}
\end{frame}
\end{document}
