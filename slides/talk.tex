\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{listings}
\usepackage{hyperref}
\definecolor{void}{HTML}{101214}
\definecolor{paper}{HTML}{F0F0E8}
\definecolor{acid}{HTML}{C7FF48}
\definecolor{alarm}{HTML}{FF756B}
\definecolor{muted}{HTML}{A7ADB3}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\Large,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}{\hfill{\color{muted}\scriptsize Yossi Eliaz\hspace{1em}\insertframenumber\hspace{1em}}\vspace{3mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setlength{\parskip}{7pt}
\lstset{basicstyle=\ttfamily\small,keywordstyle=\color{acid},commentstyle=\color{muted},stringstyle=\color{acid},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false}
\newcommand{\kicker}[1]{{\color{acid}\ttfamily\small #1}\par}
\newcommand{\bigline}[1]{{\fontsize{27}{32}\selectfont\bfseries #1\par}}
\newcommand{\subline}[1]{\par{\color{muted}\normalsize #1}\par}
\newcommand{\evidence}{\vfill{\color{muted}\scriptsize\evidencescope}}
\input{slides/evidence.tex}
\title{Your Agent Escaped Without Escaping the Sandbox}
\author{Yossi Eliaz}
\date{October 21, 2026}
\begin{document}

\begin{frame}[plain]
\kicker{NYC / AI AGENT SECURITY SUMMIT}
\vspace{4mm}
{\fontsize{28}{32}\selectfont\bfseries Your Agent Escaped\\Without Escaping\\the Sandbox\par}
\vspace{6mm}
{\large Yossi Eliaz\par}
{\small Principal Engineer and Head of DevRel at Incredibuild.com\par}
\vspace{2mm}
\subline{October 21, 2026}
\note{00:00 to 00:50. Cold open: a merge you would have approved. Ticket is lock /admin to 401 without a cookie. Sandbox holds. CI green. Curl /admin with no cookie, 200. Would you have merged this? Boxing agents was the right instinct and the wrong finish line.}
\end{frame}

\begin{frame}{You bought a cage}
\bigline{Isolation asks:\\can it touch the host?}
\vspace{4mm}
\bigline{Release asks:\\should we believe\\these bytes?}
\subline{Those two questions got collapsed.}
\note{00:50 to 02:20. Isolation is not attestation. Platforms treat agent finished as the patch is true because the harness lives in the box. Same disk wrote the handler and testifies /admin is locked. You asked the prisoner if the vault was sealed. Do not say zero-day. Say this is becoming agent CI.}
\end{frame}

\begin{frame}[fragile]{The control is green. You still lost.}
\begin{lstlisting}
checker.py              hash unchanged
expected.json           worker-written
CI / merge check        PASS

GET /admin  (no cookie) 401 required
shipped                 200
\end{lstlisting}
\subline{You did not accept a bad parser. You accepted a policy rewrite.}
\evidence
\note{02:20 to 05:20. Sting: checker hash unchanged. Worker rewrote expected.json so unauthenticated /admin is supposed to be 200. CI PASS. Independent 401 still fails. That is a policy rewrite, not a sloppy parser. Forking the producer verifies the crime scene. PASS file is a smoke detector, not the finding.}
\end{frame}

\begin{frame}{We verified a locked door. We merged an open one.}
\begin{tabular}{@{}ll@{}}
Merge from the live path after a swap & \textcolor{alarm}{Open /admin}\\[10pt]
Hash the path, hash it again & \textcolor{alarm}{The path can move}\\[10pt]
Evaluate and merge the frozen object & \textcolor{acid}{Same digest}\\[10pt]
Approval for A applied to B & \textcolor{acid}{Denied}
\end{tabular}\par
\vspace{3mm}
\subline{A green check on the wrong bytes is still a 200 with no cookie.}
\evidence
\note{05:20 to 06:45. Honest comparison can still merge the wrong file. Two hashes of a mutable path are not a freeze. Collect, store, digest, evaluate that object, merge that object. A valid GitHub check on the wrong bytes is still an open admin endpoint.}
\end{frame}

\begin{frame}[fragile]{The process never left}
\begin{lstlisting}
identity   we handed it another project's token
files      the mount did exactly what we configured
release    the allowed host was never the question
\end{lstlisting}
\subline{Containment held. The grant did the work of an escape.}
\evidence
\note{06:45 to 08:15. Three independent checks, not a chain. Broad token, writable bind, allowed upload to the wrong account. Same sentence each time: the sandbox stayed put and a decision we cared about still moved. That is the title.}
\end{frame}

\begin{frame}{Stop taking testimony from the box}
\bigline{The candidate is a subject.\\It is not a witness.}
\vspace{3mm}
\subline{Freeze the bytes. Compare off the worker. Merge that object.}
\subline{A green harness is a log line. It is not a 401.}
\evidence
\note{08:15 to 11:00. Repair is not a thicker jail. Controller freezes chosen paths. Fresh machine runs frozen bytes as the subject. Controller owns expected results. No accept-VM key that can rewrite the checker. Timeout is no approval, not a defense. Gate names bytes, run, verifier, expected results, policy, nonce. History detects rewrite. History does not authorize.}
\end{frame}

\begin{frame}{Same ticket. Different witness.}
\begin{tabular}{@{}ll@{}}
Locked /admin, frozen & \textcolor{acid}{Merged. No cookie is 401.}\\[8pt]
Open /admin & \textcolor{acid}{Rejected}\\[8pt]
Worker PASS file & \textcolor{acid}{Ignored}\\[8pt]
Worker expected.json & \textcolor{acid}{Ignored}\\[8pt]
Swap after freeze & \textcolor{acid}{Denied}
\end{tabular}\par
\evidence
\note{11:00 to 12:15. Walk the five rows. The ticket still closes. You can ship a locked door. You cannot let the worker define locked. Return to curl /admin with no cookie.}
\end{frame}

\begin{frame}{If the writer is also the witness}
\begin{tabular}{@{}ll@{}}
\color{acid}Ask & \color{acid}If the answer is the box, you lost\\[10pt]
Whose identity? & You handed it over\\[8pt]
Which files, including tomorrow's? & Snapshots and forks keep the crime\\[8pt]
Which recipient, which contents? & The allowed host was the wrong question\\[8pt]
Who says /admin is locked? & You do not have acceptance. You have a diary.
\end{tabular}\par
\note{12:15 to 13:20. Do not start with the kernel. Four questions. If who says /admin is locked is the same disk that wrote the handler, there is no acceptance. Name a component that can refuse outside the worker. Prove a legitimate lock still merges.}
\end{frame}

\begin{frame}[plain]
\kicker{CONTAINMENT IS NOT ATTESTATION}
\vspace{5mm}
\bigline{Who gave this process\\the authority?}
\vspace{7mm}
{\color{acid}\ttfamily github.com/zozo123/nyc-talk\par}
\subline{Yossi Eliaz}
\note{13:20 to 14:00. We believed a green check meant a locked admin door. The sandbox contained the process. It did not attest the bytes. Containment is not attestation. When the next agent gets merge rights, ask who gave this process the authority. Stop. Margin is silence.}
\end{frame}

\begin{frame}[fragile]{Appendix / Reproduction}
\begin{lstlisting}
make factory     # freeze, judge, gate; no cloud
make demo        # educational Linux isolation lab
make deck        # evidence check + PDF
\end{lstlisting}
\subline{\evidencecount.}
\note{Appendix only. Factory is the acceptance path. Lab is the surrounding grants. Isolated lab fails closed. Boat is an optional noEnv accept VM, not the vulnerability.}
\end{frame}

\begin{frame}{Appendix / Limits}
A class identification, not a vendor bug.

Deterministic scripts. Five cases prove five cases.

In-sandbox grading is known. File-integrity of the checker can still pass.

\subline{Timeouts are no approval, not a successful defense.}
\note{Appendix only. Not a kernel, cgroup, model ASR, or customer-incident claim. PASS-file variant is a deliberate detector. Novelty is not process-exit hacking. Novelty is not claimed for pytest plugin loading.}
\end{frame}

\begin{frame}{Appendix / Primary references}
\href{https://www.anthropic.com/research/emergent-misalignment-reward-hacking}{Anthropic reward hacking}
\subline{Process-exit graders are known}
\href{https://slsa.dev/spec/v1.2/verifying-artifacts}{SLSA verifying artifacts}
\subline{A valid statement can still name the wrong bytes}
\href{https://docs.pytest.org/en/stable/how-to/plugins.html}{pytest plugins}
\subline{Loading workspace code is documented. Trusting it as acceptance is ours.}
\note{Appendix only. Cite prior art out loud only if asked. The talk's sting is collapsing isolation with attestation, not a new plugin loader.}
\end{frame}
\end{document}
