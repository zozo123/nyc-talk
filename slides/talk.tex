\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{listings}
\usepackage{hyperref}
\definecolor{void}{HTML}{101214}
\definecolor{paper}{HTML}{F0F0E8}
\definecolor{acid}{HTML}{C7FF48}
\definecolor{alarm}{HTML}{FF756B}
\definecolor{muted}{HTML}{A7ADB3}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\Large,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\setbeamertemplate{footline}{\hfill{\color{muted}\scriptsize Yossi Eliaz\hspace{1em}\insertframenumber\hspace{1em}}\vspace{3mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setlength{\parskip}{7pt}
\lstset{basicstyle=\ttfamily\small,keywordstyle=\color{acid},commentstyle=\color{muted},stringstyle=\color{acid},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false}
\newcommand{\kicker}[1]{{\color{acid}\ttfamily\small #1}\par}
\newcommand{\bigline}[1]{{\fontsize{27}{32}\selectfont\bfseries #1\par}}
\newcommand{\subline}[1]{\par{\color{muted}\normalsize #1}\par}
\newcommand{\evidence}{\vfill{\color{muted}\scriptsize\evidencescope}}
\input{slides/evidence.tex}
\title{Your Agent Escaped Without Escaping the Sandbox}
\author{Yossi Eliaz}
\date{October 21, 2026}
\begin{document}

\begin{frame}[plain]
\kicker{NYC / AI AGENT SECURITY SUMMIT}
\vspace{4mm}
{\fontsize{28}{32}\selectfont\bfseries Your Agent Escaped\\Without Escaping\\the Sandbox\par}
\vspace{6mm}
{\large Yossi Eliaz\par}
{\small Principal Engineer and Head of DevRel at Incredibuild.com\par}
\vspace{2mm}
\subline{October 21, 2026}
\note{00:00 to 00:30. Hi, I'm Yossi. Spoiler: the agent never climbed the wall. Namespaces fine, cgroups fine, still a bad day. Four short stories. We hold the door.}
\end{frame}

\begin{frame}{Nobody climbed the wall}
\bigline{Four non-escape escapes.}
\vspace{4mm}
\textcolor{acid}{Badge}\hspace{3mm}
\textcolor{acid}{Chair}\hspace{3mm}
\textcolor{acid}{Mailbox}\hspace{3mm}
\textcolor{acid}{Rulebook}
\vspace{3mm}
\subline{The process stays put. A decision you care about still moves.}
\note{00:30 to 01:15. Sandbox is cute and necessary, not the end of the movie. Fat badge, writable folder, mailroom stamp, pencil that grades homework: mess without leaving. Four stories, four locks, four questions, reception.}
\end{frame}

\begin{frame}[fragile]{01 / It had dad's badge}
\begin{lstlisting}
GET /projects/other/private

runner token     200
task token       403
its own input    200
\end{lstlisting}
\subline{We hung the key around its neck. Mint a tiny one.}
\evidence
\note{01:15 to 03:15. Inherited credentials. Job needed a token, CI already had a fat one. Other project returns 200. Sandbox did not steal it. Fix: this task, this audience, short life. Same request 403. Own input still 200. Short life is not small scope.}
\end{frame}

\begin{frame}[fragile]{02 / It sat in the next job's chair}
\begin{lstlisting}
worker writes /shared/next-job.json

writable bind   host file changes
read-only bind  write denied
/output         still works
\end{lstlisting}
\subline{The mount worked. That was the bug.}
\evidence
\note{03:15 to 05:00. Dangerous mounts. Shared folder, writable because why not. Ordinary write, next job's file changes. Fix: read this, write that. Same write denied. Output folder still works. If the next job shouldn't be in the room, don't put the chair in the room.}
\end{frame}

\begin{frame}[fragile]{03 / The allowed door}
\begin{lstlisting}
same host, wrong mailbox

team/reports     intended
other/uploads    gets the private fixture
\end{lstlisting}
\subline{Don't let it address the envelope.}
\evidence
\note{05:00 to 07:00. Exfil through an allowed endpoint. Report service is allowed. It mails the private fixture to another account on the same host. Firewall liked the hostname. Who and what were the question. Broker builds a boring postcard. Extra fields no. Direct route gone. Real report still arrives.}
\end{frame}

\begin{frame}[fragile]{04 / The test said yes}
\begin{lstlisting}
checker.py              hash unchanged
expected.json           worker-written
CI / merge              PASS

GET /admin  (no cookie) 401 required
shipped                 200
\end{lstlisting}
\subline{We protected the referee. The player wrote the rulebook.}
\evidence
\note{07:00 to 09:30. Fun one. Ticket: /admin without cookie is 401. Checker locked, hash unchanged. It still reads expected.json. Worker makes 200 the spec. PASS. Curl is 200. Policy rewrite, then a merge you'd approve. Second box that reads the same folder is a new hoodie. PASS file is the smoke detector.}
\end{frame}

\begin{frame}{Four locks}
\begin{tabular}{@{}ll@{}}
\color{acid}Story & \color{acid}Who can actually say no\\[8pt]
Dad's badge & Tiny, short-lived task token\\[8pt]
Next job's chair & Read this, write that\\[8pt]
Allowed door & Broker + no direct route\\[8pt]
The test said yes & Freeze bytes. Grade off the worker.
\end{tabular}\par
\vspace{2mm}
\subline{A diary can still say the vault is fine. Keep a copy it cannot rewrite.}
\evidence
\note{09:30 to 11:30. Hit the four abstract locks: credential projection, scoped filesystems, process isolation, tamper-evident history. History detects a rewrite. It does not make /admin 401. Locked door still merges. Open door does not. PASS file ignored. Fake rulebook ignored. Swap denied.}
\end{frame}

\begin{frame}{Steal these four}
\begin{tabular}{@{}ll@{}}
\color{acid}Ask Monday & \color{acid}If the answer is the box...\\[8pt]
Whose identity? & You handed dad's badge over\\[8pt]
Which files, including tomorrow? & The next job's chair is still there\\[8pt]
Which mailbox, which letter? & The allowed host was the wrong question\\[8pt]
Who says /admin is locked? & That's a diary, not a verifier
\end{tabular}\par
\vspace{2mm}
\subline{Name a no outside the worker. Then prove the real job still works.}
\note{11:30 to 13:15. One agent job, four sticky notes. If who says /admin is locked is the same disk that wrote the handler, you have a confident diary. Name the thing that can refuse outside the worker. Prove the legitimate lock still merges.}
\end{frame}

\begin{frame}[plain]
\kicker{THE PROCESS NEVER LEFT}
\vspace{5mm}
\bigline{Who gave this process\\the authority?}
\vspace{7mm}
{\color{acid}\ttfamily github.com/zozo123/nyc-talk\par}
\subline{Then go enjoy New York.}
\note{13:15 to 14:00. Sandbox contained the process. We issued the badge, the chair, the stamp, and the rulebook. That's the talk you came for. Ask who gave this process the authority. Repo on the slide. Stop. Smile. No tenth slide.}
\end{frame}

\begin{frame}[fragile]{Appendix / Steal the repo}
\begin{lstlisting}
make factory     # freeze, judge, gate
make demo        # Linux isolation lab
make deck        # this PDF
\end{lstlisting}
\subline{\evidencecount.}
\note{Appendix only. Factory is the /admin path. Lab is badge, chair, mailbox. Isolated lab fails closed. No live terminal in the talk.}
\end{frame}

\begin{frame}{Appendix / What we are not claiming}
This is a friendly lab, not a vendor 0-day.

Deterministic scripts. Synthetic /admin. Finite cases.

\subline{Twenty-nine checks are not twenty-nine CVEs.}
\note{Appendix only. If asked: not a customer incident, not model success rates, not kernel escape. History is a controller copy of the log, not a blockchain product.}
\end{frame}

\begin{frame}{Appendix / If someone wants homework}
\href{https://github.com/zozo123/nyc-talk}{github.com/zozo123/nyc-talk}
\subline{Script, slides, recorded results}
\href{https://zenity.io/resources/events/ai-agent-security-summit-nyc}{AI Agent Security Summit, NYC}
\subline{You are here}
\note{Appendix only. Point at the repo if people linger. Do not present these in the 15 minutes.}
\end{frame}
\end{document}
