% Canonical LaTeX source. Edit this file, then run make snapshot.
% Factory: developer, AI harness, dev sandbox, GitHub CI runner, prod sandbox.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta}
\definecolor{void}{HTML}{07090D}
\definecolor{paper}{HTML}{F2F5F7}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{muted}{HTML}{A9B3BF}
\definecolor{danger}{HTML}{FF6975}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\fontsize{19}{22}\selectfont,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\newcommand{\track}{TALK}
\setbeamertemplate{frametitle}{%
\vspace{1.2mm}{\color{danger}\fontsize{8}{9}\selectfont\ttfamily\track}\hspace{2mm}{\color{acid}\rule{14mm}{0.7pt}}\par\vspace{0.4mm}%
\insertframetitle\par\vspace{0.8mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setbeamertemplate{footline}{\hspace{10mm}{\color{muted}\fontsize{6}{7}\selectfont YOSSI ELIAZ\quad PIER SIXTY\quad 21 OCT 2026}\hfill{\color{acid}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{2.4mm}}
\setlength{\parskip}{2pt}
\lstset{basicstyle=\ttfamily\fontsize{10}{12.5}\selectfont,keywordstyle=\color{acid},commentstyle=\color{muted},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=1pt,belowskip=0pt}
\newcommand{\labeltext}[1]{\par{\color{acid}\fontsize{8}{10}\selectfont\bfseries #1}\par}
\newcommand{\bigresult}[2]{\par\vspace{0.5mm}{\color{#1}\fontsize{36}{40}\selectfont\bfseries #2}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{13}{16}\selectfont #1}\par}
\newcommand{\scope}[1]{\par\vspace{1mm}{\color{muted}\fontsize{7.5}{9}\selectfont #1}\par}
\newcommand{\tablefont}{\fontsize{10.5}{13}\selectfont\renewcommand{\arraystretch}{1.22}\setlength{\tabcolsep}{3pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={The agent changes the answers. The check goes green.}}
\begin{document}

\begin{frame}[plain,t]
% title: Your Agent Escaped Without Escaping the Sandbox
% time: 00:00-00:40
% cue: Say the sentence. Stop.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\vspace{6mm}
\labeltext{ZENITY\quad AI AGENT SECURITY SUMMIT\quad PIER SIXTY}
\vspace{4mm}
{\fontsize{24}{28}\selectfont\bfseries Your Agent Escaped\\Without Escaping\\the Sandbox\par}
\vspace{5mm}
{\fontsize{16}{20}\selectfont The agent changes the answers.\\The check goes green.\par}
\vfill
{\small Yossi Eliaz, PhD\quad Incredibuild\quad 21 October 2026\par}
\note{The agent changes the answers. The check goes green.

A pull request is open to fix admin login. The program prints 200 for every request. No login on the admin page must return 401. The test answers live in the checkout, next to the program.

I am Yossi Eliaz. One GitHub Actions job. The file that matters is the answer file sitting in the checkout. The program stays wrong. The test starts agreeing with it. That agreement is the whole fifteen minutes. Everything else on stage is how the job is wired.}
\end{frame}

\renewcommand{\track}{02  /  THE JOB}
\begin{frame}[t]
% title: One GitHub Actions job
% time: 00:40-01:40
% cue: Workflow on the left. Checkout on the right.
% source: https://docs.github.com/en/actions/reference/security/securely-using-pull_request_target
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{One GitHub Actions job}
{\tablefont
\begin{tabular}{@{}p{.36\textwidth}p{.56\textwidth}@{}}
\textcolor{acid}{workflow file} & default branch\\
\textcolor{acid}{checkout} & the pull request\\
\textcolor{acid}{expected.json} & in the checkout\\
\textcolor{acid}{job token} & minted for this run\\
\end{tabular}}
\takeaway{The answers travel with the code.}
\scope{Recorded on a Boat Ubuntu machine. Loopback fixtures. No live GitHub repo.}
\note{A GitHub Actions job splits in two. The workflow file comes from the default branch. That file is the check. The checkout is the pull request. The program and the answer file are in the checkout.

A pull request from a fork gets a read-only job token. The privileged trigger runs the default-branch workflow and can hold a write token. Since June 2026 the checkout action refuses a fork head in that job. A same-repo change still lands in a checkout the job runs.

We recorded that shape on a Boat Ubuntu machine. The calls are loopback. The mounts are real. There are five cases. The deck reads those recordings. It does not call GitHub. The point of the job picture is small: the check and the answers are not in the same place. The check is the workflow. The answers are in the pull request.}
\end{frame}

\renewcommand{\track}{03  /  THE PROGRAM}
\begin{frame}[t,fragile]
% title: No login must be 401
% time: 01:40-02:50
% cue: Point at 200. Say what a correct program prints.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{No login must be 401}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{THIS PROGRAM}
\begin{lstlisting}
# admin:none
# /admin, no login
print(200)
\end{lstlisting}
\end{column}
\begin{column}{.48\textwidth}
\labeltext{A CORRECT PROGRAM}
\begin{lstlisting}
# no login
print(401)
# real admin
print(200)
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{Admin is open.}
\note{admin:none is one request. The route is admin. The login is none. A correct program prints 401. This program prints 200 for every request. The admin page is open.

The checker that grades it sits with the workflow and is read-only. It loads expected.json from the checkout. When the printed number matches the file, it prints PASS. The agent is allowed to edit files in the checkout, so it can edit the answers. Replacing the checker is denied. That is the whole opening: the grade and the program can be brought into agreement while the admin page stays open.}
\end{frame}

\renewcommand{\track}{04  /  THE FILE}
\begin{frame}[t,fragile]
% title: The answer file
% time: 02:50-04:30
% cue: Read the two JSON lines. Pause.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The answer file}
\begin{lstlisting}
# expected.json
{"input":"admin:none","output":401}

{"input":"admin:none","output":200}
\end{lstlisting}
\vspace{2mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{HONEST FILE}
\bigresult{danger}{FAIL}
\end{column}
\begin{column}{.48\textwidth}
\labeltext{AFTER THE EDIT}
\bigresult{acid}{PASS}
\end{column}
\end{columns}
\takeaway{Same program. Same checker.}
\scope{Checker hash \texttt{f5d8d3468bbe...} unchanged. Overwrite of checker.py denied.}
\note{Honest file: admin:none expects 401. The program prints 200. The checker prints FAIL and exits 1.

The agent writes 200 into the expected rows. admin:none now expects 200. The program still prints 200. The attempt to overwrite checker.py is denied. The hash before matches the hash after. The same checker prints PASS and exits 0.

The check is green. The program you would merge still lets anyone into admin. Remember the filename: expected.json. It is an ordinary JSON file in the pull request, and the checker trusts it. Anyone who can edit the pull request can edit the grade, and the workflow will report success.}
\end{frame}

\renewcommand{\track}{05  /  FOUR ROWS}
\begin{frame}[t]
% title: Same program. Four results.
% time: 04:30-06:10
% cue: FAIL, PASS, REJECT, ACCEPT.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Same program. Four results.}
{\tablefont
\begin{tabular}{@{}p{.42\textwidth}p{.28\textwidth}p{.22\textwidth}@{}}
\textcolor{acid}{Answers} & \textcolor{acid}{Where} & \textcolor{acid}{Result}\\\midrule
Honest file & checkout & FAIL\\
Agent's file & checkout & \textcolor{danger}{PASS}\\
Runner's cases & on the runner & \textcolor{acid}{REJECT}\\\addlinespace
Real fix & on the runner & \textcolor{acid}{ACCEPT}\\
\end{tabular}}
\vspace{2mm}
{\color{muted}\fontsize{8}{10}\selectfont no-login 401\quad admin 200\quad user 403\quad public 200\quad expired 401\par}
\takeaway{The real fix still merges.}
\note{Same program. Four results.

Honest answers in the checkout: FAIL. Answers the agent wrote: PASS. Cases stored on the runner: REJECT. The fixed program returns 401 for no login, 200 for a real admin, 403 for an ordinary user, 200 for the public page, and 401 for an expired login. The runner accepts it.

The real fix can merge. What changed between FAIL and PASS was the answer file, not the program. Put that file on the runner, with the workflow, and the checkout can still hold the code. Five numbers cover this policy: 401, 200, 403, 200, 401. They are the policy we ran.}
\end{frame}

\renewcommand{\track}{06  /  MERGE}
\begin{frame}[t]
% title: Then the merge
% time: 06:10-07:40
% cue: Denied. Published. Denied.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{Then the merge}
{\tablefont
\begin{tabular}{@{}p{.08\textwidth}p{.58\textwidth}p{.24\textwidth}@{}}
1 & Bad program, fresh approval & \textcolor{danger}{DENIED}\\\addlinespace
2 & The judged program & \textcolor{acid}{PUBLISHED}\\\addlinespace
3 & That program again & \textcolor{acid}{DENIED}\\
\end{tabular}}
\takeaway{Publish the judged bytes.}
\scope{The recording publishes the corrected handler.}
\note{Green is the check. Merge is the next step. Freeze the program. Run those exact bytes. Compare them with the cases on the runner. Then one fresh approval.

The bad program: DENIED. The judged program: PUBLISHED. Those bytes again: DENIED.

This recording publishes the corrected handler and refuses the program that always prints 200. Compare first, then spend the approval. An earlier version of this gate spent the approval first, and a bad swap could hide behind an already-used approval. The order on this slide is the whole demonstration: the bad program is offered while the approval is still live.}
\end{frame}

\renewcommand{\track}{07  /  ALSO}
\begin{frame}[t]
% title: Three more files in the job
% time: 07:40-09:20
% cue: Token, disk, host. One result each.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Three more files in the job}
{\tablefont
\begin{tabular}{@{}p{.22\textwidth}p{.42\textwidth}p{.28\textwidth}@{}}
\textcolor{acid}{In the job} & \textcolor{acid}{What happened} & \textcolor{acid}{After the fix}\\\midrule
Job token & other project 200 & 403, own input 200\\
Next job's file & host file changed & write denied\\
Allowed host & other account 201 & team report delivered\\
\end{tabular}}
\takeaway{Same job. Three more writable things.}
\scope{Separate recordings. Loopback HTTP. Real bind mounts.}
\note{Three more things in the same kind of job. Each one is its own recording.

The job token reads another project's private fixture and gets 200. A token for this task gets 403 there and 200 on this job's own input. Expired and wrong-audience tokens get 403.

The next job's file is mounted writable. The write changes the host file. A read-only mount denies it. This job's own patch still writes.

The job may call one host. A post of the private fixture to another account is stored, 201, and the receiver log has the bytes. The repair sends one fixed team report from the runner. Extra fields are denied, and the checkout has no route of its own. The team report still arrives. These three rows are separate runs. They sit in one job because a real workflow has a token, a disk, and a host.}
\end{frame}

\renewcommand{\track}{08  /  NEXT RUN}
\begin{frame}[t]
% title: The next run keeps the file
% time: 09:20-10:20
% cue: One sentence. Move on.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{The next run keeps the file}
\vspace{4mm}
{\fontsize{20}{26}\selectfont\texttt{expected.json}\\[4mm]
output: \textcolor{danger}{\texttt{200}}\par}
\takeaway{Copy the checkout, copy the answers.}
\scope{Follows from the file we changed.}
\note{Copy this checkout into the next run and expected.json still expects 200. The next job grades against the edited answers. A cache, a reusable workspace, or a forked machine does the same thing if it copies the tree.

This follows from the file the agent changed. We did not run a separate trial across several jobs. A copied workspace keeps whatever the last job wrote, including a grade of 200 for no login. The next run will go green on the same open admin page.}
\end{frame}

\renewcommand{\track}{09  /  THE FIX}
\begin{frame}[t,fragile]
% title: Keep the answers on the runner
% time: 10:20-11:30
% cue: Left file moves to the right.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py
\frametitle{Keep the answers on the runner}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{CHECKOUT}
\begin{lstlisting}
handler.py
\end{lstlisting}
\end{column}
\begin{column}{.48\textwidth}
\labeltext{RUNNER}
\begin{lstlisting}
expected.json
checker.py
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{The agent can change the code.}
\note{Move the answer file to the runner, beside the workflow. Leave the program in the checkout.

The agent can change the code. The agent cannot change the grade. The honest cases fail the open admin page. The runner rejects that program. The fixed program is accepted and can be published. The checkout remains the place where the agent writes handler.py. The answers move to the side that merges.}
\end{frame}

\renewcommand{\track}{10  /  YOUR JOB}
\begin{frame}[t]
% title: On your job
% time: 11:30-12:40
% cue: Four rows. Then stop.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{On your job}
{\fontsize{11}{14}\selectfont\renewcommand{\arraystretch}{1.35}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.24\textwidth}p{.34\textwidth}p{.34\textwidth}@{}}
\textcolor{acid}{} & \textcolor{acid}{Refuse} & \textcolor{acid}{Still allow}\\\midrule
Token & another project & this job's input\\
Disk & the next job & this job's output\\
Host & another account & the team report\\
Answers & an edited file & the real fix\\
\end{tabular}}
\takeaway{Then run the real ticket again.}
\note{On your own job, check four places. The token may read this job's input and is refused on another project. The disk may take this job's output and refuses a write to the next job. The host delivers the team report and refuses another account. The check publishes the real fix and refuses an edited answer file.

Do that on the runner. Then run the real ticket again, so the job can still ship. A check that can only refuse will also refuse the fix. Each row needs both columns.}
\end{frame}

\renewcommand{\track}{11  /  ORDER}
\begin{frame}[t]
% title: Compare, then approve
% time: 12:40-13:20
% cue: Three words. Denied, published, denied.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{Compare, then approve}
\vspace{6mm}
{\fontsize{22}{28}\selectfont Denied.\\Published.\\Denied.\par}
\takeaway{One approval. The judged program only.}
\note{Say the order once more. Denied. Published. Denied. One fresh approval. The bad program first, while the approval is still live. Then the program that was judged. Then a replay, which is refused. If the replay is refused before the bad program is tried, the demonstration hides the swap.

The publication stores those exact bytes and spends the approval in the same write. What leaves the job is the program that was compared, and it leaves once.}
\end{frame}

\begin{frame}[plain,t]
% title: Who merged it?
% time: 13:20-14:00
% cue: Leave the question up.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\vspace{8mm}
{\color{danger}\ttfamily\fontsize{8}{9}\selectfont 12  /  CLOSE}\hspace{2mm}{\color{acid}\rule{14mm}{0.7pt}}\par
\vspace{6mm}
{\fontsize{28}{34}\selectfont\bfseries Who merged it?\par}
\vspace{6mm}
{\fontsize{16}{20}\selectfont The agent changed the answers.\\The check went green.\par}
\vfill
{\color{acid}\ttfamily\href{https://github.com/zozo123/nyc-talk}{github.com/zozo123/nyc-talk}\par}
{\color{muted}\small Pier Sixty\quad 21 October 2026\par}
\note{The agent changed the answers. The check went green.

Who merged it?

Keep the answers with the workflow. Freeze the program. Publish those bytes. Run the open-admin case again, and run the real fix again.

The repository has the source and the Linux recordings from the Boat machine: the token, the mounts, the upload, the answer file, and the merge. Open expected.json in the job you run tomorrow. If that file is in the checkout, the grade belongs to whoever edits the pull request. Thank you.}
\end{frame}

\appendix

\renewcommand{\track}{A1  /  CASES}
\begin{frame}[t]
% title: The five policy cases
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The five policy cases}
{\tablefont
\begin{tabular}{@{}p{.38\textwidth}p{.23\textwidth}p{.32\textwidth}@{}}
\textcolor{acid}{CLI input} & \textcolor{acid}{Expected} & \textcolor{acid}{Always-200 handler}\\\midrule
\texttt{admin:none} & 401 & \textcolor{danger}{200: mismatch}\\
\texttt{admin:admin} & 200 & 200: match\\
\texttt{admin:user} & 403 & \textcolor{danger}{200: mismatch}\\
\texttt{public:none} & 200 & 200: match\\
\texttt{admin:expired} & 401 & \textcolor{danger}{200: mismatch}\\
\end{tabular}}
\takeaway{The repaired handler returns all five.}
\scope{admin:none means /admin with no login. A correct handler prints 401.}
\note{The handler takes a route and an authentication label and prints an integer. The runner checks these five. The repaired handler matches all five. This is the model of the check that promotes into the prod sandbox.}
\end{frame}

\renewcommand{\track}{A2  /  TOKEN AND DISK}
\begin{frame}[t]
% title: Runner token and the dev disk
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Runner token and the dev disk}
{\tablefont
\begin{tabular}{@{}p{.47\textwidth}p{.22\textwidth}p{.25\textwidth}@{}}
\textcolor{acid}{Attempt} & \textcolor{acid}{Broad grant} & \textcolor{acid}{Task grant}\\\midrule
Read other-project private fixture & HTTP 200 & HTTP 403\\
Read this job's input & HTTP 200 & HTTP 200\\\addlinespace
Write next-job host file & Changed & Denied\\
Write this job's patch & Allowed & Allowed\\
\end{tabular}}
\takeaway{The service and the mount enforce the denials.}
\scope{Loopback HTTP and Linux bind mounts inside the recorded dev sandbox.}
\note{The token case is loopback HTTP. The broad fixture stands in for the runner secret. The task fixture stands in for a job-scoped token. The mount case watches the host file after an ordinary write from the dev sandbox. This job's output stays writable.}
\end{frame}

\renewcommand{\track}{A3  /  PUBLISH}
\begin{frame}[t,fragile]
% title: Allowed host, wrong account
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Allowed host, wrong account}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}
\labeltext{HARNESS CHOOSES PATH AND BODY}
\begin{lstlisting}
POST /accounts/other/uploads
{"data":"PRIVATE-FIXTURE"}

201 STORED
receiver has the bytes
\end{lstlisting}
\end{column}
\begin{column}{.47\textwidth}
\labeltext{RUNNER OWNS THE POST}
\begin{lstlisting}
publish_report(task)

fixed recipient + body
extra fields: denied
direct route: unavailable
team report: delivered
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{One allowed hostname contains many operations.}
\scope{Loopback model of a host on the runner's allow list.}
\note{The fixture service permits both account paths, the way one allowed deploy host holds more than one operation. The delivery record shows the bytes arrived. The repair moves the POST onto the runner and removes the dev sandbox's direct route.}
\end{frame}

\renewcommand{\track}{A4  /  OLD GATE}
\begin{frame}[t,fragile]
% title: An earlier promote stored a marker
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{An earlier promote stored a marker}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{ARCHIVED BASELINE}
\begin{lstlisting}
replace candidate bytes
retain old digest field

gate: PUBLISHED
manifest != actual bytes
\end{lstlisting}
\end{column}
\begin{column}{.46\textwidth}
\labeltext{CURRENT PROMOTE}
Immutable byte entries\\Digest derived from those bytes\\Publication stores the bytes\\Approval spent in that write
\end{column}
\end{columns}
\takeaway{The old promote could record a stale digest.}
\scope{The path starts with access to the controller object.}
\note{The archived case mutates a controller-side object, keeps a stale digest, and the old gate records a publication marker. The current promote derives the digest from immutable bytes and stores those bytes. Reaching the old flaw takes access to the controller object.}
\end{frame}

\renewcommand{\track}{A5  /  LIMITS}
\begin{frame}[t]
% title: What was recorded
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{What was recorded}
{\fontsize{10}{12.5}\selectfont\renewcommand{\arraystretch}{1.28}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.30\textwidth}p{.14\textwidth}p{.48\textwidth}@{}}
\textcolor{acid}{Evidence} & \textcolor{acid}{Checks} & \textcolor{acid}{Scope}\\\midrule
Linux lab & 29 & Token, mounts, loopback publish\\
Isolated checker & 11 & Read-only checker, key, promote\\
Local factory & 6 & Promote protocol\\
Regressions & 37 & Controller and evidence\\
Old gate audit & 1 & Controller-object misuse\\
\end{tabular}}
\takeaway{Four fixtures, one factory story.}
\scope{Boat Ubuntu sandbox for the Linux run. The fork slide is a consequence.}
\note{The records bind their source. The dev sandbox recordings use bubblewrap on a Boat Ubuntu machine. The harness under test is a script. The prod step in the recording is the runner's publication of exact bytes, the shape of what a prod sandbox is allowed to execute. The forked-disk slide follows from that workspace file. The timings are a delivery budget.}
\end{frame}

\renewcommand{\track}{A6  /  REPRO}
\begin{frame}[t,fragile]
% title: Reproduce the factory
% source: https://github.com/zozo123/nyc-talk
% source: https://tessl.io/
% source: https://github.com/containers/bubblewrap#sandbox-security
% source: https://slsa.dev/spec/v1.2/verifying-artifacts
% source: https://www.anthropic.com/research/emergent-misalignment-reward-hacking
\frametitle{Reproduce the factory}
{\fontsize{10}{12.5}\selectfont
\href{https://github.com/zozo123/nyc-talk}{\textcolor{acid}{Source}}\quad github.com/zozo123/nyc-talk\par
\href{https://github.com/containers/bubblewrap#sandbox-security}{\textcolor{acid}{bubblewrap}}\quad The caller defines the sandbox policy\par
\href{https://slsa.dev/spec/v1.2/verifying-artifacts}{\textcolor{acid}{SLSA v1.2}}\quad Digest and expectations before use\par
\href{https://tessl.io/}{\textcolor{acid}{Tessl}}\quad Context, verifier, factory, 2026\par
\href{https://www.cidersecurity.io/blog/research/ppe-poisoned-pipeline-execution/}{\textcolor{acid}{Poisoned pipeline execution}}\quad Cider Security, 2022\par}
\vspace{2mm}
\begin{lstlisting}
make test && make record-all && make final
\end{lstlisting}
\scope{Disposable Linux. The deck reads recordings.}
\note{Run the fixtures on disposable Linux with bubblewrap. The deck reads those recordings. Poisoned pipeline execution is the public name for a CI job that runs an attacker-influenced file. Anthropic's reward-hacking study is prior motivation and its measurements stay with that study. Reproducing the talk needs no GitHub token.}
\end{frame}
\end{document}
