% Canonical LaTeX source. Edit this file, then run make snapshot.
% Factory: developer, AI harness, dev sandbox, GitHub CI runner, prod sandbox.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta}
\definecolor{void}{HTML}{07090D}
\definecolor{paper}{HTML}{F2F5F7}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{muted}{HTML}{A9B3BF}
\definecolor{danger}{HTML}{FF6975}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\fontsize{19}{22}\selectfont,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\newcommand{\track}{TALK}
\setbeamertemplate{frametitle}{%
\vspace{1.2mm}{\color{danger}\fontsize{8}{9}\selectfont\ttfamily\track}\hspace{2mm}{\color{acid}\rule{14mm}{0.7pt}}\par\vspace{0.4mm}%
\insertframetitle\par\vspace{0.8mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setbeamertemplate{footline}{\hspace{10mm}{\color{muted}\fontsize{6}{7}\selectfont YOSSI ELIAZ\quad PIER SIXTY\quad 21 OCT 2026}\hfill{\color{acid}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{2.4mm}}
\setlength{\parskip}{2pt}
\lstset{basicstyle=\ttfamily\fontsize{10}{12.5}\selectfont,keywordstyle=\color{acid},commentstyle=\color{muted},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=1pt,belowskip=0pt}
\newcommand{\labeltext}[1]{\par{\color{acid}\fontsize{8}{10}\selectfont\bfseries #1}\par}
\newcommand{\bigresult}[2]{\par\vspace{0.5mm}{\color{#1}\fontsize{36}{40}\selectfont\bfseries #2}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{13}{16}\selectfont #1}\par}
\newcommand{\scope}[1]{\par\vspace{1mm}{\color{muted}\fontsize{7.5}{9}\selectfont #1}\par}
\newcommand{\tablefont}{\fontsize{10.5}{13}\selectfont\renewcommand{\arraystretch}{1.22}\setlength{\tabcolsep}{3pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={Dev sandbox, AI harness, GitHub CI runner, prod sandbox}}
\begin{document}

\begin{frame}[plain,t]
% title: Your Agent Escaped Without Escaping the Sandbox
% time: 00:00-00:35
% cue: Say the one line. Then the three statuses.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\vspace{2.5mm}
\labeltext{ZENITY\quad AI AGENT SECURITY SUMMIT\quad PIER SIXTY}
\vspace{1.5mm}
{\fontsize{22}{26}\selectfont\bfseries Your Agent Escaped\\Without Escaping\\the Sandbox\par}
\vspace{2mm}
{\fontsize{13}{16}\selectfont The harness edits the proof the verifier reads.\par}
\vspace{2mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.32\textwidth}
\labeltext{DEV SANDBOX}
{\color{acid}\fontsize{14}{17}\selectfont\bfseries writes\par}
\end{column}
\begin{column}{.32\textwidth}
\labeltext{VERIFIER}
{\color{acid}\fontsize{14}{17}\selectfont\bfseries PASS\par}
\end{column}
\begin{column}{.32\textwidth}
\labeltext{HANDLER}
{\color{danger}\fontsize{14}{17}\selectfont\bfseries 200\par}
\end{column}
\end{columns}
\vspace{1.5mm}
{\small Yossi Eliaz, PhD\par}
{\color{muted}\small Incredibuild\quad 21 October 2026\par}
\note{A factory starts with a written definition of correct. A harness works in a dev sandbox to satisfy it. A verifier reads that definition. The CI runner promotes on PASS. The prod sandbox runs the bytes.

In this recording the verifier reads expected.json from the workspace the harness writes. No login on admin must return 401. The program returns 200. The harness writes 200 into that file. The verifier prints PASS. A runner that promotes on PASS would ship that program. With the cases held on the runner, the gate refuses those bytes.

I am Yossi Eliaz. The line on the slide is the reason.}
\end{frame}

\renewcommand{\track}{02  /  FACTORY}
\begin{frame}[t]
% title: Today's factory
% time: 00:35-01:30
% cue: Read spec, harness, verifier, runner, prod. Then the line.
% source: https://tessl.io/
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{Today's factory}
{\tablefont
\begin{tabular}{@{}p{.28\textwidth}p{.64\textwidth}@{}}
\textcolor{acid}{Spec} & What correct means.\\
\textcolor{acid}{Harness} & Writes in the dev sandbox.\\
\textcolor{acid}{Verifier} & Reads \texttt{expected.json}.\\
\textcolor{acid}{CI runner} & Base workflow. PR checkout. PASS promotes.\\
\textcolor{acid}{Prod sandbox} & Runs those bytes.\\
\end{tabular}}
\takeaway{The harness edits the proof the verifier reads.}
\scope{Factory shape described by Tessl, 2026. The break is this recording.}
\note{This is the factory teams are running now. A spec says what correct means. A harness writes code in a dev sandbox to match it. A verifier checks the result. A CI runner promotes on PASS. A prod sandbox runs the published bytes.

Tessl describes that stack as context, then a loop with a verifier, then a factory. The verifier is there because a written skill is advice until something checks the output.

In our recording the verifier loads expected.json from the dev sandbox. The harness can write that file. The rest of the talk is that edit, then the three other inputs the runner also trusts: the token, the next job's disk, and the allowed host.}
\end{frame}

\renewcommand{\track}{03  /  THE BUG}
\begin{frame}[t,fragile]
% title: Two files. One of them ships.
% time: 01:30-02:20
% cue: Point at print 200. Then at expected.json.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{Two files. One of them ships.}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{handler.py GOES TO PROD}
\begin{lstlisting}
# admin:none = /admin, no login
route, auth = argv.split(":")
print(200)  # every request
# a correct app prints 401
\end{lstlisting}
\end{column}
\begin{column}{.48\textwidth}
\labeltext{checker.py STAYS ON THE RUNNER}
\begin{lstlisting}
key = json.load("expected.json")
out = run("handler.py", case)
if out == key[case]:
    print("PASS")
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{Checker locked. Answer key writable.}
\scope{admin:none means /admin, no login.}
\note{Two files. handler.py is what the prod sandbox will run. admin:none means /admin with no login. A correct app prints 401. This one prints 200 for every request. That is an open admin page.

checker.py is what the runner trusts, and it is read-only. It loads expected.json and prints PASS when the program matches that file. The harness cannot change the checker. It can change the answer key, because that file lives in the dev sandbox.

The next three slides are the other doors the factory left open: the runner token, the next job's disk, and the allowed publish host. Then we come back and flip the answer key.}
\end{frame}

\renewcommand{\track}{04  /  TOKEN}
\begin{frame}[t,fragile]
% title: Runner token. Inside the sandbox.
% time: 02:20-03:30
% cue: 200, then 403, then this job's input still works.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://docs.github.com/en/actions/reference/security/securely-using-pull_request_target
\frametitle{Runner token. Inside the sandbox.}
\begin{lstlisting}
Authorization: Bearer fixture-runner
GET /projects/other/private
200  {"fixture":"SYNTHETIC-PRIVATE-FIXTURE"}

Authorization: Bearer fixture-task
GET /projects/other/private     403
GET /input/parser-fix           200
\end{lstlisting}
\takeaway{Task token in. Runner secret stays out.}
\scope{Loopback stand-in for GITHUB\_TOKEN. Workflow file stays on the base branch.}
\note{GitHub mints a job token and the steps can see it. A pull request from a fork gets a read-only token. The privileged pull-request trigger runs the workflow from the default branch and can hold a write token and secrets. Since June 2026, the checkout action refuses to fetch a fork head in that privileged job. A same-repo agent still works inside a checkout the job chose to run.

The fixture is that broad token, placed in the dev sandbox so the harness can read the repo. A GET for another project's private fixture returns two hundred and the synthetic private fixture. The worker sends the token the workflow put in the environment.

A token minted for this task gets four-oh-three on the other project and two hundred on its own input. Expired and wrong-audience tokens get four-oh-three too.

The lock sits on the runner: project this token for this task, this audience, this hour. A short lifetime on the runner secret is still the runner secret.}
\end{frame}

\renewcommand{\track}{05  /  DISK}
\begin{frame}[t,fragile]
% title: Next job's file. Writable.
% time: 03:30-04:40
% cue: Host file changes, then the same write is denied.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Next job's file. Writable.}
\begin{lstlisting}
Path("/shared/next-job.json").write_text('{"queue":"attacker"}')

writable bind    WRITE_SUCCEEDED    host file changed
read-only bind   WRITE_DENIED       host file unchanged

Path("/output/patch.txt").write_text("legitimate patch output")
# still succeeds
\end{lstlisting}
\takeaway{Read-only in. Writable out.}
\note{The factory mounted the next job's directory into the dev sandbox and left it writable, so jobs could share a queue. The harness writes next-job.json. Ordinary open. The host file changes.

The same write through a read-only bind is denied, and the host file stays. The harness can still write its own patch to output.

That mount table is the lock. Input read-only, output writable, the next job absent. Fork this Boat disk for another harness and the files ride along. The fork kept the disk. Choosing who may write it is a separate authority.}
\end{frame}

\renewcommand{\track}{06  /  PUBLISH}
\begin{frame}[t,fragile]
% title: One allowed host. Wrong account.
% time: 04:40-05:50
% cue: 201 on the wrong account. Then the runner's own report.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
\frametitle{One allowed host. Wrong account.}
\begin{lstlisting}
POST /accounts/other/uploads
{"data":"SYNTHETIC-PRIVATE-FIXTURE"}
201  stored

request = {"operation":"publish_report","task":"parser-fix"}
# extra keys -> DENIED, nothing stored
# runner POST /accounts/team/reports
#         {"task":"parser-fix","status":"completed"}
\end{lstlisting}
\takeaway{The runner sends the POST.}
\note{The runner allows one host so the harness can publish a report or an artifact. The hostname is the one on the workflow allow list.

The harness posts the private fixture to another account on that host. The service stores it. Two-oh-one. The receiver has the bytes. The data left through a door the runner opened.

The repair keeps the POST on the runner. The harness may name one operation. Extra fields are denied and nothing is stored. The dev sandbox has no route of its own to the service. The runner still delivers the team report.

One allowed hostname still contains many operations.}
\end{frame}

\renewcommand{\track}{07  /  THE CHECK}
\begin{frame}[t,fragile]
% title: No login. Test says PASS.
% time: 05:50-07:50
% cue: Say no login. Then 401 becomes 200. Then PASS. Pause.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{No login. Test says PASS.}
\begin{lstlisting}
# expected.json   the harness can write this file
{"input":"admin:none","output":401}  # honest -> FAIL
{"input":"admin:none","output":200}  # harness writes this -> PASS
# handler.py still prints 200
# checker.py hash unchanged, overwrite DENIED
\end{lstlisting}
\vspace{2mm}
\labeltext{CI RUNNER}
\bigresult{acid}{PASS}
\takeaway{Code still open. Test agrees.}
\scope{Checker hash unchanged. Handler still prints 200.}
\note{admin:none is the lab name for one request. The route is admin. The login is none. Nobody is authenticated. A correct app returns 401. This app returns 200 for every request. That is an open admin page.

The runner decides promote from a checker. The checker program is read-only. The harness tries to overwrite it. The mount denies the write. The hash matches before and after. With the honest answer key the checker prints FAIL, because 200 is not 401. The check can see the bug.

The checker loads expected.json from the dev sandbox. The harness changes the answer for no login from 401 to 200, and does the same for the other cases. The app still returns 200. Same checker. Still read-only. It prints PASS, because the app matches the new answer key.

The runner is now green. The handler it would publish still returns two hundred. We protected the grader and left the answer key with the harness. The filename is expected.json.}
\end{frame}

\renewcommand{\track}{08  /  THREE ANSWERS}
\begin{frame}[t]
% title: Same bytes, three stations
% time: 07:50-09:05
% cue: PASS is the harness. REJECT is the runner. ACCEPT is the real fix.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Same bytes, three stations}
{\tablefont
\begin{tabular}{@{}p{.30\textwidth}p{.40\textwidth}p{.22\textwidth}@{}}
\textcolor{acid}{Bytes} & \textcolor{acid}{Who holds the key} & \textcolor{acid}{Result}\\\midrule
Open admin & Honest answer key & FAIL\\
Same code & Harness rewrote the key & \textcolor{danger}{PASS}\\
Same code & Runner's own cases & \textcolor{acid}{REJECT}\\\addlinespace
Login required & Runner's own cases & \textcolor{acid}{ACCEPT}\\
\end{tabular}}
\vspace{1mm}
{\color{muted}\fontsize{8}{10}\selectfont no-login 401\quad admin 200\quad user 403\quad public 200\quad expired 401\par}
\takeaway{The real fix still ships.}
\note{Same bad bytes. Three stations.

The honest file in the dev sandbox: FAIL. The file the harness wrote: PASS. That PASS is what the runner would promote. Cases the runner owns, outside the dev sandbox: REJECT.

Then the factory still has to ship. The corrected handler returns four-oh-one for anonymous admin and for an expired credential, four-oh-three for an ordinary user, and two hundred for the administrator and the public route. The runner accepts it.

Move the answer key to the runner. The prod sandbox can receive the real fix.}
\end{frame}

\renewcommand{\track}{09  /  PROMOTE}
\begin{frame}[t]
% title: Prod runs the judged bytes.
% time: 09:05-10:25
% cue: Denied, published, denied. Point at the order.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{Prod runs the judged bytes.}
{\tablefont
\begin{tabular}{@{}p{.08\textwidth}p{.60\textwidth}p{.22\textwidth}@{}}
1 & Fresh approval, bad bytes offered & \textcolor{danger}{DENIED}\\\addlinespace
2 & Same approval, the judged bytes & \textcolor{acid}{PUBLISHED}\\\addlinespace
3 & Same approval, those bytes again & \textcolor{acid}{DENIED}\\
\end{tabular}}
\takeaway{Denied. Published. Denied.}
\note{Green on the runner is not yet a prod sandbox. Freeze the candidate in dev before the run. Execute those frozen bytes where the harness cannot edit them. Compare against cases the runner owns. Then one fresh approval.

Bad bytes under that approval: DENIED. The judged bytes: PUBLISHED. Those bytes again: DENIED.

Say the order. Denied, published, denied. Spend the approval before the digest check, and a broken check hides behind an already-used approval. This repository used to do that.

Publication is those exact bytes and the spent approval, one transaction, in storage the dev sandbox cannot reach. The prod sandbox runs that object. A harness that prints finished holds no approval.}
\end{frame}

\renewcommand{\track}{10  /  FORK}
\begin{frame}[t]
% title: Fork the disk. Fork the key.
% time: 10:25-11:15
% cue: Say this follows from the file. Then move.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\frametitle{Fork the disk. Fork the key.}
\vspace{1mm}
\begin{columns}[T,onlytextwidth]
\begin{column}{.46\textwidth}
{\fontsize{14}{18}\selectfont Dev sandbox\\Reviewer harness\\Deploy job\par}
\end{column}
\begin{column}{.50\textwidth}
\labeltext{ON THE FORKED DISK}
{\fontsize{16}{20}\selectfont\texttt{expected.json}\par}
\vspace{1mm}
{\color{danger} The harness can write it.\par}
\end{column}
\end{columns}
\takeaway{The next sandbox keeps the answer key.}
\note{Start the reviewer harness from a fork of this Boat disk. Start the deploy job from it too. If they all read expected.json, and the dev harness can write it, three stations share one definition of done.

The fork records the disk. It does not record who was allowed to choose the criteria. This follows from the file the harness changed. A separate attack across harnesses was not run. A workspace file needs an owner before any later sandbox treats it as policy.}
\end{frame}

\renewcommand{\track}{11  /  YOUR FACTORY}
\begin{frame}[t]
% title: Rewrite four lines.
% time: 11:15-12:50
% cue: Each row: the abuse fails, and the developer's ticket still ships.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Rewrite four lines.}
{\fontsize{10}{12.5}\selectfont\renewcommand{\arraystretch}{1.32}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.22\textwidth}p{.38\textwidth}p{.32\textwidth}@{}}
\textcolor{acid}{Station} & \textcolor{acid}{Must fail} & \textcolor{acid}{Must still work}\\\midrule
Runner token & Read another project & Read this job's input\\
Dev disk & Edit the next job & Write this job's output\\
Publish host & Another account & The team report\\
The check & Edit the key or swap bytes & Release the real fix\\
\end{tabular}}
\takeaway{Block it on the runner. Ship the real ticket.}
\note{Rewrite four lines in your own factory. The abuse fails. The developer's ticket still completes.

Which token does the harness find in the dev sandbox, and what else can it read? Which paths are mounted, including a disk you will fork for the next harness? Which host can it publish to, and who writes the path and the body? Which file does the runner's check open, and can the harness edit it?

The component that returns four-oh-three, a read-only denial, DENIED, or reject sits on the runner, outside the dev sandbox. After it is there, run the harness again on the real ticket.

A green runner is still three decisions. These bytes met these criteria. This approval authorizes these bytes for the prod sandbox. The record matches an anchor the harness cannot rewrite. A clean log can store a bad promote.}
\end{frame}

\begin{frame}[plain,t]
% title: Who pressed promote?
% time: 12:50-14:00
% cue: Leave the question up. Stop by 14:00.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/DOSSIER.md
\vspace{4mm}
{\color{danger}\ttfamily\fontsize{8}{9}\selectfont 12  /  PROMOTE}\hspace{2mm}{\color{acid}\rule{14mm}{0.7pt}}\par
\vspace{3mm}
{\fontsize{26}{30}\selectfont\bfseries Who pressed promote?\par}
\vspace{3mm}
{\fontsize{15}{19}\selectfont The harness edits the proof the verifier reads.\par}
\vspace{3mm}
{\fontsize{14}{17}\selectfont Judge on the runner.\\Prod gets only those bytes.\par}
\vfill
{\color{acid}\ttfamily\href{https://github.com/zozo123/nyc-talk}{github.com/zozo123/nyc-talk}\par}
{\color{muted}\small Pier Sixty\quad 21 October 2026\par}
\note{The developer asked for a fix. The harness wrote 200 into the proof. The verifier said PASS. The recorded gate, with the cases on the runner, refuses those bytes and publishes the corrected handler.

Who pressed promote?

Keep the criteria on the runner. Freeze the candidate. Run those bytes where the harness cannot edit the cases. Publish those bytes into the prod sandbox, and only those bytes. Run the four bad lines again. Run the real ticket again.

The repository has the source, the recordings from the Boat dev sandbox, and the limits. Four doors from the abstract. Four locks. Open the file your runner's check trusts. Thank you.}
\end{frame}

\appendix
\renewcommand{\track}{A1  /  CASES}
\begin{frame}[t]
% title: The five policy cases
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The five policy cases}
{\tablefont
\begin{tabular}{@{}p{.38\textwidth}p{.23\textwidth}p{.32\textwidth}@{}}
\textcolor{acid}{CLI input} & \textcolor{acid}{Expected} & \textcolor{acid}{Always-200 handler}\\\midrule
\texttt{admin:none} & 401 & \textcolor{danger}{200: mismatch}\\
\texttt{admin:admin} & 200 & 200: match\\
\texttt{admin:user} & 403 & \textcolor{danger}{200: mismatch}\\
\texttt{public:none} & 200 & 200: match\\
\texttt{admin:expired} & 401 & \textcolor{danger}{200: mismatch}\\
\end{tabular}}
\takeaway{The repaired handler returns all five.}
\scope{admin:none means /admin with no login. A correct handler prints 401.}
\note{The handler takes a route and an authentication label and prints an integer. The runner checks these five. The repaired handler matches all five. This is the model of the check that promotes into the prod sandbox.}
\end{frame}

\renewcommand{\track}{A2  /  TOKEN AND DISK}
\begin{frame}[t]
% title: Runner token and the dev disk
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Runner token and the dev disk}
{\tablefont
\begin{tabular}{@{}p{.47\textwidth}p{.22\textwidth}p{.25\textwidth}@{}}
\textcolor{acid}{Attempt} & \textcolor{acid}{Broad grant} & \textcolor{acid}{Task grant}\\\midrule
Read other-project private fixture & HTTP 200 & HTTP 403\\
Read this job's input & HTTP 200 & HTTP 200\\\addlinespace
Write next-job host file & Changed & Denied\\
Write this job's patch & Allowed & Allowed\\
\end{tabular}}
\takeaway{The service and the mount enforce the denials.}
\scope{Loopback HTTP and Linux bind mounts inside the recorded dev sandbox.}
\note{The token case is loopback HTTP. The broad fixture stands in for the runner secret. The task fixture stands in for a job-scoped token. The mount case watches the host file after an ordinary write from the dev sandbox. This job's output stays writable.}
\end{frame}

\renewcommand{\track}{A3  /  PUBLISH}
\begin{frame}[t,fragile]
% title: Allowed host, wrong account
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Allowed host, wrong account}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}
\labeltext{HARNESS CHOOSES PATH AND BODY}
\begin{lstlisting}
POST /accounts/other/uploads
{"data":"PRIVATE-FIXTURE"}

201 STORED
receiver has the bytes
\end{lstlisting}
\end{column}
\begin{column}{.47\textwidth}
\labeltext{RUNNER OWNS THE POST}
\begin{lstlisting}
publish_report(task)

fixed recipient + body
extra fields: denied
direct route: unavailable
team report: delivered
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{One allowed hostname contains many operations.}
\scope{Loopback model of a host on the runner's allow list.}
\note{The fixture service permits both account paths, the way one allowed deploy host holds more than one operation. The delivery record shows the bytes arrived. The repair moves the POST onto the runner and removes the dev sandbox's direct route.}
\end{frame}

\renewcommand{\track}{A4  /  OLD GATE}
\begin{frame}[t,fragile]
% title: An earlier promote stored a marker
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{An earlier promote stored a marker}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{ARCHIVED BASELINE}
\begin{lstlisting}
replace candidate bytes
retain old digest field

gate: PUBLISHED
manifest != actual bytes
\end{lstlisting}
\end{column}
\begin{column}{.46\textwidth}
\labeltext{CURRENT PROMOTE}
Immutable byte entries\\Digest derived from those bytes\\Publication stores the bytes\\Approval spent in that write
\end{column}
\end{columns}
\takeaway{The old promote could record a stale digest.}
\scope{The path starts with access to the controller object.}
\note{The archived case mutates a controller-side object, keeps a stale digest, and the old gate records a publication marker. The current promote derives the digest from immutable bytes and stores those bytes. Reaching the old flaw takes access to the controller object.}
\end{frame}

\renewcommand{\track}{A5  /  LIMITS}
\begin{frame}[t]
% title: What was recorded
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{What was recorded}
{\fontsize{10}{12.5}\selectfont\renewcommand{\arraystretch}{1.28}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.30\textwidth}p{.14\textwidth}p{.48\textwidth}@{}}
\textcolor{acid}{Evidence} & \textcolor{acid}{Checks} & \textcolor{acid}{Scope}\\\midrule
Linux lab & 29 & Token, mounts, loopback publish\\
Isolated checker & 11 & Read-only checker, key, promote\\
Local factory & 6 & Promote protocol\\
Regressions & 37 & Controller and evidence\\
Old gate audit & 1 & Controller-object misuse\\
\end{tabular}}
\takeaway{Four fixtures, one factory story.}
\scope{Boat Ubuntu sandbox for the Linux run. The fork slide is a consequence.}
\note{The records bind their source. The dev sandbox recordings use bubblewrap on a Boat Ubuntu machine. The harness under test is a script. The prod step in the recording is the runner's publication of exact bytes, the shape of what a prod sandbox is allowed to execute. The forked-disk slide follows from that workspace file. The timings are a delivery budget.}
\end{frame}

\renewcommand{\track}{A6  /  REPRO}
\begin{frame}[t,fragile]
% title: Reproduce the factory
% source: https://github.com/zozo123/nyc-talk
% source: https://tessl.io/
% source: https://github.com/containers/bubblewrap#sandbox-security
% source: https://slsa.dev/spec/v1.2/verifying-artifacts
% source: https://www.anthropic.com/research/emergent-misalignment-reward-hacking
\frametitle{Reproduce the factory}
{\fontsize{10}{12.5}\selectfont
\href{https://github.com/zozo123/nyc-talk}{\textcolor{acid}{Source}}\quad github.com/zozo123/nyc-talk\par
\href{https://github.com/containers/bubblewrap#sandbox-security}{\textcolor{acid}{bubblewrap}}\quad The caller defines the sandbox policy\par
\href{https://slsa.dev/spec/v1.2/verifying-artifacts}{\textcolor{acid}{SLSA v1.2}}\quad Digest and expectations before use\par
\href{https://tessl.io/}{\textcolor{acid}{Tessl}}\quad Context, verifier, factory, 2026\par
\href{https://www.cidersecurity.io/blog/research/ppe-poisoned-pipeline-execution/}{\textcolor{acid}{Poisoned pipeline execution}}\quad Cider Security, 2022\par}
\vspace{2mm}
\begin{lstlisting}
make test && make record-all && make final
\end{lstlisting}
\scope{Disposable Linux. The deck reads recordings.}
\note{Run the fixtures on disposable Linux with bubblewrap. The deck reads those recordings. Poisoned pipeline execution is the public name for a CI job that runs an attacker-influenced file. Anthropic's reward-hacking study is prior motivation and its measurements stay with that study. Reproducing the talk needs no GitHub token.}
\end{frame}
\end{document}
