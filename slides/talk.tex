% Canonical LaTeX source. Edit this file, then run make snapshot.
% Factory: developer, AI harness, dev sandbox, GitHub CI runner, prod sandbox.
\documentclass[aspectratio=169,11pt]{beamer}
\usepackage[T1]{fontenc}
\usepackage{lmodern,listings,booktabs,array,tikz}
\usetikzlibrary{positioning,arrows.meta}
\definecolor{void}{HTML}{07090D}
\definecolor{paper}{HTML}{F2F5F7}
\definecolor{acid}{HTML}{B7FF35}
\definecolor{muted}{HTML}{A9B3BF}
\definecolor{danger}{HTML}{FF6975}
\setbeamercolor{background canvas}{bg=void}
\setbeamercolor{normal text}{fg=paper}
\setbeamercolor{frametitle}{fg=paper}
\setbeamercolor{structure}{fg=acid}
\setbeamerfont{frametitle}{size=\fontsize{19}{22}\selectfont,series=\bfseries}
\setbeamertemplate{navigation symbols}{}
\newcommand{\track}{TALK}
\setbeamertemplate{frametitle}{%
\vspace{1.2mm}{\color{danger}\fontsize{8}{9}\selectfont\ttfamily\track}\hspace{2mm}{\color{acid}\rule{14mm}{0.7pt}}\par\vspace{0.4mm}%
\insertframetitle\par\vspace{0.8mm}}
\setbeamersize{text margin left=10mm,text margin right=10mm}
\setbeamertemplate{footline}{\hspace{10mm}{\color{muted}\fontsize{6}{7}\selectfont YOSSI ELIAZ\quad PIER SIXTY\quad 21 OCT 2026}\hfill{\color{acid}\fontsize{6}{7}\selectfont\insertframenumber}\hspace{10mm}\vspace{2.4mm}}
\setlength{\parskip}{2pt}
\hfuzz=1pt
\lstset{basicstyle=\ttfamily\fontsize{10}{12.5}\selectfont,keywordstyle=\color{acid},commentstyle=\color{muted},breaklines=true,columns=fullflexible,keepspaces=true,showstringspaces=false,aboveskip=1pt,belowskip=0pt}
\newcommand{\labeltext}[1]{\par{\color{acid}\fontsize{8}{10}\selectfont\bfseries #1}\par}
\newcommand{\bigresult}[2]{\par\vspace{0.5mm}{\color{#1}\fontsize{36}{40}\selectfont\bfseries #2}\par}
\newcommand{\takeaway}[1]{\par\vfill{\fontsize{13}{16}\selectfont #1}\par}
\newcommand{\scope}[1]{\par\vspace{1mm}{\color{muted}\fontsize{7.5}{9}\selectfont #1}\par}
\newcommand{\tablefont}{\fontsize{10.5}{13}\selectfont\renewcommand{\arraystretch}{1.22}\setlength{\tabcolsep}{3pt}}
\hypersetup{pdftitle={Your Agent Escaped Without Escaping the Sandbox},pdfauthor={Yossi Eliaz},pdfsubject={The agent changes the answers. The check goes green.}}
\begin{document}

\begin{frame}[plain,t]
% title: What changed when the test went green?
% time: 00:00-01:00
% cue: Show FAIL, then PASS. Ask what changed.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\vspace{8mm}
\labeltext{ZENITY\quad AI AGENT SECURITY SUMMIT\quad PIER SIXTY}
\vspace{6mm}
{\fontsize{22}{26}\selectfont\bfseries What changed when\\the test went green?\par}
\vspace{8mm}
{\color{danger}\fontsize{28}{32}\selectfont\bfseries FAIL}\hspace{8mm}{\color{muted}\fontsize{28}{32}\selectfont$\longrightarrow$}\hspace{8mm}{\color{acid}\fontsize{28}{32}\selectfont\bfseries PASS}
\vfill
{\small Yossi Eliaz, PhD\quad Incredibuild\quad 21 October 2026\par}
\note{What changed when the test went green?

The recording starts at FAIL and ends at PASS. Same setup, one worker step in between. I am Yossi Eliaz. For the next fourteen minutes we stay on that pair of results and name the file that moved. By the end, the room should be able to point at that file and say why PASS appeared.

The worker in the recording is a script. The program is a small command-line model of an access check. It prints an integer. It does not serve an HTTP admin page, and this talk does not show a GitHub merge. The signal on the slide is the checker's own FAIL and PASS.}
\end{frame}

\renewcommand{\track}{02  /  ONE CASE}
\begin{frame}[t,fragile]
% title: No login prints 200
% time: 01:00-02:30
% cue: Name the case. Then the two numbers.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{No login prints 200}
\begin{lstlisting}
# admin:none
# model of /admin with no login
# required status: 401
print(200)
\end{lstlisting}
\takeaway{The program fails this case.}
\scope{CLI fixture. Five integer cases. Scripted worker.}
\note{One case is enough to see the failure. admin:none is the model's name for the admin route with no login. The required status is 401. The program prints 200 for every input, including this one.

A correct program prints 401 for no login, 200 for a real admin, 403 for an ordinary user, 200 for the public route, and 401 for an expired login. This program prints 200 five times. The checker will compare those integers with a file. Until that file is in view, FAIL and PASS are just two words on a slide.}
\end{frame}

\renewcommand{\track}{03  /  THE CHECKER}
\begin{frame}[t,fragile]
% title: The checker is protected
% time: 02:30-04:00
% cue: Denied first. Then the line it reads.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The checker is protected}
\begin{lstlisting}
# write to checker.py -> DENIED
expected = json.loads(Path("expected.json").read_text())
\end{lstlisting}
\takeaway{The executable holds. It reads a workspace file.}
\scope{Read-only bind mount. Hash f5d8d3468bbe before and after.}
\note{The worker tries to replace the checker with a program that prints PASS. The read-only mount returns DENIED. The checker hash is f5d8d3468bbe before that step and the same value after it.

The checker then loads expected.json from its working directory. That file is in the workspace. The protection on the executable does not cover the file the executable uses to decide success. With the honest file, this same checker prints FAIL and exits 1. The executable protection worked. The next slide is the input that protection leaves writable.}
\end{frame}

\renewcommand{\track}{04  /  WHAT MOVED}
\begin{frame}[t,fragile]
% title: Only the answer file moved
% time: 04:00-06:00
% cue: Read across the row. Stop on PASS.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Only the answer file moved}
{\fontsize{8}{10}\selectfont\renewcommand{\arraystretch}{1.35}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.16\textwidth}p{.24\textwidth}p{.24\textwidth}p{.22\textwidth}p{.08\textwidth}@{}}
 & \textcolor{acid}{Program} & \textcolor{acid}{Checker} & \textcolor{acid}{Answers} & \textcolor{acid}{}\\\midrule
Honest & \texttt{76fcb0904f51} & \texttt{f5d8d3468bbe} & \texttt{0494f1a01dee} & FAIL\\
Edited & \texttt{76fcb0904f51} & \texttt{f5d8d3468bbe} & \texttt{390e5e764916} & \textcolor{acid}{PASS}\\
\end{tabular}}
\vspace{3mm}
\begin{lstlisting}
{"input":"admin:none","output":401}
{"input":"admin:none","output":200}
\end{lstlisting}
\takeaway{The worker changed what counted as correct.}
\scope{SHA-256 prefixes of the bytes in the isolated recording.}
\note{Read the row. Program 76fcb0904f51 both times. Checker f5d8d3468bbe both times. The answer file moves from 0494f1a01dee to 390e5e764916. The verdict moves from FAIL to PASS.

The diff is the required status for admin:none, 401 becoming 200, and the same change on the other four rows. The worker changed what counted as correct. The checker executed, compared the program with that file, and the numbers matched.

The worker changed what counted as correct. The checker compared the program with the new file, and the numbers matched.}
\end{frame}

\renewcommand{\track}{05  /  HASHES}
\begin{frame}[t]
% title: What the three hashes say
% time: 06:00-07:00
% cue: One sentence per hash.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{What the three hashes say}
{\fontsize{13}{17}\selectfont
Program \texttt{76fcb0904f51} stayed.\\
Checker \texttt{f5d8d3468bbe} stayed.\\
Answers \texttt{0494f1a01dee} became \texttt{390e5e764916}.\par}
\takeaway{A hash of the new answers is not the policy.}
\note{The unchanged program hash means this comparison did not repair the program. The unchanged checker hash means this comparison did not edit that executable. The changed answer-file hash names the dependency that moved.

Hashing the altered answers does not make them authoritative. Their identity and their owner have to come from the trusted policy. In this run the owner of the new file was the worker. If you only hash the file after the worker writes it, you have a name for the new bytes and you still have the worker as the author of the criteria.}
\end{frame}

\renewcommand{\track}{05  /  WHY}
\begin{frame}[t,fragile]
% title: The file defines success
% time: 07:00-08:00
% cue: Point at the load. Then at who can write it.
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The file defines success}
\begin{lstlisting}
expected = json.loads(Path("expected.json").read_text())
# PASS when the program matches expected
\end{lstlisting}
\vspace{4mm}
{\fontsize{15}{19}\selectfont The workspace can write it.\\The checker treats it as the grade.\par}
\takeaway{Write permission included the acceptance criteria.}
\note{This is why the green result happened. expected.json looks like ordinary project data. The checker gives that file the authority to define success. The significance comes from the decision that reads it.

The worker can write the workspace. The checker runs with that workspace as its working directory. So a workspace write is a write to the acceptance criteria. An inventory of protected executables still leaves this input on the worker's side of the line.

The worker here is a script with that write in it. The recording shows the action is permitted. It does not measure whether a particular model would choose it. For an engineer or a CISO, the useful question is which input the trusted decision treats as the definition of success, and who is allowed to write that input. A list of locked binaries still leaves that question open on the table tonight.}
\end{frame}

\renewcommand{\track}{06  /  SAME PROGRAM}
\begin{frame}[t]
% title: Controller-owned criteria
% time: 08:00-09:15
% cue: REJECT the same program. The fix is the next slide.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{Controller-owned criteria}
{\fontsize{9}{11}\selectfont\renewcommand{\arraystretch}{1.4}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.28\textwidth}p{.28\textwidth}p{.36\textwidth}@{}}
\textcolor{acid}{Program} & \textcolor{acid}{Criteria} & \textcolor{acid}{Verdict}\\\midrule
\texttt{76fcb0904f51} & controller-owned cases & \textcolor{acid}{REJECT}\\
\end{tabular}}
\takeaway{The same bad program is rejected.}
\scope{Separate acceptance path. Controller compares outputs with its own cases.}
\note{The repair uses a different acceptance path. The controller runs the frozen program and compares the integers with cases it owns. It does not load expected.json from the workspace.

Program 76fcb0904f51, the one that prints 200, is REJECT. The demonstrated edit no longer moves this verdict. This path is the controller's comparison. It is a separate implementation from the workspace checker whose hash is f5d8d3468bbe.}
\end{frame}

\renewcommand{\track}{07  /  THE FIX}
\begin{frame}[t]
% title: The corrected program
% time: 09:15-10:30
% cue: Different hash. Five numbers. ACCEPT.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\frametitle{The corrected program}
\vspace{2mm}
{\fontsize{16}{20}\selectfont Program \texttt{5ead8eab70e0}\par}
\vspace{3mm}
{\ttfamily 401\quad 200\quad 403\quad 200\quad 401\par}
\vspace{4mm}
{\color{acid}\fontsize{28}{32}\selectfont\bfseries ACCEPT}
\takeaway{The legitimate task still passes.}
\scope{Same controller-owned cases. A different program.}
\note{The corrected program has hash 5ead8eab70e0. It returns 401 for no login, 200 for a real admin, 403 for an ordinary user, 200 for the public route, and 401 for an expired login. The controller accepts it.

The repair stops the demonstrated edit and keeps the legitimate task. The accepted program is not the program from the failing run. Its hash is different, and the criteria are the controller's cases.}
\end{frame}

\renewcommand{\track}{08  /  PUBLISH}
\begin{frame}[t]
% title: One unused approval
% time: 10:30-12:00
% cue: Say unused. Then denied, published, denied.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{One unused approval}
{\tablefont
\begin{tabular}{@{}p{.08\textwidth}p{.62\textwidth}p{.22\textwidth}@{}}
1 & Bad bytes, approval still unused & \textcolor{danger}{DENIED}\\\addlinespace
2 & Approved bytes, same approval & \textcolor{acid}{PUBLISHED}\\\addlinespace
3 & Approved bytes, approval now used & \textcolor{acid}{DENIED}\\
\end{tabular}}
\takeaway{Publication stores the approved bytes.}
\scope{Local gate. Exact bytes and the spent approval, one transaction.}
\note{Acceptance and publication are separate. The approval on this slide was issued for the corrected program and has not been used yet. The first attempt offers the bad program under that fresh approval. The gate returns DENIED. The second attempt offers the approved bytes and returns PUBLISHED. The third attempt offers those bytes again and returns DENIED.

Because the first denial happens while the approval is unused, the refusal is the byte check. A used approval would also deny a replay, and that would hide a bad swap. An earlier version of this gate consumed the approval first. The published row is the corrected program, compared by the controller and stored as those bytes. The first row is the important one for the question people will ask: the denial happens before the approval is spent, so replay protection is not what rejected the bad program.}
\end{frame}

\renewcommand{\track}{08  /  THREE QUESTIONS}
\begin{frame}[t]
% title: Three questions for another system
% time: 12:00-13:15
% cue: Read the three questions. Do not name a vendor.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{Three questions for another system}
{\fontsize{14}{18}\selectfont
1. Who can change the inputs that define acceptance?\par
\vspace{3mm}
2. Which bytes were the candidate, the checker, and the criteria?\par
\vspace{3mm}
3. Does publication store exactly the approved bytes?\par}
\takeaway{Ask these before calling a green check a release.}
\note{Take these three questions to another system. Who can change the inputs that define acceptance? What evidence identifies the candidate, the checker, and the criteria used for this decision? Does publication consume exactly the approved bytes?

These questions inspect a pipeline. They do not by themselves show that another product has this bug. On our recording, the worker can change expected.json, and that file defines acceptance for the workspace checker. The hashes name the candidate, the checker, and the criteria for this comparison. The unused approval shows that publication stored the approved bytes, because the bad bytes were refused while that approval was still unused.}
\end{frame}

\renewcommand{\track}{09  /  NEXT}
\begin{frame}[t]
% title: Next experiment
% time: 13:15-13:40
% cue: Read it as a question. Label it unfinished.
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{Next experiment}
\vspace{4mm}
{\color{danger}\ttfamily NEXT EXPERIMENT\par}
\vspace{4mm}
{\fontsize{15}{19}\selectfont Once the checker and the answer file are protected, which remaining input can the worker use to change the verdict?\par}
\note{Leave this up long enough to read. Once the checker and the answer file are both protected, which remaining input can the worker use to change the verdict? That experiment is not in this recording. If a later run finds such an input, that input becomes the result. If none does, the repair held for the inputs we tried. I am leaving it labeled as the next experiment so the boundary of this talk stays visible.}
\end{frame}

\begin{frame}[plain,t]
% title: The worker changed what counted as correct
% time: 13:40-14:00
% cue: PASS beside the unchanged program. Stop.
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/isolated-factory.json
\vspace{8mm}
{\fontsize{22}{27}\selectfont\bfseries The worker changed\\what counted as correct.\par}
\vspace{6mm}
{\ttfamily program \texttt{76fcb0904f51}\\
checker \texttt{f5d8d3468bbe}\\
answers \texttt{0494f1a01dee} $\longrightarrow$ \texttt{390e5e764916}\\[2mm]
\textcolor{acid}{\bfseries PASS}}
\vfill
{\color{acid}\ttfamily\href{https://github.com/zozo123/nyc-talk}{github.com/zozo123/nyc-talk}\par}
{\color{muted}\small Pier Sixty\quad 21 October 2026\par}
\note{The worker changed what counted as correct. The program and the checker on the slide are the same prefixes as the failing run. The answer file is the one that moved, and the checker printed PASS. That is the moment to leave up: PASS beside the unchanged bad program, with the reason visible in the answer-file hash.

The repository has the source and the Linux recordings. The token, the mount, the allowed host, and the older gate are in the following slides. Each has its own prerequisite. The older gate flaw needs access to a controller-side object. The worker path to that flaw is not in this recording. Thank you.}
\end{frame}

\appendix


\renewcommand{\track}{A1  /  CASES}
\begin{frame}[t]
% title: The five policy cases
% source: https://github.com/zozo123/nyc-talk/blob/main/factory/core.py
\frametitle{The five policy cases}
{\tablefont
\begin{tabular}{@{}p{.38\textwidth}p{.23\textwidth}p{.32\textwidth}@{}}
\textcolor{acid}{CLI input} & \textcolor{acid}{Expected} & \textcolor{acid}{Always-200 handler}\\\midrule
\texttt{admin:none} & 401 & \textcolor{danger}{200: mismatch}\\
\texttt{admin:admin} & 200 & 200: match\\
\texttt{admin:user} & 403 & \textcolor{danger}{200: mismatch}\\
\texttt{public:none} & 200 & 200: match\\
\texttt{admin:expired} & 401 & \textcolor{danger}{200: mismatch}\\
\end{tabular}}
\takeaway{The repaired handler returns all five.}
\scope{admin:none means /admin with no login. A correct handler prints 401.}
\note{The handler takes a route and an authentication label and prints an integer. The runner checks these five. The repaired handler matches all five. This is the model of the check that promotes into the prod sandbox.}
\end{frame}

\renewcommand{\track}{A2  /  TOKEN AND DISK}
\begin{frame}[t]
% title: Runner token and the dev disk
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Runner token and the dev disk}
{\tablefont
\begin{tabular}{@{}p{.47\textwidth}p{.22\textwidth}p{.25\textwidth}@{}}
\textcolor{acid}{Attempt} & \textcolor{acid}{Broad grant} & \textcolor{acid}{Task grant}\\\midrule
Read other-project private fixture & HTTP 200 & HTTP 403\\
Read this job's input & HTTP 200 & HTTP 200\\\addlinespace
Write next-job host file & Changed & Denied\\
Write this job's patch & Allowed & Allowed\\
\end{tabular}}
\takeaway{The service and the mount enforce the denials.}
\scope{Loopback HTTP and Linux bind mounts inside the recorded dev sandbox.}
\note{The token case is loopback HTTP. The broad fixture stands in for the runner secret. The task fixture stands in for a job-scoped token. The mount case watches the host file after an ordinary write from the dev sandbox. This job's output stays writable.}
\end{frame}

\renewcommand{\track}{A3  /  PUBLISH}
\begin{frame}[t,fragile]
% title: Allowed host, wrong account
% source: https://github.com/zozo123/nyc-talk/blob/main/lab/run.py
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json
\frametitle{Allowed host, wrong account}
\begin{columns}[T,onlytextwidth]
\begin{column}{.47\textwidth}
\labeltext{HARNESS CHOOSES PATH AND BODY}
\begin{lstlisting}
POST /accounts/other/uploads
{"data":"PRIVATE-FIXTURE"}

201 STORED
receiver has the bytes
\end{lstlisting}
\end{column}
\begin{column}{.47\textwidth}
\labeltext{RUNNER OWNS THE POST}
\begin{lstlisting}
publish_report(task)

fixed recipient + body
extra fields: denied
direct route: unavailable
team report: delivered
\end{lstlisting}
\end{column}
\end{columns}
\takeaway{One allowed hostname contains many operations.}
\scope{Loopback model of a host on the runner's allow list.}
\note{The fixture service permits both account paths, the way one allowed deploy host holds more than one operation. The delivery record shows the bytes arrived. The repair moves the POST onto the runner and removes the dev sandbox's direct route.}
\end{frame}

\renewcommand{\track}{A4  /  OLD GATE}
\begin{frame}[t,fragile]
% title: An earlier promote stored a marker
% source: https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md
% source: https://github.com/zozo123/nyc-talk/blob/main/evidence/baseline-audit.json
\frametitle{An earlier promote stored a marker}
\begin{columns}[T,onlytextwidth]
\begin{column}{.48\textwidth}
\labeltext{ARCHIVED BASELINE}
\begin{lstlisting}
replace candidate bytes
retain old digest field

gate: PUBLISHED
manifest != actual bytes
\end{lstlisting}
\end{column}
\begin{column}{.46\textwidth}
\labeltext{CURRENT PROMOTE}
Immutable byte entries\\Digest derived from those bytes\\Publication stores the bytes\\Approval spent in that write
\end{column}
\end{columns}
\takeaway{The old promote could record a stale digest.}
\scope{The path starts with access to the controller object.}
\note{The archived case mutates a controller-side object, keeps a stale digest, and the old gate records a publication marker. The current promote derives the digest from immutable bytes and stores those bytes. Reaching the old flaw takes access to the controller object.}
\end{frame}

\renewcommand{\track}{A5  /  LIMITS}
\begin{frame}[t]
% title: What was recorded
% source: https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md
\frametitle{What was recorded}
{\fontsize{10}{12.5}\selectfont\renewcommand{\arraystretch}{1.28}\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}p{.30\textwidth}p{.14\textwidth}p{.48\textwidth}@{}}
\textcolor{acid}{Evidence} & \textcolor{acid}{Checks} & \textcolor{acid}{Scope}\\\midrule
Linux lab & 29 & Token, mounts, loopback publish\\
Isolated checker & 11 & Read-only checker, key, promote\\
Local factory & 6 & Promote protocol\\
Regressions & 37 & Controller and evidence\\
Old gate audit & 1 & Controller-object misuse\\
\end{tabular}}
\takeaway{Four fixtures, one factory story.}
\scope{Boat Ubuntu sandbox for the Linux run. The fork slide is a consequence.}
\note{The records bind their source. The dev sandbox recordings use bubblewrap on a Boat Ubuntu machine. The harness under test is a script. The prod step in the recording is the runner's publication of exact bytes, the shape of what a prod sandbox is allowed to execute. The forked-disk slide follows from that workspace file. The timings are a delivery budget.}
\end{frame}

\renewcommand{\track}{A6  /  REPRO}
\begin{frame}[t,fragile]
% title: Reproduce the factory
% source: https://github.com/zozo123/nyc-talk
% source: https://tessl.io/
% source: https://github.com/containers/bubblewrap#sandbox-security
% source: https://slsa.dev/spec/v1.2/verifying-artifacts
% source: https://www.anthropic.com/research/emergent-misalignment-reward-hacking
\frametitle{Reproduce the factory}
{\fontsize{10}{12.5}\selectfont
\href{https://github.com/zozo123/nyc-talk}{\textcolor{acid}{Source}}\quad github.com/zozo123/nyc-talk\par
\href{https://github.com/containers/bubblewrap#sandbox-security}{\textcolor{acid}{bubblewrap}}\quad The caller defines the sandbox policy\par
\href{https://slsa.dev/spec/v1.2/verifying-artifacts}{\textcolor{acid}{SLSA v1.2}}\quad Digest and expectations before use\par
\href{https://tessl.io/}{\textcolor{acid}{Tessl}}\quad Context, verifier, factory, 2026\par
\href{https://www.cidersecurity.io/blog/research/ppe-poisoned-pipeline-execution/}{\textcolor{acid}{Poisoned pipeline execution}}\quad Cider Security, 2022\par}
\vspace{2mm}
\begin{lstlisting}
make test && make record-all && make final
\end{lstlisting}
\scope{Disposable Linux. The deck reads recordings.}
\note{Run the fixtures on disposable Linux with bubblewrap. The deck reads those recordings. Poisoned pipeline execution is the public name for a CI job that runs an attacker-influenced file. Anthropic's reward-hacking study is prior motivation and its measurements stay with that study. Reproducing the talk needs no GitHub token.}
\end{frame}
\end{document}
