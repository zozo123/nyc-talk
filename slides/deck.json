{
  "title": "Your Agent Escaped Without Escaping the Sandbox",
  "author": "Yossi Eliaz",
  "date": "October 21, 2026",
  "main_slides": 12,
  "slides": [
    {
      "kind": "title",
      "title": "Your Agent Escaped\nWithout Escaping\nthe Sandbox",
      "kicker": "AI AGENT SECURITY SUMMIT / NEW YORK / OCT 21, 2026",
      "subtitle": "Yossi Eliaz, PhD\nPrincipal Engineer and Head of DevRel, Incredibuild",
      "conclusion": "Four authority failures. One release decision.",
      "time": "00:00-00:35",
      "cue": "Start on the title. Do not introduce the company or sell a product.",
      "notes": "You ask a coding agent to fix authorization. Its checker reports PASS. The checker file has exactly the same hash as before. The unauthenticated admin case still returns two hundred.\n\nI am Yossi Eliaz. I will show how that happens, alongside three other failures that use capabilities we deliberately give a worker. Every demonstration uses synthetic fixtures. The worker scripts are deterministic, so we can inspect the mechanism without debating what a model intended.",
      "sources": ["https://zenity.io/resources/events/ai-agent-security-summit-2026", "https://github.com/zozo123/nyc-talk/tree/main/factory"]
    },
    {
      "kind": "boundary",
      "title": "The worker can act. Who can authorize?",
      "kicker": "THREAT MODEL",
      "panels": [
        {"label": "WORKER-CONTROLLED", "code": "task code\nwritable workspace\ntool requests\nproposed results"},
        {"label": "CONTROLLER-OWNED", "code": "task-scoped grants\nexpected results\napproval key + run\npublication store"}
      ],
      "conclusion": "The worker must not acquire the authority that judges its own output.",
      "evidence": "Trusted kernel + controller; deterministic fixtures; no model attack-success claim.",
      "time": "00:35-01:25",
      "cue": "Point to the boundary. State assumptions before showing attacks.",
      "notes": "Here is the threat model. Treat the worker as an arbitrary task program. It can edit its code and writable files, invoke its tools, and ask for its output to be published. We trust the controller and the operating-system boundary. We do not give the worker the controller's key or storage.\n\nIn the isolated runs, Linux user, PID, and mount namespaces really are different. Network access is disabled where the experiment requires it. The question is what authority remains through the interfaces we intentionally leave open.\n\nI will use four concrete interfaces: a credential, a shared mount, an upload operation, and the inputs to a checker. For each fix, the legitimate task must still work.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/lab/run.py", "https://github.com/zozo123/nyc-talk/blob/main/research/INVARIANTS.md", "https://github.com/containers/bubblewrap#sandbox-security"]
    },
    {
      "kind": "split",
      "title": "1. The inherited token reaches another project.",
      "kicker": "INHERITED CREDENTIALS",
      "panels": [
        {"label": "RUNNER TOKEN", "code": "GET /projects/other/private\nAuthorization: Bearer fixture-runner\n\n200\nSYNTHETIC-PRIVATE-FIXTURE"},
        {"label": "TASK TOKEN", "code": "GET /projects/other/private\n403\n\nGET /input/parser-fix\n200"}
      ],
      "conclusion": "Project a short-lived task grant. The service must enforce its scope.",
      "evidence": "Recorded: credentials.before / after / positive / expired / wrong-audience",
      "time": "01:25-02:35",
      "cue": "Read 200, then 403, then the legitimate 200. The tokens are public fixtures.",
      "notes": "The task needs one input. We start the worker with a synthetic runner token inherited through its environment. That token is broader than the task. The worker asks the service for another project's private fixture. The service returns two hundred and the fixture bytes.\n\nThe process has not escaped. The service has accepted a credential we placed inside it.\n\nNow use the task token. The same request returns four-oh-three. The authorized task input still returns two hundred. The service also rejects an expired token and a token for the wrong audience.\n\nThe important control is enforced scope. A short expiry reduces the exposure window, but a broad token can still do damage before it expires. Project only the grant this task needs, and validate that grant at the resource. These token strings and the private data on this slide are fixtures, not live credentials.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/lab/run.py", "https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json"]
    },
    {
      "kind": "split",
      "title": "2. The mount grants a write into the next job.",
      "kicker": "DANGEROUS MOUNTS",
      "panels": [
        {"label": "SHARED DIRECTORY: WRITABLE", "code": "open(\"/shared/next-job.json\",\n     \"w\").write(payload)\n\nWRITE_SUCCEEDED\nhost fixture changed"},
        {"label": "SHARED DIRECTORY: READ-ONLY", "code": "same write\n\nWRITE_DENIED\nhost fixture unchanged\n/output/patch.txt still works"}
      ],
      "conclusion": "Give the task a narrow input view and a separate writable output.",
      "evidence": "Recorded with real bind mounts: mount.before / after / positive",
      "time": "02:35-03:40",
      "cue": "Do not call this a filesystem escape. The write follows the mount policy.",
      "notes": "The second interface is a shared directory. In the first configuration it is writable. The worker performs an ordinary file write, and the next-job fixture on the host changes. The mount is doing exactly what we configured it to do.\n\nWe repeat the same operation with a read-only bind. The write is denied and the host fixture remains unchanged. A separate output mount still accepts the legitimate patch.\n\nThat positive control matters. We did not fix the system by making the agent unable to work.\n\nFor a system that forks snapshots or reuses workspaces, ask which writable state crosses into the next task. That is an architectural implication of sharing those bytes; we did not measure a snapshot attack here. In this experiment, the demonstrated boundary is the bind mount.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/lab/run.py", "https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json", "https://github.com/containers/bubblewrap#sandbox-security"]
    },
    {
      "kind": "split",
      "title": "3. The allowed service accepts the wrong upload.",
      "kicker": "EXFILTRATION THROUGH AN ALLOWED ENDPOINT",
      "panels": [
        {"label": "WORKER CHOOSES PATH + BODY", "code": "POST /accounts/other/uploads\n{\"data\":\"PRIVATE-FIXTURE\"}\n\n201 STORED\nreceiving service has the bytes"},
        {"label": "BROKER OWNS THE OPERATION", "code": "{\"operation\":\"publish_report\",\n \"task\":\"parser-fix\"}\n\nextra fields -> DENIED\nfixed team report -> ACCEPTED"}
      ],
      "conclusion": "Remove the direct route; mediate destination, operation, and payload.",
      "evidence": "Loopback upload fixture; isolated direct-route probe; receiver-side delivery checks.",
      "time": "03:40-05:00",
      "cue": "Explain that the service permits both account paths by design. This is a capability mistake.",
      "notes": "Here the task needs to publish a completion report. Our synthetic upload service supports a team report path and another account's upload path. Both live on the same service. The worker sends the private fixture to the other path. The service returns created, and we inspect its receiving-side delivery record to confirm the bytes arrived.\n\nA hostname decision cannot express which account should receive which data. This fixture models that distinction; it is not a test of an enterprise firewall or a SaaS vendor.\n\nIn the corrected configuration the worker has no direct route to the service. It can request one operation from a broker. The broker constructs the destination and a fixed body itself. Recipient, payload, and URL overrides are rejected. The legitimate report still arrives.\n\nBoth parts matter: the broker must constrain the operation, and the worker must not bypass it. This deliberately narrow reporting interface does not prove that every free-form output channel is free of exfiltration.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/lab/run.py", "https://github.com/zozo123/nyc-talk/blob/main/evidence/results.json"]
    },
    {
      "kind": "split",
      "title": "4. The checker is read-only. Its answer key is not.",
      "kicker": "VERIFIER TAMPERING",
      "panels": [
        {"label": "UNCHANGED CHECKER", "code": "expected = json.loads(\n  Path(\"expected.json\")\n  .read_text())\n\ncompare(handler, expected)"},
        {"label": "WORKER-EDITED ANSWER KEY", "code": "input:  admin:none\noutput: 401 -> 200\n\nchecker.py write: DENIED\nchecker.py hash: UNCHANGED"}
      ],
      "conclusion": "Protecting a checker file does not protect the inputs that define success.",
      "evidence": "Isolated factory: honest key fails; answer-key edit succeeds; unchanged checker passes.",
      "time": "05:00-06:25",
      "cue": "Pause after \"Its answer key is not.\" This is the central reveal.",
      "notes": "Now the fourth interface: the meaning of done. The policy says the unauthenticated admin case must return four-oh-one. The candidate is an intentionally broken handler that always returns two hundred. It is a small status-code model, invoked as a Python program, not a deployed web server.\n\nWe put the checker on a read-only mount. With the honest answer key, it reports FAIL. The worker attempts to overwrite the checker and the operating system denies the write.\n\nBut the checker reads expected dot json from the workspace. The worker changes that file to expect two hundred. The checker now reports PASS. Its SHA-256 is unchanged. The broken handler's bytes are unchanged too.\n\nThe failure is in what the checker trusts. Hashing its source does not bind the specification it loads. A separate machine would not repair this dependency if we copied the same worker-controlled answer key into it. The isolation experiment is recorded; the second-machine statement is the consequence of that dependency, not a separate measured trial.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py", "https://github.com/zozo123/nyc-talk/blob/main/factory/core.py"]
    },
    {
      "kind": "table",
      "title": "Keep the candidate fixed. Change who owns the cases.",
      "kicker": "CONTROLLED COMPARISON",
      "columns": ["Candidate", "Expected results", "Observed decision"],
      "rows": [["Always 200", "Honest workspace key", "FAIL"], ["Same bytes", "Worker changes key", "PASS"], ["Same bytes", "Controller-owned cases", "REJECT"], ["Correct handler", "Controller-owned cases", "ACCEPT"]],
      "conclusion": "The controller rejects the bad bytes and still accepts the repaired task.",
      "evidence": "Five fixed cases: anonymous, admin, ordinary user, public route, expired credential.",
      "time": "06:25-08:00",
      "cue": "Walk one row at a time. PASS here means the weak checker was fooled, not that the app is safe.",
      "notes": "This comparison is the evidence. Follow the first three rows: the candidate does not change. With the honest workspace key, the weak checker fails it. After the worker changes the key, the same checker passes it. With controller-owned expectations, those same candidate bytes are rejected.\n\nThen the positive control: the corrected handler is accepted. We test five cases. An anonymous admin request is four-oh-one. An administrator receives two hundred. An ordinary user receives four-oh-three. The public route returns two hundred. An expired credential receives four-oh-one.\n\nThe always-two-hundred candidate fails three of those expectations. The corrected candidate meets all five.\n\nThis is a deterministic reproduction of a known trust-boundary failure. It does not tell us how often a language model discovers the trick, and five cases do not establish universal application security. What it establishes is narrower and useful: moving the answer key outside worker control changes the decision while we hold the bad artifact fixed. That is the experiment to repeat against your own acceptance pipeline.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py", "https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md"]
    },
    {
      "kind": "pipeline",
      "title": "Make the release decision outside the worker.",
      "kicker": "REFERENCE ACCEPTANCE PROTOCOL",
      "steps": ["FREEZE", "EXECUTE", "COMPARE", "AUTHORIZE", "PUBLISH"],
      "descriptions": ["copy artifact bytes", "read-only candidate", "controller cases", "bind run + policy", "exact approved bytes"],
      "code": "approval = MAC(artifact, verifier, cases, policy,\n               environment, task, run, nonce, expiry)",
      "conclusion": "An approval is a scoped capability for one exact artifact and one decision context.",
      "evidence": "MAC is an internal shared-key model. Publication is an atomic local SQLite record of bytes.",
      "time": "08:00-09:20",
      "cue": "Point to the worker/controller split, not just the left-to-right arrows.",
      "notes": "The repair has a sequence. Copy the candidate into a narrow frozen representation. Execute those bytes with a read-only candidate mount. The controller compares observed outputs against expectations the worker cannot rewrite.\n\nOnly that controller can issue the approval. The approval binds the artifact manifest, verifier, expected results, policy, environment description, task, run, a fresh nonce, and an expiry. Publication rechecks that context and stores the exact approved bytes.\n\nIn this repository, publication is a local SQLite transaction. It is a runnable model of the release boundary, not a claim that we integrated with a production deployment system. Consuming the nonce and storing the bytes happen together.\n\nThe trust assumption remains important: the worker cannot access the controller key, database, or comparison code. A local Python subprocess with isolated-import flags is not an operating-system sandbox. That is why the local protocol tests and the Linux isolation experiments are separate evidence classes. A hash chain supports integrity checking; it does not decide whether the application behavior meets policy.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/factory/core.py", "https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py", "https://slsa.dev/spec/v1.2/verifying-artifacts"]
    },
    {
      "kind": "table",
      "title": "Challenge the gate before you trust the green check.",
      "kicker": "NEGATIVE TESTS + A POSITIVE CONTROL",
      "columns": ["Attempt", "Required outcome", "Control"],
      "rows": [["Bad candidate", "REJECT", "Good candidate accepted"], ["Fresh approval, different bytes", "DENIED", "Original bytes still publish"], ["Consumed approval", "DENIED", "One publication only"], ["Timeout / malformed output", "NO APPROVAL", "No fabricated verdict"]],
      "conclusion": "Test substitution before consuming the nonce, or the wrong defense may hide the bug.",
      "evidence": "Protocol tests cover changed context, expiry, replay, immutable bytes, and concurrent publication.",
      "time": "09:20-10:35",
      "cue": "Emphasize FRESH. Keep the controller-API audit in the appendix unless asked.",
      "notes": "Now attack the approval protocol itself. Take a fresh, unused approval for the good candidate and offer different bytes. The gate must deny it. Immediately offer the original good bytes with that same approval. They must still publish. Only then test replay.\n\nThat ordering prevents a misleading test: if you consume the nonce first, a substitution test can pass because the nonce is already used, even when artifact binding is broken. We corrected that ordering in this repository.\n\nWe also hardened our frozen representation and made publication store bytes instead of only a success marker. The appendix records the baseline flaw as controller API misuse. We have not demonstrated a worker crossing the OS boundary to reach those objects.\n\nFinally, distinguish rejection from missing evidence. A timed-out or malformed execution produces no approval. That is a fail-closed outcome, but it is not proof that the candidate met the policy. Keep that distinction in the dashboard and in the release logic.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/tests/test_factory.py", "https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md"]
    },
    {
      "kind": "boundary",
      "title": "More agents do not create an independent judge.",
      "kicker": "MULTI-AGENT IMPLICATION / NOT A MEASURED TRIAL",
      "panels": [
        {"label": "SHARED AUTHORITY", "code": "builder -> expected.json\nreviewer -> expected.json\njudge   -> expected.json\n\nthree roles, one writable key"},
        {"label": "SEPARATE AUTHORITY", "code": "workers -> proposals\ncontroller -> policy\nisolated runs -> observations\n\none controlled publication gate"}
      ],
      "conclusion": "Independence follows write authority and trusted dependencies, not agent count.",
      "evidence": "Architectural inference from the answer-key experiment. No multi-agent benchmark is claimed.",
      "time": "10:35-11:40",
      "cue": "Connect to software factories without adding untested phase/temperature claims.",
      "notes": "This matters when one worker becomes a software factory. You may have a builder, a reviewer, and a judge. They may run different models or use different prompts. But if all three read a writable answer key produced by the builder, you have replicated the dependency.\n\nTrace who can write the specification, fixtures, dependencies, evidence, and final approval. A second role or a second machine is not automatically a second authority boundary.\n\nYou can still let many workers explore, generate competing patches, and propose tests. Keep those proposals distinct from the criteria authorized to promote a release. The final publication path should accept only an approval bound to the selected artifact and decision context.\n\nThat is an architectural inference from the dependency we just measured. We are not presenting a multi-agent benchmark or claiming that changing model temperature is a security control.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/research/INVARIANTS.md", "https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py"]
    },
    {
      "kind": "table",
      "title": "Four tests to run on your own deployment.",
      "kicker": "TAKE-HOME CHECKLIST",
      "columns": ["Boundary", "Try the unauthorized action", "Keep this working"],
      "rows": [["Credential", "Read a different project", "Task input"], ["Filesystem", "Write outside task output", "Legitimate patch"], ["Egress", "Change recipient or body", "Authorized report"], ["Acceptance", "Change cases or swap bytes", "Verified artifact"]],
      "conclusion": "For every denial, require a positive control that still completes the task.",
      "evidence": "Scope credentials; narrow mounts; enforce broker-only egress; bind independent acceptance to bytes.",
      "time": "11:40-13:10",
      "cue": "This is the audience photo slide. Give it a pause.",
      "notes": "These are the four tests I would take back to a deployment review.\n\nUse the worker's credential to ask for another project's data. The resource should deny it while the real task input remains available.\n\nAttempt a write outside the task's output area. Confirm the shared input stays unchanged and the legitimate patch still appears.\n\nTry to change the recipient or body of the permitted report. Verify both that the broker refuses the request and that the worker cannot bypass the broker. Inspect the receiving system rather than trusting a worker log.\n\nFinally, change an input that determines success. Then try to substitute bytes under a fresh approval. The acceptance decision must use criteria outside worker write authority, and publication must bind to the bytes that were evaluated.\n\nEach denial should come from a component the worker cannot rewrite. Each test needs a positive control. Otherwise a completely broken system can look perfectly secure because nothing ever completes. The repository contains the fixtures, raw observations, source hashes, and reproduction commands so you can inspect exactly what each test means.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/lab/run.py", "https://github.com/zozo123/nyc-talk/blob/main/factory/isolated.py", "https://github.com/zozo123/nyc-talk/blob/main/RUNBOOK.md"]
    },
    {
      "kind": "close",
      "title": "The worker may propose the artifact.\nIt must not define why that artifact\nis eligible for release.",
      "kicker": "KEEP EXECUTION AND ACCEPTANCE AS SEPARATE AUTHORITIES",
      "subtitle": "github.com/zozo123/nyc-talk",
      "code": "make test\nmake demo\nmake factory-isolated",
      "conclusion": "Source. Evidence. Counterexamples. Positive controls.",
      "time": "13:10-14:00",
      "cue": "Stop at the repository address. Leave the closing slide visible. One minute remains.",
      "notes": "The four failures used interfaces we intentionally gave the worker: a token, a mount, a permitted service, and an answer key. The useful question is not only whether the process can leave its sandbox. It is which decisions the process can cause while it remains inside.\n\nLet the worker propose code, tests, and explanations. Keep the authority that defines acceptance outside its write boundary, and bind the release to the exact artifact that authority evaluated.\n\nThe source and recorded evidence are at github dot com, zozo one two three, nyc dash talk. Thank you.",
      "sources": ["https://github.com/zozo123/nyc-talk"]
    },
    {
      "kind": "table",
      "title": "Appendix / What the evidence actually establishes",
      "appendix": true,
      "columns": ["Evidence class", "Execution", "Limit"],
      "rows": [["Isolation lab", "Linux namespaces + mounts", "29 assertions, not 29 vulnerabilities"], ["Checker experiment", "Read-only checker + mutable key", "Deterministic scripts; five-case model"], ["Protocol regressions", "Local controller tests", "No OS isolation claim"], ["Multi-agent / snapshots", "Architectural implications", "Not measured attack trials"]],
      "conclusion": "No vendor zero-day, customer incident, model success rate, or general sandbox certification.",
      "notes": "Use this slide to answer scope questions. The documented baseline API flaw is ours. Kernel compromise, malicious controllers, timing channels and full application correctness are outside the experiments. The lab uses loopback HTTP for credentials and upload; the admin policy is a CLI status-code model.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/research/RESULTS.md", "https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md"]
    },
    {
      "kind": "table",
      "title": "Appendix / The five policy cases",
      "appendix": true,
      "columns": ["Input", "Expected", "Always-200 candidate"],
      "rows": [["admin:none", "401", "200: mismatch"], ["admin:admin", "200", "200: match"], ["admin:user", "403", "200: mismatch"], ["public:none", "200", "200: match"], ["admin:expired", "401", "200: mismatch"]],
      "conclusion": "CLI fixture: python3 handler.py admin:none. These are modeled status codes.",
      "notes": "Three of the five outputs differ for the broken candidate. These tests establish only the five stated criteria. The corrected handler covers them; the experiment does not prove authentication correctness for an actual service.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/factory/core.py"]
    },
    {
      "kind": "split",
      "title": "Appendix / We audited our own reference gate.",
      "appendix": true,
      "panels": [
        {"label": "PINNED BASELINE: 4b06c51", "code": "frozen.files[\"handler.py\"] = BAD\nfrozen.digest stays unchanged\n\ngate says PUBLISHED\nmanifest no longer matches bytes"},
        {"label": "CORRECTED PROTOCOL", "code": "immutable byte entries\nderived manifest + digest\nfresh substitution test\natomic nonce + byte publication\nconcurrent replay regression"}
      ],
      "conclusion": "Internal API integrity flaw. No demonstrated worker-to-controller exploit path.",
      "notes": "At the pinned baseline, a caller with controller Python-object access could mutate files without updating the stored digest. The original publish function logged a marker, not candidate bytes. We reproduce those facts and do not describe them as a proven production exploit. The fix makes the bytes immutable, derives digests, and records exact bytes transactionally.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/research/AUDIT.md", "https://github.com/zozo123/nyc-talk/blob/main/research/baseline/core.py", "https://github.com/zozo123/nyc-talk/blob/main/tests/test_factory.py"]
    },
    {
      "kind": "code",
      "title": "Appendix / Reproduce without cloud credentials.",
      "appendix": true,
      "code": "make test                 # controller protocol regressions\nmake factory              # local fixture logic\nmake demo                 # Linux namespace / mount lab\nmake factory-isolated     # read-only checker / answer key\nmake record-all            # refresh evidence after edits\nmake snapshot              # compile the Beamer PDF",
      "conclusion": "Linux + bubblewrap for isolated runs. Python standard library for experiments. TeX Live for the deck.",
      "notes": "A failure to create a namespace is an infrastructure failure, not a successful defense. CI runs the isolated experiments on Ubuntu. The stage presentation uses the saved PDF and recorded transcript; it does not require venue Wi-Fi or cloud provisioning. make record-all intentionally runs isolated experiments and must succeed before refreshing evidence.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/Makefile", "https://github.com/zozo123/nyc-talk/blob/main/.github/workflows/verify.yml"]
    },
    {
      "kind": "split",
      "title": "Appendix / Tamper evidence is not a verdict.",
      "appendix": true,
      "panels": [
        {"label": "HISTORY CHECK", "code": "head = H(previous || event)\n\nrewrite events\nrecompute chain\ncompare to controller anchor"},
        {"label": "ACCEPTANCE CHECK", "code": "frozen candidate\ncontroller-owned policy\nindependent comparison\nscoped approval\nexact-byte publication"}
      ],
      "conclusion": "The chain detects a rewrite only against an independently retained trusted anchor.",
      "notes": "An attacker who can rewrite the log and its trusted anchor can recompute a consistent chain. The lab checks disagreement with a controller-held head; it does not deploy a transparency service. Neither a chain nor a signature establishes that the application satisfies its task. Those mechanisms authenticate evidence and decisions under explicit trust assumptions.",
      "sources": ["https://github.com/zozo123/nyc-talk/blob/main/lab/run.py", "https://slsa.dev/spec/v1.2/verifying-artifacts"]
    },
    {
      "kind": "sources",
      "title": "Appendix / Prior work and source map",
      "appendix": true,
      "items": [["Primary experiments", "github.com/zozo123/nyc-talk"], ["Sandbox policy is caller-defined", "github.com/containers/bubblewrap"], ["Provenance verification + artifact binding", "slsa.dev/spec/v1.2/verifying-artifacts"], ["Reward hacking in programming tasks", "Anthropic, Nov 21, 2025: emergent misalignment"], ["Event / date", "Zenity AI Agent Security Summit 2026: Oct 21, NYC"]],
      "conclusion": "A reproducible boundary demonstration, with explicit limits.",
      "notes": "The Anthropic work is related motivation, not evidence that our deterministic scripts represent any deployed model. The SLSA reference motivates artifact and trusted-identity binding; this repository does not claim SLSA certification. Bubblewrap documents that security depends on its supplied policy. The event page verifies the NYC date; it does not independently confirm this speaker slot length.",
      "sources": ["https://github.com/zozo123/nyc-talk", "https://github.com/containers/bubblewrap#sandbox-security", "https://slsa.dev/spec/v1.2/verifying-artifacts", "https://www.anthropic.com/research/emergent-misalignment-reward-hacking", "https://zenity.io/resources/events/ai-agent-security-summit-2026"]
    }
  ]
}
